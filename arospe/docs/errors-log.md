# Errors Log

A structured log of real mistakes made in this project and the concrete rule adopted to avoid repeating them. Not a general bug tracker — only entries that produced a lasting convention belong here.

**Older entries have been archived.** Entries dated 2026-07-21 through 2026-08-19 (the oldest fourteen) were moved, byte-for-byte and unedited, to [errors-log-archive.md](errors-log-archive.md) to keep this file under a manageable size — history is never rewritten in place, only relocated. Any correction or narrowing blockquote that was already attached to an archived entry moved with it. Check the archive before assuming a mistake from before 2026-08-20 was never recorded.

## Browse by topic

An agent working on a specific domain can jump straight to the 1-3 relevant entries below instead of reading the whole log top to bottom — see [contracts.md](contracts.md#token-efficient-reading-and-dispatch-rule)'s Token-Efficient Reading and Dispatch Rule. Covers all 46 entries across this file and the archive; an entry touching more than one domain is listed under each. `(archive)` marks an entry that lives in [errors-log-archive.md](errors-log-archive.md).

**Livewire/Blade/Flux rendering & compilation quirks**
- [CSS Grid's default `align-items: stretch` lets one tall sibling cell distort a Flux `<ui-field>`'s own internal row heights in its unrelated neighbours](#css-grids-default-align-items-stretch-lets-one-tall-sibling-cell-distort-a-flux-ui-fields-own-internal-row-heights-in-its-unrelated-neighbours--2026-09-11) — 2026-09-11
- [A `data-test` hook on `<flux:modal>` itself is always present, open or closed — a "the modal stayed open" assertion needs the hook on conditionally-rendered content inside it](#a-data-test-hook-on-fluxmodal-itself-is-always-present-open-or-closed--a-the-modal-stayed-open-assertion-needs-the-hook-on-conditionally-rendered-content-inside-it--2026-09-10) — 2026-09-10
- [A SECOND `->call()` on an already-mounted `Livewire::test()` component does not re-throw `AuthorizationException` the way the first one does](#a-second---call-on-an-already-mounted-livewiretest-component-does-not-re-throw-authorizationexception-the-way-the-first-one-does--2026-09-10) — 2026-09-10
- [Livewire skips `ConvertEmptyStringsToNull`/`TrimStrings`, and Laravel skips non-implicit rules for a blank string — the two combine to let a raw `''` reach a `DECIMAL` column](#livewire-skips-convertemptystringstonulltrimstrings-and-laravel-skips-non-implicit-rules-for-a-blank-string--the-two-combine-to-let-a-raw--reach-a-decimal-column--2026-09-10) — 2026-09-10
- [A `flux:fieldset` wrapping a Flux field silently swallows its auto-rendered validation error](#a-fluxfieldset-wrapping-a-flux-field-silently-swallows-its-auto-rendered-validation-error--2026-09-07) — 2026-09-07
- [Absolutely-positioned dropdown occludes a sibling via DOM order, not CSS](#an-absolutely-positioned-element-with-no-explicit-top-takes-its-static-position-from-dom-order-and-can-silently-occlude-a-sibling--2026-08-31) — 2026-08-31
- [A Livewire directive modifier's duration can't be interpolated into an attribute NAME](#a-livewire-directive-modifiers-duration-cannot-be-interpolated-as-part-of-a-component-tags-attribute-name--2026-08-31) — 2026-08-31
- [A bare `@disabled(...)` inside a `<flux:button>` tag corrupts the whole compiled view](#a-bare-disableddisabled-inside-a-fluxbutton-tags-attribute-list-corrupts-the-whole-compiled-view--2026-08-31) — 2026-08-31
- [Two `@directive(...)` calls in one component-tag attribute string silently fail to compile](#two-directive-calls-in-one-blade-component-tags-attribute-string-silently-fail-to-compile--2026-08-26) — 2026-08-26
- [A `null` Livewire property bound to a native `<select>` silently dropped the user's own pick](errors-log-archive.md#a-null-livewire-property-bound-to-a-native-select-silently-dropped-the-users-own-pick--2026-08-16) — 2026-08-16 (archive)
- [`disabled:cursor-not-allowed` on a Flux button was never the cursor the user saw](errors-log-archive.md#disabledcursor-not-allowed-on-a-flux-button-was-never-the-cursor-the-user-saw--2026-08-16) — 2026-08-16 (archive)
- [A conditionally-bound `tooltip` prop rendered an empty tooltip on every enabled row](errors-log-archive.md#a-conditionally-bound-fluxbutton-tooltip-prop-rendered-an-empty-tooltip-on-every-enabled-row--2026-08-16) — 2026-08-16 (archive)

**Testing/QA process & infrastructure**
- [A SECOND `->call()` on an already-mounted `Livewire::test()` component does not re-throw `AuthorizationException` the way the first one does](#a-second---call-on-an-already-mounted-livewiretest-component-does-not-re-throw-authorizationexception-the-way-the-first-one-does--2026-09-10) — 2026-09-10
- [Two `php artisan test` invocations against the same worktree's testing database, run concurrently, produced ~47 spurious failures across completely unrelated tests](#two-php-artisan-test-invocations-against-the-same-worktrees-testing-database-run-concurrently-produced-47-spurious-failures-across-completely-unrelated-tests--2026-09-10) — 2026-09-10
- [A geography-entry factory override to `name` silently left `normalized_name` stale, making a search test fail against real search code](#a-geographyentryfactory-override-to-name-silently-left-normalized_name-stale-making-a-search-test-fail-against-real-search-code--2026-09-07) — 2026-09-07
- [Playwright's `selectOption` fires `change` unconditionally, so no `->select()`-driven test can be a null-`<select>` desync's regression net](#playwrights-selectoption-fires-change-unconditionally-so-no--select-driven-browser-test-can-be-the-regression-net-for-a-null-select-desync--2026-09-06) — 2026-09-06
- [A verification record listing two of three quality gates is a record of two gates](#a-verification-record-that-lists-two-of-three-quality-gates-is-a-record-of-two-gates--2026-08-26) — 2026-08-26
- [A count-based HTML assertion counted a wrapper element it never meant to include](#a-count-based-assertion-over-rendered-html-counted-a-wrapper-element-it-never-meant-to-include--2026-08-21) — 2026-08-21
- [Both per-change quality gates are scoped by default, and both silently passed](#both-of-this-projects-per-change-quality-gates-are-scoped-by-default-and-both-silently-passed--2026-08-20) — 2026-08-20
- [A planned test asserted a refusal by `verified`, a middleware that refuses nobody in this app](#a-planned-test-asserted-a-refusal-by-verified-a-middleware-that-refuses-nobody-in-this-app--2026-08-20) — 2026-08-20
- [A Pest `arch()` rule over an array of namespaces shipped green while proving nothing](errors-log-archive.md#a-pest-arch-rule-over-an-array-of-namespaces-shipped-green-while-proving-nothing--2026-08-18) — 2026-08-18 (archive)
- [Two agents dispatched in parallel both wrote to the same Blade view](errors-log-archive.md#two-agents-dispatched-in-parallel-both-wrote-to-the-same-blade-view--2026-08-16) — 2026-08-16 (archive)
- [A test asserted against a fixture address the local `.env` also pointed `SUPER_ADMIN_EMAIL` at](errors-log-archive.md#a-test-asserted-against-a-fixture-address-that-the-local-env-also-pointed-super_admin_email-at--2026-08-12) — 2026-08-12 (archive)

**Environment & infrastructure gotchas** (stale build artifacts, container/db config, resource limits)
- [A `.env.testing` missing most of `.env`'s content produced a wide spray of unrelated-looking Feature test failures](#a-envtesting-missing-most-of-envs-content-produced-a-wide-spray-of-unrelated-looking-feature-test-failures--2026-09-06) — 2026-09-06
- [GitHub Actions' Ubuntu runner ships `libheif` with no AVIF encoder, so Imagick silently wrote a 0-byte `.avif`](#github-actions-ubuntu-runner-ships-libheif-with-no-avif-encoder-so-imagick-silently-wrote-a-0-byte-avif--2026-09-06) — 2026-09-06
- [A stale `public/hot` file made all 19 browser tests time out, misread as real UI bugs](#a-stale-publichot-file-from-an-old-npm-run-dev-session-made-all-19-browser-tests-time-out-misread-as-real-ui-bugs--2026-08-28) — 2026-08-28
- [An Imagick resource limit was requested in the wrong unit, masked by `policy.xml` clamping it](#an-imagick-resource-limit-was-requested-in-the-wrong-unit-masked-by-the-hosts-own-policyxml-silently-clamping-it--2026-08-28) — 2026-08-28
- [A "runs as non-root" claim was re-verified using the wrong `sail` invocation](#a-test-suites-own-runs-as-non-root-claim-was-re-verified-using-the-wrong-sail-invocation--2026-08-28) — 2026-08-28
- [The full test suite degrades under repeated heavy runs in one session, unrelated to any code change](#the-full-test-suite-run-repeatedly-and-heavily-in-one-session-degrades-in-ways-unrelated-to-any-code-change--2026-08-28) — 2026-08-28
- [A missing `.env.testing` let a self-healing `migrate:fresh` wipe the shared dev database](#a-missing-envtesting-file-let-a-self-healing-migratefresh-wipe-the-shared-dev-database--2026-08-26) — 2026-08-26

**Database/transactions/migrations**
- [`DB::transaction($fn, attempts: N)` retried a closure that mutated a model created outside it](#dbtransactionfn-attempts-n-retried-a-closure-that-mutated-a-model-created-outside-it-producing-a-silent-lost-update-reported-as-success--2026-09-04) — 2026-09-04
- [Wrapping existing code in `DB::transaction()` moved a cache flush nobody had written](#wrapping-existing-code-in-a-dbtransaction-moved-a-cache-flush-nobody-had-written--2026-08-21) — 2026-08-21
- [A redundant `users_uuid_unique` index survived the UUID primary-key conversion](errors-log-archive.md#a-redundant-users_uuid_unique-index-survived-the-uuid-primary-key-conversion--2026-08-12) — 2026-08-12 (archive)

**Authorization/security**
- [A scope exclusion named screens while the story edited a class those screens share](#a-scope-exclusion-named-screens-while-the-story-edited-a-class-those-screens-share--2026-08-24) — 2026-08-24
- [Wrapping existing code in `DB::transaction()` moved a permission-cache flush nobody had written](#wrapping-existing-code-in-a-dbtransaction-moved-a-cache-flush-nobody-had-written--2026-08-21) — 2026-08-21
- [A guard took the state it was guarding as a parameter, reopening its own hole one level up](#a-guard-took-the-state-it-was-guarding-as-a-parameter-reopening-its-own-hole-one-level-up--2026-08-20) — 2026-08-20
- [A security page documented the vulnerable code as current, because it was written before its own fix](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) — 2026-08-20
- [Two of three security-audit rounds found the flaw in the previous round's fix](errors-log-archive.md#two-of-the-three-security-audit-rounds-found-the-flaw-in-the-previous-rounds-fix--2026-08-19) — 2026-08-19 (archive)
- [A listener read the pre-save value with `getOriginal()`, already overwritten by `save()`](errors-log-archive.md#a-listener-read-the-pre-save-value-with-getoriginal-which-save-had-already-overwritten--2026-08-17) — 2026-08-17 (archive)
- [Freeing a deleted user's email left their password-reset token live](errors-log-archive.md#freeing-a-deleted-users-email-left-their-password-reset-token-live--2026-08-14) — 2026-08-14 (archive)
- [A row carrying the configured email was treated as proof of mailbox ownership](errors-log-archive.md#a-row-carrying-the-configured-email-was-treated-as-proof-of-mailbox-ownership--2026-08-09) — 2026-08-09 (archive)

**Actions/API contract design** (parameter defaults, ambiguity)
- [An action's own parameter default reintroduced the omission ambiguity its stricter collaborator was built to close](#an-actions-own-parameter-default-reintroduced-the-omission-ambiguity-its-stricter-collaborator-was-built-to-close--2026-09-01) — 2026-09-01
- [A guard took the state it was guarding as a parameter, reopening its own hole one level up](#a-guard-took-the-state-it-was-guarding-as-a-parameter-reopening-its-own-hole-one-level-up--2026-08-20) — 2026-08-20

**Docs/process/review quality**
- [One docs pass reported two gaps that were not there, both marked "verified"](#one-docs-pass-reported-two-gaps-that-were-not-there-both-marked-verified--2026-08-29) — 2026-08-29
- [A reviewer's "correction" replaced an accurate technical explanation with a wrong one, unverified](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) — 2026-08-24
- [A deferred story's findings were claims about a tree that no longer existed](#a-deferred-storys-findings-were-claims-about-a-tree-that-no-longer-existed-and-one-of-them-would-have-reopened-a-bug-in-this-log--2026-08-23) — 2026-08-23
- [A security page documented the vulnerable code as current, because it was written before its own fix](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) — 2026-08-20
- [A task file's relative links broke silently when it moved to `in-progress/`/`done/`](errors-log-archive.md#a-task-files-relative-links-broke-silently-when-it-moved-to-in-progressdone--2026-08-17) — 2026-08-17 (archive)
- [A doc's "this app has no X yet" claim outlived the X by two tasks](errors-log-archive.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13) — 2026-08-13 (archive)
- [Gherkin scenarios written with a generic "I" and bundled multi-action steps](errors-log-archive.md#gherkin-scenarios-written-with-a-generic-i-and-bundled-multi-action-steps--2026-07-21) — 2026-07-21 (archive)

## Entry format

Newest entry first, directly below this line. Every entry uses this exact structure:

```markdown
## <short problem title> — <YYYY-MM-DD>
- **Context**: what was being worked on
- **What happened**: observed symptom/error
- **Root cause**: why it happened
- **Fix applied**: what changed, with a file path or commit/PR reference
- **How to avoid it next time**: a concrete, actionable rule — link to a `conventions/` doc if one covers it
```

## CSS Grid's default `align-items: stretch` lets one tall sibling cell distort a Flux `<ui-field>`'s own internal row heights in its unrelated neighbours — 2026-09-11

- **Context**: bugfix pass on `resources/views/livewire/shipping.blade.php`'s create/edit rate modal (`App\Livewire\Shipping\Index`, story 0037's own screen). A user report: the Min./Max. weight/Price row rendered visibly misaligned — the "Max. weight (kg)" input sat noticeably lower than its two siblings.
- **What happened**: the three fields sit in one `grid grid-cols-3` row. "Max. weight" alone carries a `:description` help text ("Leave blank for 'and above' -- no upper limit."). `flux:with-field` renders `:description` **before** the slotted control inside its own `<ui-field>` (a `display: grid` custom element with one implicit row per direct child), so the extra description row pushed that one field's *input* down relative to its siblings, which have no description of their own. Moving the help text to `:description:trailing` (rendered *after* the control) was tried first and made a second, unrelated bug visible: the translation string's real double quotes, echoed once by `with-field.blade.php`'s own `{{ $description }}`, rendered as a literal `&quot;` in the browser instead of a real quote — reproduced by execution, not by reading the Blade source. Moving the help text **outside** `flux:input` entirely (a plain sibling `<flux:text>` after it, inside a wrapping `<div>`) fixed the quote-escaping regression but only *narrowed* the misalignment (measured 40px → 18px) rather than closing it: with the wrapper cell now taller than its siblings (from the extra sibling text), the two *other* fields' own `<ui-field>` — untouched, identical markup — measured a label row height of 35.5px instead of their normal 17.5px, verified with `getComputedStyle`/`getBoundingClientRect` via a headless-browser diagnostic script, not assumed from the Blade source.
- **Root cause**: the `grid grid-cols-3` container had no `align-items` override, so it used CSS Grid's default `stretch` — every cell in the row is stretched to the height of the tallest one. Once "Max. weight"'s own cell grew taller (from the sibling help text), its neighbours' `<ui-field>` cells (still `display: grid` internally, with an implicit-row layout) were stretched too, and that extra stretched height was redistributed across **their own** internal grid rows in a way that shifted their label/input position — not merely added blank space at the bottom. This reproduced with byte-identical DOM structure between the tall and short fields (confirmed by diffing `outerHTML`), so the discrepancy is purely a stretch/redistribution effect, not a markup difference.
- **Fix applied**: added `items-start` to the row's grid container (`class="grid grid-cols-1 items-start gap-4 sm:grid-cols-3"`), so every cell keeps its own natural height instead of stretching to the tallest. Combined with the help text living outside `flux:input` (a sibling `<flux:text>` with an explicit `id`, referenced by an explicit `aria-describedby` on the input — restoring the accessibility association `:description` would otherwise have wired up automatically), all three inputs now measure the exact same `top` in the rendered DOM. Verified visually and by measurement (not merely "no PHP/console error") before and after. See `resources/views/livewire/shipping.blade.php`'s inline comment at the grid container for the full reasoning, kept in place rather than only in this log.
- **How to avoid it next time**: **a CSS Grid row of Flux fields with uneven natural heights (one field carries a description/help text the others don't) must not rely on the default `align-items: stretch`** — it does not merely add blank space to the shorter cells, it can let the stretched height propagate into a Flux `<ui-field>`'s *own* internal grid layout and shift where its label/input actually sit, even though that cell's markup never changed. Add `items-start` (or `items-end` if anchoring to the bottom is the actual intent) to any grid row mixing fields with and without a description/help text. Separately: **`:description:trailing` is not a safe drop-in replacement for `:description` in this codebase** — it double-escapes a translation string containing literal quote characters (verified by execution, rendering a literal `&quot;`); when a field's help text must not affect sibling alignment, move it to a plain sibling element outside `flux:input` instead, with an explicit `id`/`aria-describedby` pair to keep the accessibility association `with-field.blade.php` would otherwise provide for free. Diagnose alignment bugs like this by measuring the real rendered DOM (`getBoundingClientRect`/`getComputedStyle` via a headless browser) before proposing a fix — reasoning from the Blade source alone would not have predicted either failure mode here.

> **Widened same day (2026-09-11) — the first pass fixed only the reported row and missed a second, structurally identical instance in the same modal.** The Carrier/Zone row above the weight row (`grid grid-cols-1 gap-4 sm:grid-cols-2`) has the exact same shape: "Zone"'s cell is taller than "Carrier"'s because it carries a "Manage shipping zones" link underneath the select, and the grid had no `align-items` override either — so "Carrier"'s own `<ui-field>` was distorted the identical way, only surfaced once the user pointed at a still-misaligned screenshot after the first fix shipped. **The rule this adds: when this failure mode is found in one grid row, grep the same file for every other `grid grid-cols-` container and check each one for a sibling with extra content (a link, a help text, a badge) that makes its cell taller than its neighbours' — fixing only the row a screenshot happened to show is not the same as fixing the file.** `items-start` was added to this second grid too; both grids in this modal now carry it.

## A `data-test` hook on `<flux:modal>` itself is always present, open or closed — a "the modal stayed open" assertion needs the hook on conditionally-rendered content inside it — 2026-09-10

- **Context**: story 0039 (payment methods store-settings screen — UI), the edit modal on `resources/views/livewire/payment-methods.blade.php`. B2's browser test — the highest-risk scenario on this screen, per the task's own analysis — must prove a checksum-invalid IBAN rejection leaves the modal **open** rather than closed, alongside proving the previously-configured IBAN keeps showing and the database is unchanged. The first draft put `data-test="payment-method-modal"` directly on the outer `<flux:modal name="payment-method-modal" wire:model="showModal">` tag, the same tag every other property on the modal (`wire:model`, `@close`) already lives on.
- **What happened**: found during this story's own code review before it shipped as a live test gap, rather than after — `assertPresent('@payment-method-modal')` would have passed regardless of whether the save actually failed and the modal genuinely stayed open, because the hook was on an element Flux renders **unconditionally**. A test written against it would have looked like it verified "the modal remains open" while actually verifying nothing about `$showModal`'s real value.
- **Root cause**: `<flux:modal>` always renders its own wrapper markup in the DOM — Flux implements open/closed as CSS/JS visibility state on that wrapper (the underlying `<dialog>`/`<div>` element), not as a PHP conditional that removes the tag from the response. Livewire's own `wire:model="showModal"` binding controls *visibility*, not *presence*. So a hook placed on `<flux:modal>` itself answers "did this component mount a modal at all" (always true on this screen, from first paint), never "is the modal open right now" — the two questions look identical from the Blade source but are not the same assertion.
- **Fix applied**: `data-test="payment-method-modal"` moved onto the modal's own **inner** `<div>`, which is wrapped in `@if ($showModal)` inside the `<flux:modal>` tag — so the hook is only ever in the rendered HTML while the modal is genuinely open. `tests/Browser/PaymentMethodsIndexTest.php`'s B2 (`assertPresent('@payment-method-modal')` after a rejected save) and B3 (`assertMissing('@payment-method-modal')` after Cancel) both now assert something real, and both carry an inline comment stating why the hook sits where it does.
- **How to avoid it next time**: **when a test needs to assert a Flux modal's open/closed state structurally (not by inferring it from a side effect), put the `data-test` hook on content that is itself conditionally rendered inside the modal — `@if ($showModal) ... @endif` — never on the `<flux:modal>` tag itself, which is present in the DOM from first paint regardless of open/closed state.** This generalises past `<flux:modal>` to any Flux component whose "closed" state is implemented as CSS/JS visibility rather than PHP conditional rendering (a `flux:dropdown`'s popover body is the same shape) — verify by reading the rendered HTML with the component in both states, not by reasoning from the Blade source, the same discipline this log's other Flux/Blaze entries already establish.

## A SECOND `->call()` on an already-mounted `Livewire::test()` component does not re-throw `AuthorizationException` the way the first one does — 2026-09-10

- **Context**: story 0038 (payment methods backend), Phase 5 code review finding N1 — adding a test that revokes `payment-methods.edit` **between** `App\Livewire\PaymentMethods\Index::openEditModal()` succeeding and `save()` being called, to prove `save()` carries its own gate rather than inheriting one only from `openEditModal()`.
- **What happened**: the first draft wrote `expect(fn () => $component->set('iban', ...)->call('save'))->toThrow(AuthorizationException::class);` — the identical shape every `openEditModal()`-refusal test in this app already uses successfully (e.g. `expect(fn () => Livewire::test(Index::class)->call('openEditModal', ...))->toThrow(AuthorizationException::class)`). It failed with `Exception "Illuminate\Auth\Access\AuthorizationException" not thrown.` A debug reproduction confirmed the gate genuinely ran and genuinely refused — `$method->fresh()->iban` stayed at its original value after the call — but no PHP exception of any kind propagated out of `->call('save')` for `expect()->toThrow()` to catch.
- **Root cause**: not fully traced to a specific line in Livewire's internals, but empirically confirmed to depend on **which call in the chain** the exception is thrown from. `Livewire::test(Index::class)` (the component's *first* mount, plus any method chained directly onto that same expression) runs synchronously in the test's own call stack, so an exception thrown there is a normal PHP exception `expect()->toThrow()` catches correctly. A **second**, later `->call()`/`->set()` on an *already-hydrated* `$component` variable goes through Livewire's request-simulation pipeline instead, which appears to intercept `AuthorizationException` and turn it into a response object rather than letting it escape as a thrown exception — so nothing reaches `expect()->toThrow()`, and the test fails on "exception not thrown" even though the refusal itself worked exactly as intended.
- **Fix applied**: rewrote the test to `try { $component->set(...)->call('save'); } catch (AuthorizationException) { /* nothing */ }`, followed by a plain assertion on the resulting database state — the exact shape `tests/Feature/SalesRegions/RefusalLoggingTest.php`'s own `'save() authorization refusal is logged'` test already uses for the identical "revoke, then a second `->call()`" scenario, discovered only after tracing why that file's test wasn't written the "obvious" `expect()->toThrow()` way either. See `tests/Feature/PaymentMethods/AuthorizationTest.php`.
- **How to avoid it next time**: **when writing a Livewire authorization test that revokes a permission and then calls a SECOND method on an already-mounted `Livewire::test()` component (as opposed to the very first `Livewire::test(Component::class)->call(...)` in one chain), use `try { ... } catch (ExceptionClass) {}` followed by a state assertion — never `expect(fn () => ...)->toThrow(...)`.** The two call positions are not interchangeable even though they look identical at the call site; this repo already had the correct pattern in `SalesRegions\RefusalLoggingTest.php` with no comment explaining why it differs from the `openEditModal()`-first-call tests beside it, which is exactly the kind of silent precedent this log exists to make explicit. Before writing a *second*-call authorization-refusal test from scratch, grep an existing `RefusalLoggingTest.php` for the shape rather than copying the first-call pattern by pattern-matching alone.

## Two `php artisan test` invocations against the same worktree's testing database, run concurrently, produced ~47 spurious failures across completely unrelated tests — 2026-09-10

- **Context**: story 0038 (payment methods backend), running this repo's Full Test Suite Gate before declaring the work done, from a host-native worktree (`.claude/worktrees/0038-payment-methods-bank-transfer-backend/`) pointed at its own dedicated `testing5` database, per [worktree-databases.md](testing/worktree-databases.md).
- **What happened**: a full unscoped `vendor/bin/pest` run was started in the background (`DB_DATABASE=testing5`). While it was still running, a second, scoped `php artisan test --filter=PaymentMethod` invocation — against the **same** `testing5` database — was started to spot-check a fix. The scoped run hung past its own timeout; once both were cleared and the full suite re-run **alone**, it went from 47 failures to 0 with **no code change**. The 47 failures were spread across completely unrelated modules (`UserRolesAndPermissionsTest`, `ResolveProductTaxRateTest`, and others touching nothing this story wrote) and shared one unmistakable signature: an assertion comparing two UUID strings that should have been identical, failing with two *different*, both-plausible-looking UUIDv7 values — the signature of two processes racing against the same rows in the same database, not a real regression in either.
- **Root cause**: two independent `php artisan test`/`vendor/bin/pest` processes, invoked from the same worktree, target the identical database the moment `DB_DATABASE` (or `.env.testing`) resolves to the same value for both — nothing in this repo's tooling detects or prevents a second concurrent invocation. [worktree-databases.md](testing/worktree-databases.md) already documents the *cross-worktree* version of this (two worktrees must never share one `testing<N>` database) but says nothing about the identical hazard occurring **within one worktree**, from two shell invocations running at once — which is exactly as unsafe, for the identical reason: `RefreshDatabase`'s per-test transactions and any raw inserts from one process are invisible to, and can be overwritten mid-flight by, the other.
- **Fix applied**: no code change — the 47 "failures" were never real. Killed both stray processes, confirmed no `php`/`pest` process was still running against `testing5` (`ps aux`), and re-ran the full suite alone; it passed cleanly.
- **How to avoid it next time**: **never start a second `php artisan test`/`vendor/bin/pest` invocation against a worktree's testing database while an earlier one from the same worktree may still be running** — check `ps aux | grep -i pest` first, or simply wait for the background run's completion notification before spot-checking anything with the same database. A spray of failures with no code-level explanation, especially ones comparing two superficially-similar-looking generated values (UUIDs, timestamps, autoincrement ids) as unequal, is the tell to check for a concurrent process **before** trusting the failure list as real — the fix is almost always to kill the strays and re-run alone, not to start debugging the failing assertions.

## Livewire skips `ConvertEmptyStringsToNull`/`TrimStrings`, and Laravel skips non-implicit rules for a blank string — the two combine to let a raw `''` reach a `DECIMAL` column — 2026-09-10

- **Context**: story 0037 (shipping carriers and rates — UI), the `maxWeightKg` field on `App\Livewire\Shipping\Index`'s create/edit rate modal — `''` (blank) is the "and above" sentinel for `shipping_rates.max_weight_kg`, so a blank submission is a legitimate, common input, not an edge case. Story 0036's own `App\Concerns\ShippingRateValidationRules::maxWeightRules()` opens with `'nullable'` and its inline comment reads (at the time) *"'nullable' FIRST and it short-circuits: an absent max means 'and above', so `gte` must not run against a null."*
- **What happened**: read out of installed vendor source rather than assumed, three findings compounded into a real gap. **(1)** `vendor/livewire/livewire/src/Mechanisms/HandleRequests/HandleRequests.php:80-89` opts every `/livewire/update` request out of Laravel's global `ConvertEmptyStringsToNull` and `TrimStrings` middleware (`skipWhen(fn () => $this->isLivewireRequest())`), so a blank form field reaches the component as a literal `''`, never `null`, and untrimmed. **(2)** `'nullable'`'s own short-circuit only fires for a genuine PHP `null` — `Validator::isNotNullIfMarkedAsNullable()` (`Validator.php:886`) tests `is_null()`, and `''` is not null. What actually protects a blank string is a completely different mechanism: `isValidatable()` (`Validator.php:819`) checks `presentOrRuleIsImplicit()` (`Validator.php:839`) *first*, and for a blank string it skips every rule that is not itself implicit — `numeric`, `decimal`, `min`, `max`, `gte` all qualify, so the whole rule set is skipped and the field "passes" with `$validated['max_weight_kg']` still equal to `''`. **(3)** That raw `''` reaches a `DECIMAL(8,3)` column, and what happens next depends entirely on the connection: under MySQL's strict mode it raises `Incorrect decimal value` (SQLSTATE 22007) as an uncaught `QueryException` — a 500 — while a looser engine stores it without complaint. The task's own first draft carried a fourth, load-bearing but **false** premise here — that this repo's test suite runs on SQLite (loosely typed, silently accepts `''`) while production runs MySQL strict (a 500), making the bug "green in CI, a 500 in production." Re-verified directly against `phpunit.xml:29` and `.env.example:28`: both pin `DB_CONNECTION=mysql`, with the latter's own comment reading *"never SQLite"* — there is no CI/production engine divergence in this repo at all. The real risk is simpler and worse: a blank `max_weight_kg` 500s in **every** environment alike, including the test suite that should have caught it, unless the value is normalised before validation ever runs.
- **Root cause**: Livewire's request-payload opt-out (finding 1) and Laravel's blank-string implicit-rule skip (finding 2) are two independent, individually-documented behaviours that nobody had previously combined against a `nullable` decimal field on this project — each is correct in isolation, and the interaction is what is dangerous. A `'nullable'` rule reads, at a glance, as "this field tolerates absence," and the pre-existing inline comment stated exactly that reading without qualifying it — true for a real `null`, silently **not** true for the blank string a form control actually submits.
- **Fix applied**: `App\Livewire\Shipping\Index::saveRate()` normalises `$maxWeightKg` to a real `null` (`trim($this->maxWeightKg) === '' ? null : ...`) as the first statement after authorization and *before* `Validator::make(...)->validate()` ever runs — never relying on the blank-string skip to "coincidentally" behave safely. `App\Concerns\ShippingRateValidationRules::maxWeightRules()`'s own inline comment and docblock were corrected in place (quoting the old text per this project's audit-authored-page convention) to state precisely what `'nullable'` does and does not protect against, so a future caller of that trait cannot make the same assumption this story's own first draft made.
- **How to avoid it next time**: **a `'nullable'` validation rule protects only a genuine `null` value — it says nothing about a blank string, and on a Livewire `/livewire/update` request a blank form field is *always* a blank string, never `null`, because Livewire deliberately opts every such request out of `ConvertEmptyStringsToNull`.** Two concrete habits. **(a)** When a `wire:model`-bound field's "empty" state must reach a `nullable` database column, normalise the sentinel (`''`) to a real `null` in the component, before validation — never assume the validator's own rule chain will do it, since Laravel's blank-string skip (`presentOrRuleIsImplicit()`) is a *different* mechanism from `nullable`'s null-check and happens to produce a similar-looking "the field passed" outcome for a completely different reason. **(b)** Before writing a comment that claims a story-wide engine divergence (SQLite-green, MySQL-500, or the reverse) explains why a bug is masked, verify the actual connection every environment uses — `phpunit.xml` and `.env.example` here, checked directly rather than assumed from a plausible-sounding narrative — since a false "it's only a CI artifact" framing understates a bug that, as here, 500s in every environment including the one meant to catch it.

## A `GeographyEntryFactory` override to `name` silently left `normalized_name` stale, making a search test fail against real search code — 2026-09-07

- **Context**: story 0034 (shipping zones UI), writing `tests/Feature/Shipping/SearchGeographyEntriesTest.php` for `App\Actions\Shipping\SearchGeographyEntries` (D-4) — a resolver that matches a normalized search term against `geography_entries.normalized_name` with a prefix `LIKE`. Per the task file's own test-data strategy, rows were built with `Database\Factories\GeographyEntryFactory`'s default state (a plain country row) plus its `community()`/`municipality()` states rather than seeding the real ~8,300-row catalog, overriding the faked `name` with explicit PRD names (`GeographyEntry::factory()->create(['name' => 'España'])`) for traceability.
- **What happened**: every search-by-prefix test failed — searching a normalized `"esp"` found nothing, even though a row named "España" genuinely existed. The failure looked exactly like a bug in the resolver's own `LIKE`/index logic, which is what made it worth tracing rather than dismissing as a fixture problem.
- **Root cause**: `GeographyEntryFactory::definition()`/`community()`/`municipality()` each compute `normalized_name` **inside their own state closure**, from a name they generate themselves (`fake()->unique()->country()`, etc.) — not from whatever `name` the caller later passes to `create([...])`. Overriding `name` via `create(['name' => 'España'])` replaces the display name but leaves `normalized_name` computed from the original faked value, so the row exists with `name = 'España'` and `normalized_name = 'somefakedcountryname'`. The resolver's `LIKE 'esp%'` predicate is applied against `normalized_name`, so it correctly finds nothing — the fixture was internally inconsistent, not the code under test.
- **Fix applied**: every test building a `GeographyEntry` row with an explicit `name` also passes the matching `'normalized_name' => app(NormalizeForSearch::class)($name)'` — wrapped in three small helpers (`makeCountryEntry()`, `makeCommunityEntry()`, `makeMunicipalityEntry()`) in `tests/Feature/Shipping/SearchGeographyEntriesTest.php`, so every call site stays a one-line override rather than repeating the `normalized_name` computation inline. The factory itself was **not** changed — it is owned by story 0032, out of this story's scope, and "recompute `normalized_name` from whatever `name` ends up persisted" is a real design option for a future story to make, not a bug to patch around silently.
- **How to avoid it next time**: **when a factory computes a derived column (a normalized/slugged/hashed copy of another field) inside its own `definition()`/state closure, overriding the source field via `create([...])` does NOT recompute the derived one — pass both explicitly, every time, or the row is internally inconsistent in a way that only a test exercising the derived column will ever notice.** This is a narrower instance of a shape already implicit elsewhere in this codebase (`sales_regions.slug`, `product_variants.combination_hash`/`sku`) wherever a factory or seeder writes a derived value once at creation time — the fix is always the same: compute the derived value from the override at the call site, never trust the factory's own internal generation to have "already handled it" once any of its inputs are overridden.

## A `flux:fieldset` wrapping a Flux field silently swallows its auto-rendered validation error — 2026-09-07

- **Context**: story 0031a (product variant generator — the cartesian combination builder UI), building the axis-picker modal in [`resources/views/livewire/products/variant-builder.blade.php`](../resources/views/livewire/products/variant-builder.blade.php). `attributeTypeIds` (a `flux:checkbox.group`) is deliberately named to equal `GenerateProductVariantCombinations`'s own validation bag key (D-17.1), specifically so a Flux field auto-renders its bound error with no explicit `flux:error` in the template — the one place on this whole screen where that shortcut is meant to work. The first draft wrapped the checkbox group in a separate `flux:fieldset` carrying its own `:legend`, matching how a plain HTML `<fieldset>` groups a checkbox list.
- **What happened**: a refused generation (over the cap, an empty type, nothing selected) threw the expected `ValidationException` keyed on `attributeTypeIds`, and the modal correctly stayed open per D-17.3 — but the error message itself never rendered anywhere on screen. No PHP error, no console error, no failed request; `Livewire::test()->call('generateCombinations')->assertHasErrors('attributeTypeIds')` passed (the error genuinely lands in the bag), which is exactly what made the gap easy to miss — every server-side signal said the refusal worked, and only looking at the actual rendered HTML showed the message was gone.
- **Root cause**: reproduced by reading the real vendor source rather than guessing, per this log's own [hedge rule](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24). `flux:with-field` — the mixin every `flux:checkbox.group`/`flux:input`/etc relies on to auto-render its bound `<flux:error>` slot — wraps its whole label/description/error block in a single conditional:

  ```blade
  {{-- vendor/livewire/flux/stubs/resources/views/flux/with-field.blade.php --}}
  <?php if (isset($label) || isset($description) || isset($descriptionTrailing)): ?>
      <flux:field :attributes="$fieldAttributes">
          {{-- ...label, description, $slot... --}}
          <flux:error :attributes="..." />   {{-- the whole error slot lives INSIDE this branch --}}
      </flux:field>
  <?php else: ?>
      {{ $slot }}   {{-- no flux:field wrapper at all -- and no error slot -- when none of the three is set --}}
  <?php endif; ?>
  ```

  A `flux:checkbox.group` with no `:label`/`:description`/`:description:trailing` prop of its own never enters the `if` branch — it falls straight to the bare `{{ $slot }}` else-branch, with **no** `<flux:error>` anywhere in its output, regardless of whether the property name matches the error bag key. Wrapping the group in a separate `flux:fieldset` with its own `:legend` does not help: `flux:fieldset`'s `:legend` is not one of the three props `with-field` inspects, so from `flux:checkbox.group`'s own perspective nothing was ever set, and the property-name-equals-bag-key shortcut (D-17.1's whole reason for choosing that name) silently does nothing.
- **Fix applied**: `:label` and `:description` moved directly onto `flux:checkbox.group` itself, with the wrapping `flux:fieldset` removed — matching [`resources/views/livewire/roles.blade.php`](../resources/views/livewire/roles.blade.php)'s own existing precedent (`<flux:checkbox.group wire:model="..." :label="__('Permissions')">`), which had shipped the correct shape since task 0011 with nothing pointing at it as the reason. Re-verified by execution: the same refused-generation test now shows the error rendered, with `assertSee` against the real HTML rather than only `assertHasErrors`.
- **How to avoid it next time**: **a Flux field's auto-rendered error slot (`flux:with-field`) exists only when the field itself carries `:label`, `:description`, or `:description:trailing` — a wrapping `flux:fieldset`/`flux:field` with its own heading does not satisfy that condition, however visually equivalent the two look.** When relying on the property-name-equals-bag-key shortcut this project already uses elsewhere ([conventions/naming.md](conventions/naming.md), D-17.1 here), put `:label`/`:description` directly on the bound Flux component, never on a separate wrapping element. And more generally, matching this log's now-repeated Flux/Blaze lesson: **a passing `assertHasErrors()` (or any assertion against component *state*) proves the error was raised — it proves nothing about whether the *template* renders it.** Verify a validation-error-rendering claim against the real compiled HTML (`assertSee`, or a browser test), the same discipline the two `@disabled`/`@js()` entries below already establish for a different Flux/Blaze compile-time trap.

## A `.env.testing` missing most of `.env`'s content produced a wide spray of unrelated-looking Feature test failures — 2026-09-06

- **Context**: story 0032 (shipping geography catalog seed), opening a brand-new `git worktree` for the story per [worktree-databases.md](testing/worktree-databases.md)'s setup steps.
- **What happened**: `.env.testing` was hand-written from scratch rather than copied from `.env` — six lines (`APP_ENV`, `DB_*`), following step 3's *content* without first doing step 1's literal *copy*. The story's own new tests (which touch no session, no encrypted cookie, no Vite asset — pure database seeding with no HTTP layer at all) passed cleanly throughout, which masked the gap. Running the **full** unscoped suite as this project's own quality-gate rule requires surfaced 116 failures spread across `ProductCategories\IndexTest`, `Users\IndexTest`, `SalesRegions\IndexTest`, `Products\AttributeTypesIndexTest`, `Media\UploadTest`, `Settings\ProfileUpdateTest` and others — modules this story never touched. The first failure signature (`Failed asserting that an instance of class Illuminate\View\ViewException is an instance of class Illuminate\Auth\Access\AuthorizationException`) reads exactly like a real authorization regression: a Livewire component's own exception being swallowed and replaced by a *rendering* failure, not an auth one.
- **Root cause**: two missing pieces, found one after the other, neither hinted at by the symptom. **(1)** `.env.testing` had no `APP_KEY` at all — Laravel's `LoadEnvironmentVariables` loads `.env.testing` in place of `.env` (not merged) once `APP_ENV=testing`, so every session/cookie/encryption-touching code path failed with `No application encryption key has been specified`, and because that exception fires *while rendering a Blade view* (session/error-page machinery), PHPUnit reported it as the wrapping `Illuminate\View\ViewException` rather than the real cause — which is what made it look like an authorization bug. **(2)** Once `APP_KEY` was restored (copying `.env`'s real value), the next failure was `Vite manifest not found at: public/build/manifest.json` — a fresh worktree checkout has no `public/build/` (gitignored, never built) and no `public/storage` symlink (also gitignored), and nothing in the setup steps had built either yet. Both gaps are silent precisely because the *first* test files exercised (this story's own) never render a page or an uploaded asset — the gap was invisible until the unscoped, whole-suite run touched a screen that does.
- **Fix applied**: `.env.testing` rewritten as a full copy of `.env` with only `APP_ENV`/`DB_HOST`/`DB_DATABASE` changed (matching every other worktree's `.env.testing` — confirmed by diffing against a sibling worktree's file), then `npm install && npm run build` and `php artisan storage:link` run once. The full suite went from 116 failures to 0 (1921/1924 passed, 3 pre-existing skips) with **no application code changed** — proving the failures were entirely environmental. [worktree-databases.md](testing/worktree-databases.md) gained an explicit warning on step 1 and a new step 6 covering the build/link prerequisite.
- **How to avoid it next time**: **when opening a new worktree, follow [worktree-databases.md](testing/worktree-databases.md)'s setup steps as a literal copy-then-edit, not a "write the six lines I know matter" shortcut** — `.env.testing` needs `APP_KEY` and everything else `.env` carries, not only the `DB_*`/`APP_ENV` lines this doc's own step 3 calls out (that step lists what to *change*, not the complete list of what the file needs). Separately: **run `npm run build` and `php artisan storage:link` once per fresh worktree checkout, before trusting any Feature test result** — both are silently absent from a `git worktree add`, and a test suite that happens to never render a page or serve a file will not reveal either gap. Most generally: **when a scoped or early test run is green but a class of failure appears only once the full suite runs, suspect the environment before suspecting a regression** — a symptom that spans many unrelated modules at once (here: `ViewException` standing in for a dozen different real causes) is the signature of a setup gap, not a code change, and the fix is to run the exact failing test file *alone* with a clean cache before writing up a finding as a real regression.

## Playwright's `selectOption` fires `change` unconditionally, so no `->select()`-driven browser test can be the regression net for a null-`<select>` desync — 2026-09-06

- **Context**: story 0031 (product variants — the variant builder inside the product editor), designing the browser-test detector for D-9's null-`<select>` desync trap — two native `<select>`s per combination row, bound to array paths, the worst case in the codebase yet for [the 2026-08-16 null-`<select>` desync entry](errors-log-archive.md#a-null-livewire-property-bound-to-a-native-select-silently-dropped-the-users-own-pick--2026-08-16). Phase 1's `frontend-qa` re-verified, by reading the actual Pest browser-testing plugin source rather than reasoning about it, whether any of the three obvious tests for this bug class can actually fail against it — independently re-verified by `product-owner` before being adopted.
- **What happened**: none of the three can. `Livewire::test()->set(...)` writes the property directly with no DOM in play, so it was already known to be blind. `assertValue()`/`assertSelected()` resolve to Playwright's `inputValue()` → `select.value`, which returns `''` both when the disabled placeholder is correctly selected **and** when `selectedIndex === -1` (the bug) — indistinguishable. And `visit(...)->select(...)` — `InteractsWithElements::select()`, a one-line pass-through to Playwright's `selectOption` (`vendor/pestphp/pest-plugin-browser/src/Api/Concerns/InteractsWithElements.php:117-122`, `$this->guessLocator($field)->selectOption($option);`) — cannot fail against the bug either: the browser driver's injected `selectOption` implementation sets `select.value`, sets `option.selected = true`, then dispatches `input` **and** `change` **unconditionally**, which destroys any pre-existing `selectedIndex === -1` state before it can be observed and manufactures the very `change` event whose *absence* is what the original 2026-08-16 bug was about. Worse: a shipped test's own inline comment claims the opposite. `tests/Browser/UsersIndexTest.php` (around lines 114-117) argues that picking the first real option after the disabled placeholder "is the only choice that actually exercises the fix" (`$roleId` defaulting to `''` rather than `null`) — false: `->select()` fires `change` regardless of which option is picked, so that test passes identically whether `App\Livewire\Users\Index::$roleId` defaults to `''` (the real fix) or to `null` (the reintroduced bug). It remains a legitimate round-trip test; it was never the regression net its own comment claims it is.
- **Root cause**: every Pest/Playwright helper in this family either writes the property directly (never touching the DOM) or drives the `<select>` through `selectOption`, which is written to make a real user's pick land reliably — including firing the DOM events a real click would fire — rather than to probe the DOM's own pre-interaction state. That design choice is exactly what makes the whole family structurally blind to a bug that only exists **before** any interaction happens: a `null`/`undefined`-bound property desyncing `selectedIndex` from the `<option selected>` placeholder the markup declares. `UsersIndexTest.php`'s comment was written as if picking a *different* option (versus the first) were the risk; the real risk is that any `->select()` call at all repairs the desync as a side effect of firing `change`, no matter which option is picked.
- **Fix applied**: no test file was edited to correct the false claim — `tests/Browser/UsersIndexTest.php:114-117`'s comment is left as it stands, and this entry is the correction, per this project's audit-authored-page convention of recording a wrong claim rather than silently rewriting code outside the scope of the pass that found it. The **real** detector — used for the first time in `tests/Browser/Products/VariantBuilderTest.php` (case B1) — reads `selectedIndex` directly, **before any interaction**:

  ```php
  // tests/Browser/Products/VariantBuilderTest.php
  $page->assertScript(
      'document.querySelector(\'[name="combinationRows.0.typeId"]\').selectedIndex',
      0                       // 0 = the disabled placeholder is genuinely selected
  );                          // -1 = wire:model assigned null/undefined -- the bug
  ```

  `assertScript()` strict-compares against a live-page `evaluate()`, so it fails the instant a bound value is `null`, before any user action, with no event-timing dependency and no flakiness. `selectedIndex` is the only DOM property of the three approaches above that actually separates the two states.
- **How to avoid it next time**: **a "does the picked value round-trip through the browser correctly" test and a "was this specific pre-interaction DOM-desync bug prevented" test are not the same test, and a helper that fires a synthetic `change`/`input` event as a side effect of "picking a value" can never be the second kind.** When a regression's whole mechanism is "a bound value never had the chance to diverge because nothing touched it yet," the detector must read a DOM property **before** any simulated interaction — `assertScript()` against `selectedIndex`, never `assertValue()`/`assertSelected()`/`->select()`. Reach for this shape whenever a native `<select>` (or any element whose pre-interaction state is the thing under test) is bound to a value that could legitimately be absent. And more generally: **before trusting a shipped test's own inline comment about what it proves, re-derive the mechanism independently** — read the actual driver/helper source, as this story's `frontend-qa` did for `InteractsWithElements::select()`, rather than accepting a plausible-sounding claim at face value. This is the same lesson [the 2026-08-24 hedge entry](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) and [the 2026-08-29 "verified" entry](#one-docs-pass-reported-two-gaps-that-were-not-there-both-marked-verified--2026-08-29) both already establish, arriving from a third direction: a **test's own inline rationale**, not a review comment or a docs pass, was the confidently wrong claim this time.

## GitHub Actions' Ubuntu runner ships `libheif` with no AVIF encoder, so Imagick silently wrote a 0-byte `.avif` — 2026-09-06

- **Context**: `feature-entrega2-ARP` branch, no story in progress — just pushing to GitHub after `sail artisan test` had reported the whole suite green locally.
- **What happened**: the same suite failed in the `tests` GitHub Actions workflow, with exactly one failure: `Tests\Unit\Actions\Media\GenerateImageConversionsTest > the generated .avif file is genuinely AVIF, asserted on the ISO-BMFF ftyp brand bytes` — `expect(substr($avifBytes, 4, 4))->toBe('ftyp')` got `''` instead. `substr()` of an empty/near-empty string returns `''`, so the real fact is that `$result['avif_path']`'s stored content was essentially **0 bytes** on the runner, not merely mis-formatted. `App\Actions\Media\GenerateImageConversions::__invoke()` never noticed anything wrong: `$disk->put($avifPath, $image->encode(new AvifEncoder)->toString())` writing an empty string still returns `true`, so the `?: throw new RuntimeException(...)` guard next to it (see [conventions/base-standards.md](conventions/base-standards.md), story 0019 Phase 4 finding F-3) never fires — an empty write and a successful write are indistinguishable to that check.
- **Root cause**: since a recent GitHub-hosted Ubuntu runner image update, `libheif` ships with AVIF **decode** support only — its AVIF **encoder** plugins (`libheif-plugin-aomenc` for the AV1/libaom codec, and siblings) were split into separate packages that Ubuntu does **not** install by default, confirmed against [actions/runner-images#13728](https://github.com/actions/runner-images/issues/13728) (closed by GitHub as "not planned" — this is accepted upstream behavior, not a transient outage). Imagick therefore still *registers* AVIF as a known format (an `extension_loaded('imagick')`/`Imagick::queryFormats('AVIF')` check would report success) but silently produces an empty blob the moment it actually tries to **encode** one, rather than throwing. This project's own Sail image (`docker/8.5/Dockerfile`) installs `php8.5-imagick` via a plain `apt-get install`, which pulls the AVIF encoder plugin in as a `Recommends` dependency — so the encoder has always been present locally, and this gap was invisible until the first real CI run exercising it. It is the same underlying codepath, but a different mechanism, from [the 2026-08-28 Imagick resource-limit entry](#an-imagick-resource-limit-was-requested-in-the-wrong-unit-masked-by-the-hosts-own-policyxml-silently-clamping-it--2026-08-28) — that one was a wrong resource-limit *unit* masked by a stricter local `policy.xml`; this one is a missing *codec* on the CI host that Sail's own `apt-get install` happens to satisfy as a side effect nobody asked for.
- **Fix applied**: added an `Install libheif AVIF encoder plugin` step to [`.github/workflows/tests.yml`](../../.github/workflows/tests.yml) (`sudo apt-get update && sudo apt-get install -y libheif-plugin-aomenc`), placed immediately after the existing `Setup PHP` step (which already carries the `extensions: imagick` correctness input from story 0019 — see [testing/ci/pipeline-integration.md](testing/ci/pipeline-integration.md)) and before `Run Tests`, so both matrix legs (PHP 8.4 and 8.5) get the encoder before any test runs. The step is deliberately independent of `Setup PHP`'s own extension-resolution mechanism (`shivammathur/setup-php`), since the missing piece is a system library plugin, not a PHP extension.
- **How to avoid it next time**: **when a test that decodes or encodes through a system-library-backed format (an image/video codec, a font renderer, a compression algorithm) passes under Sail but fails only in CI, suspect the underlying system library the extension links against before suspecting the test or the application code — and check decode and encode as two separately-installable capabilities, not one.** A format being *registered* (`queryFormats()`, `extension_loaded()`) proves nothing about whether every operation on it is wired to a real codec; only exercising the actual operation (as this project's own byte-signature assertion already does, per [security/image-upload-processing.md](security/image-upload-processing.md)) catches the gap. This is the **second** Imagick/AVIF-specific CI-vs-local environment mismatch on this exact codepath in two weeks — a future change touching `GenerateImageConversions` or its CI step should treat "verify on a real GitHub Actions run, not only Sail" as a standing requirement for this one action, not a one-off diagnostic.

## `DB::transaction($fn, attempts: N)` retried a closure that mutated a model created outside it, producing a silent lost update reported as success — 2026-09-04

- **Context**: story 0029 (product variants — core backend). The same remediation that added `product_variants`' cross-table SKU-collision locking also added `attempts: 3` to three `DB::transaction()` calls (`CreateProduct`, `CreateProductVariant`, `UpdateProduct`), so the fixed lock order those transactions rely on converges under a real deadlock instead of 500-ing on the first collision.
- **What happened**: `appsec-auditor`'s Phase 4 re-audit — re-auditing the fix as new code, per the entry below on this same log — reproduced that `UpdateProduct`'s own retry was unsafe. Its closure captures `$skuChanged = $product->sku !== $sku` and then calls `$product->update([...])`, where `$product` is an `Eloquent` model **created outside the closure** and passed in as a parameter. Executed rather than reasoned about: on attempt 1, `update()` runs and `syncOriginal()` fires; on attempt 2 (after a rollback from a genuine `1213` or, far more likely, a `Lock wait timeout exceeded`), `$product->sku !== $sku` now reads `false` and `isDirty()` after `fill()` also reads `false` — so `update()` issues **no SQL at all**, the SKU re-derivation cascade is skipped entirely (`$skuChanged` gates it), and the action **returns a `Product` whose in-memory attributes show the new values** while nothing was actually written or rolled forward. A silent lost update, reported to the caller as a success. `App\Actions\Products\SyncProductGallery` shares the exact same shape (`forceFill(...)->save()` on the same outside-the-closure instance), so the featured-image/gallery write is lost with it.
- **Root cause**: `attempts: N` makes the closure **retryable**, and a retry restores the database to its pre-attempt state but does **not** reset any PHP object the closure mutated on a prior attempt — `Model::update()`/`save()` calls `syncOriginal()` on success regardless of whether the surrounding transaction later rolls back, so a second attempt reads its own first attempt's dirty-tracking as the baseline and silently no-ops. This is invisible by construction: `save()` on a model with nothing dirty is a **successful** no-op, not an error, so nothing throws and nothing logs. The two other `attempts: 3` sites in the same remediation (`CreateProduct`, `CreateProductVariant`) do **not** have this problem, because both build the row they mutate **inside** the closure via `forceCreate()` — a retried attempt there re-does real work rather than skipping it, the distinguishing property that makes `attempts: N` safe on one sibling and unsafe on another that looks identical from the outside.
- **Fix applied**: `UpdateProduct`'s `DB::transaction()` call now takes **no** retry parameter at all, with a comment above it naming this hazard so nobody re-adds `attempts: N` without re-deriving the analysis first — see [`app/Actions/Products/UpdateProduct.php`](../app/Actions/Products/UpdateProduct.php). The alternative fix considered and rejected was the key-passing shape `App\Actions\SalesRegions\SetSalesRegionActive` already established (pass **keys** into the closure, re-fetch every row the closure mutates from inside it, on every attempt) — not adopted here because `UpdateProduct` returns the caller's own `$product` instance and its cascade reads `$skuChanged` from a pre-mutation attribute, so re-shaping the action's return contract to close this window costs more than removing the retry outright. Full reproduction, the executed before/after table, and a second, coupled finding (`attempts` is silently inert on a **nested** `DB::transaction()` — only the outermost transaction's `attempts` ever fires, so `App\Livewire\Products\Editor::save()`'s own outer transaction, once story 0031 ships it, must **not** get an `attempts:` of its own without first fixing this same closure shape one level up) are in [security/derived-column-invariants.md](../security/derived-column-invariants.md#what-the-remediation-introduced-a-retried-transaction-is-a-retry-safe-unit-or-it-is-a-lost-update).
- **How to avoid it next time**: **adding `attempts: N` to a `DB::transaction()` call is a change to the closure's contract, not a flag — before adding it, check that every model, collection or accumulator the closure writes to was either created *inside* it or is re-fetched by key at the top of it, on every attempt.** The two safe shapes in this codebase: build/insert the row inside the closure (`forceCreate()`/`create()`, as `CreateProduct`/`CreateProductVariant` do), or pass primitive keys into the closure and re-read every row from the database as the closure's own first statement (as `SetSalesRegionActive` does). An Eloquent model instantiated *outside* the closure and mutated *inside* it is the common, easy-to-miss case, and it fails **silently** rather than loudly — there is no exception to catch, because a non-dirty `save()` is defined to succeed.

## An action's own parameter default reintroduced the omission ambiguity its stricter collaborator was built to close — 2026-09-01

- **Context**: story 0024, `App\Actions\Products\CreateProduct`/`UpdateProduct` wrapping `App\Actions\Products\SyncProductGallery` — the single writer of `products.featured_media_id` and every `product_media` row, whose own contract (D-17) is deliberately strict: the ordered gallery array it receives is the **complete, authoritative** new state, never a delta, so an omitted id is a detach and an empty array is "clear the whole gallery".
- **What happened**: the first implementation gave `CreateProduct`'s/`UpdateProduct`'s `$featuredMediaId`/`$orderedGalleryMediaIds` parameters ordinary PHP defaults (`= null`, `= []`) — the reflexive shape for "this field is optional". `appsec-auditor`'s Phase 4 audit round 1 flagged it as **F-1** (Medium): any caller that simply omitted the two arguments — a future queued job, a partial form submit, a test written before the gallery UI existed — would reach `SyncProductGallery` with an empty array and a `null` featured id, silently detaching every existing image and nulling the featured image, with no error and no way to distinguish "the caller meant to clear the gallery" from "the caller never mentioned it". The re-audit found the identical shape a second time (**F-6**) on `UpdateProduct`'s `$description` parameter, which had kept `CreateProduct`'s `= null` default even though the two actions are not equivalent for that field: on **create**, omitting `description` and "no description" are the same real state (a brand-new product genuinely starts empty), so the default is safe there and was deliberately kept; on **update**, omitting it does not mean "clear the description" — it usually means the caller never touched that field — so the identical-looking default silently wipes an existing description on a partial-field update.
- **Root cause**: `SyncProductGallery`'s whole design point is that **omission from its input is a decision, never left ambiguous** — that is what "complete, authoritative array" means. A default on the *wrapping* action's own signature reopens exactly the ambiguity the stricter collaborator exists to remove, one layer closer to the real caller: the collaborator can no longer tell "the actor explicitly submitted an empty gallery" from "the actor never mentioned the gallery at all", because both now produce the identical call. The `$description` case shows the same root cause without any collaborator in the picture at all — a full-replacement field on an **update** path has the identical property (omission from the payload is ambiguous between "clear it" and "leave it"), so the rule is not really about `SyncProductGallery` specifically.
- **Fix applied**: both `CreateProduct` and `UpdateProduct` now require `$featuredMediaId`/`$orderedGalleryMediaIds` with **no default** — a caller must always state intent, matching `SyncProductGallery`'s own contract. `UpdateProduct`'s `$description` parameter lost its `= null` default (F-6); `CreateProduct`'s keeps it, deliberately, with a docblock recording *why* the two actions are allowed to differ on this one parameter. See [`app/Actions/Products/CreateProduct.php`](../app/Actions/Products/CreateProduct.php) and [`UpdateProduct.php`](../app/Actions/Products/UpdateProduct.php).
- **How to avoid it next time**: **a default is safe on a parameter only when omitting it and the default's own value denote the *same real-world state* for every call site that will ever use it — never merely "this field is optional" as a convenience.** Two concrete checks. **(a)** When an action wraps a collaborator whose contract treats its input as complete and authoritative (a full-replace `sync()`, an ordered array, any "this is everything" shape), the wrapping action must not supply a default for the parameter it forwards — the collaborator's own strictness is undone the moment something upstream of it can omit the value silently. **(b)** On an **update** path specifically, a default that was correct on the matching **create** action does not transfer by symmetry — "no value yet" (create) and "value not being touched" (update) are different states, and only the first one is safely represented by a default. This is [the "absence in a payload from a partially-visible form is not a decision" family](#a-guard-took-the-state-it-was-guarding-as-a-parameter-reopening-its-own-hole-one-level-up--2026-08-20) arriving from a new direction: there the ambiguity lived in the *payload a partially-visible UI form submits*; here it lives one layer lower, in the *action's own parameter defaults*, with no form or UI involved at all.

## An absolutely-positioned element with no explicit `top` takes its static position from DOM order, and can silently occlude a sibling — 2026-08-31

- **Context**: story 0022 (shared searchable multi-select component), building the results dropdown in [`resources/views/livewire/components/searchable-multi-select.blade.php`](../resources/views/livewire/components/searchable-multi-select.blade.php). The dropdown is `position: absolute` (D10's forced manual-popover shape — see [the debounce entry below](#a-livewire-directive-modifiers-duration-cannot-be-interpolated-as-part-of-a-component-tags-attribute-name--2026-08-31) for that deviation's own story) and carries no explicit `top-*` utility, relying on document flow for its offset — the same `mt-1`-only shape `wysiwyg-editor.blade.php`'s own link popover already uses.
- **What happened**: while the dropdown block was written directly after the search `<flux:input>` and before the chip row, opening it with any rendered height at all — including just the empty-state message — visually and interactively covered the chip row beneath it. `Livewire::test()` and the whole component-level Feature suite stayed green throughout, because neither ever renders real CSS; the defect surfaced only in a real browser test, as a Playwright click on a chip's remove control hanging until the tool's own actionability timeout — no PHP error, no console error, no failed HTTP request anywhere to point at.
- **Root cause**: `position: absolute` removes an element from normal flow for layout purposes, but its **static position** — the position it would have occupied had it stayed `static` — is still derived from DOM order, and nothing in this markup ever overrode that with an explicit `top`. The dropdown and the chip row are siblings; because the dropdown appeared first in source order, its static-position offset placed it directly over the chip row the moment it had any height, silently occluding the remove buttons underneath. `document.elementFromPoint()` at a chip's own remove-control coordinates returned the dropdown's empty-state `<div>`, not the button — confirmed by direct browser reproduction, the identical diagnostic the [`disabled:cursor-not-allowed` entry](errors-log-archive.md#disabledcursor-not-allowed-on-a-flux-button-was-never-the-cursor-the-user-saw--2026-08-16) further down this log already used to find a different DOM-vs-markup mismatch.
- **Fix applied**: the dropdown block in `resources/views/livewire/components/searchable-multi-select.blade.php` was moved to render **after** the chip area in source order, with no CSS change at all — the fix is purely a reordering, because it is the static-position fallback that moved. An inline comment above the dropdown records the exact failure (including the `elementFromPoint()` result) and forbids moving it back "for readability" without re-verifying.
- **How to avoid it next time**: **an absolutely-positioned element with no explicit `top`/`left` takes its placement from where it sits in the DOM, not from where it visually appears to sit relative to a sibling — fix an occlusion like this by reordering the markup, not by reaching for a `top` value, unless the offset genuinely needs to vary at runtime.** More generally: **when a real-browser click hangs on Playwright's own actionability timeout with no error anywhere in the stack, suspect occlusion before suspecting a broken handler** — `document.elementFromPoint()` at the target control's real on-screen coordinates is the fast, direct way to find out what the browser actually thinks is on top, and it is a diagnostic a `Livewire::test()`-only suite structurally cannot run. This is the same "the DOM's own ground truth disagrees with what the markup intends" lesson the `pointer-events`/`tooltip`-presence entries below already establish, arriving here through document flow instead of a Flux prop.

## A Livewire directive modifier's duration cannot be interpolated as part of a component tag's attribute NAME — 2026-08-31

- **Context**: story 0022, wiring `$debounceMs` (a per-consumer configurable prop, default 300) into the search input's debounce. The obvious form is `wire:model.live.debounce.{{ $debounceMs }}ms="search"`, mirroring the literal `wire:model.live.debounce.300ms` this app already uses elsewhere.
- **What happened**: `code-reviewer`'s Phase 2 INVEST review (finding B1) caught the shape before Phase 3 wrote real code around it — but it is worth recording because the *shape itself* had already been silently worked around twice, undocumented: [`resources/views/livewire/media/gallery.blade.php`](../resources/views/livewire/media/gallery.blade.php) hard-codes a literal `wire:model.live.debounce.300ms`, and [`resources/views/livewire/components/wysiwyg-editor.blade.php`](../resources/views/livewire/components/wysiwyg-editor.blade.php) sidesteps the whole shape by debouncing through a hand-rolled Alpine `setTimeout()` instead. Neither prior file states *why* a dynamic duration was avoided; story 0022's own implementation is the first to write the reason down, in [`resources/views/livewire/components/searchable-multi-select.blade.php`](../resources/views/livewire/components/searchable-multi-select.blade.php)'s file-banner comment.
- **Root cause**: Blade's `ComponentTagCompiler` parses a component tag's attribute **names** with the regex `[\w\-:.@%]+`, which does not include `{`, `}` or `$` — so an interpolated `wire:model.live.debounce.{{ $debounceMs }}ms` attribute *name* is not a shape `compileComponentTags()` can parse at all, because that compiler runs before Blade's own `{{ }}` echo compiler ever sees the string. This is a **different** failure from the three `livewire/blaze` attribute-*value*-folding traps already in this log (the bare-`@disabled`, the conditionally-bound `tooltip`, and the two-`@directive()` entries below): those are about a value not compiling correctly inside a folded Flux tag; this is about a dynamic segment inside the attribute's own **name**, which no Blade component tag — Flux-folded or not — can ever parse, because the tag compiler never gets as far as evaluating anything for a name it cannot even tokenize.
- **Fix applied**: `resources/views/livewire/components/searchable-multi-select.blade.php` uses a plain (non-`.live`) `wire:model="search"` for deferred binding, plus a hand-rolled Alpine `x-on:input="clearTimeout(searchDebounceTimeout); searchDebounceTimeout = setTimeout(() => $wire.set('search', $event.target.value), {{ $debounceMs }})"` — the same debounce mechanism `wysiwyg-editor.blade.php` already established, now stated as the *reason* rather than merely followed. `{{ $debounceMs }}` interpolates cleanly here because it sits inside an ordinary attribute **value** (`x-on:input="..."`), the shape Blade *does* compile correctly throughout this file — the file's own banner comment states the distinction explicitly so a future reader does not "fix" the working value-interpolation back into the broken name-interpolation form for consistency.
- **How to avoid it next time**: **a `wire:*.debounce.Xms`-style Livewire directive *modifier* is part of the attribute's name, and a value that must vary per consumer (a prop, not a compile-time constant) cannot be interpolated into it — reach for a hand-rolled Alpine `setTimeout()` + `$wire.set(...)` instead**, the mechanism `wysiwyg-editor.blade.php` already established, and write the reason into a comment rather than leaving the next reader to rediscover it by trial and error. This generalizes past debounce: **any Livewire directive whose configuration lives in its modifier chain (`.debounce.Xms`, `.throttle.Xms`, …) is fixed at compile time and cannot vary per component instance** — a literal duration works, a property never will.

## A bare `@disabled(...)`/`:disabled="..."` inside a `<flux:button>` tag's attribute list corrupts the whole compiled view — 2026-08-31

- **Context**: story 0021 (shared WYSIWYG rich-text editor), building the eight-button toolbar in [`resources/views/livewire/components/wysiwyg-editor.blade.php`](../resources/views/livewire/components/wysiwyg-editor.blade.php). Every button needs to render `disabled` when the component's own `$disabled` read-only flag is `true` — the same conditional-attribute shape it would be tempting to write as one `<flux:button ... @disabled($disabled) />` tag, the way a plain HTML `<button>` would take it.
- **What happened**: `code-reviewer`'s Phase 5 review (finding N4) reproduced, via direct `Blade::render()` execution, that a bare `@disabled(...)` — or an equivalent `:disabled="..."` binding — written inside a `<flux:button>` tag's attribute list does not compile the way it would on a plain HTML tag. It corrupts the tag's **whole** attribute string into invalid PHP, which is a PHP **syntax error** that breaks the entire compiled view — not merely that one button's disabled state, and not a silent mis-render like the two traps immediately below. Any page embedding the component would 500 outright.
- **Root cause**: `livewire/blaze` (already implicated in the two entries directly below this one) folds `@blaze`-annotated Flux components at compile time, and its component-tag compiler parses a tag's raw attribute list **before** the general Blade directive pass runs. `@disabled(...)`/`:disabled="..."` written as if it were an ordinary attribute is not a form that parser tolerates inside a folded tag — it does not resolve to a conditional `disabled=""` the way it would on a native HTML element; it corrupts the compiled attribute string outright.
- **Fix applied**: every one of the toolbar's eight controls in `resources/views/livewire/components/wysiwyg-editor.blade.php` is written as a full `@if ($disabled) <flux:button ... disabled /> @else <flux:button ... x-on:mousedown.prevent="..." /> @endif` pair — two complete, separately-compiled tags — rather than one tag carrying a conditional `disabled` attribute. An inline comment sits directly above the first occurrence, naming the finding and pointing at the two related traps below so nobody "simplifies" it back into a single conditional attribute.
- **How to avoid it next time**: **in this codebase, never write a bare `@disabled(...)` directive directly inside a `<flux:*>` component tag's attribute list — branch the whole tag with `@if`/`@else` instead**, exactly as the `tooltip`-presence trap and the `cursor-not-allowed!` placement rule immediately below already establish for other props on the same kind of tag. This is now the **third** documented Flux/Blaze markup trap sharing one root cause (`livewire/blaze`'s compile-time folding not tolerating a directive where a plain attribute is expected) — verify any new one by rendering the real compiled view (`Blade::render()`, or a rendering test asserting real HTML), never by reading the Blade source. This one is easier to catch than its two siblings only because it throws outright rather than silently mis-rendering — but only for whoever renders the page first, which in an untested branch may be a real user.

> **Correction — 2026-09-04, story 0027 (products list + editor UI), Phase 5 code review.** This entry's own title and its "What happened"/"How to avoid it next time" text named **two** broken shapes — a bare `@disabled(...)` directive *and* "an equivalent `:disabled="..."` binding" — and only the first reproduces. `code-reviewer` found `resources/views/livewire/products/editor.blade.php`'s gallery-strip reorder buttons using exactly the second shape on two real `<flux:button>` tags (`:disabled="$index === 0"` and `:disabled="$index === count($galleryPreviews) - 1"`) and verified **by execution** that both render correctly — the disabled state toggles as expected across the whole browser and rendering suite, with no compile error anywhere.
>
> Re-verified directly against `Blade::render()` rather than taken on that verification's word: `Blade::render('<flux:button :disabled="$disabled">Test</flux:button>', ['disabled' => true])` compiles and renders `disabled="disabled"` correctly (and correctly omits the attribute at `false`); `Blade::render('<flux:button @disabled($disabled)>Test</flux:button>', ['disabled' => true])` still throws `Illuminate\View\ViewException: syntax error, unexpected token ":", expecting "("` on the identical tag. The two forms are not equivalent to `livewire/blaze`'s tag compiler — a colon-bound attribute expression (`:disabled="..."`) compiles through the ordinary Blade attribute-value path; a `@directive(...)` call written where an attribute is expected does not, regardless of which directive it is.
>
> **The rule is narrowed to what actually reproduces: never write a bare `@disabled(...)` (or any other) *directive call* inside a `<flux:*>` component tag's attribute list — a `:prop="..."` colon-bound attribute expression is safe there at any value.** This is the same shape [the "Two `@directive(...)` calls" entry above](#two-directive-calls-in-one-blade-component-tags-attribute-string-silently-fail-to-compile--2026-08-26) already found for `@js()` — a directive call and a colon-bound expression are not interchangeable inside a folded Flux tag's attribute list, however similar they look. **Verify a Flux/Blaze markup rule by executing the *specific* shape in question, never by generalising a directive-call finding onto its colon-bound syntactic neighbour** — the title and root-cause paragraphs above are left as originally written, per this project's audit-authored-page convention, rather than silently narrowed.

> **Narrowed 2026-09-04 (story 0027) — the claim above is over-broad, and is corrected in place with what it used to say, per this project's audit-authored-page convention.** The title and the "What happened"/"Root cause"/"How to avoid it next time" bullets all named **two** shapes as equally broken — *"a bare `@disabled(...)` — or an equivalent `:disabled="..."` binding"*. `code-reviewer` verified **by execution** while building story 0027's product editor that this is false for the second shape: `App\Livewire\Products\Editor`'s view uses exactly the shape this entry forbade, `:disabled="$index === 0"` on two `<flux:button>` tags (the gallery-strip reorder controls), and it compiles and renders correctly across the whole browser/rendering suite — no corrupted view, no PHP syntax error. This pass re-verified it directly: `Blade::render('<flux:button :disabled="$disabled">Test</flux:button>', ['disabled' => true])` compiles cleanly and renders `disabled="disabled"` on the button, while `Blade::render('<flux:button @disabled($disabled)>Test</flux:button>', ['disabled' => true])` throws `Illuminate\View\ViewException` (a wrapped PHP parse error, `unexpected token ":"`) immediately. **What actually reproduces is the bare `@disabled(...)` directive specifically — written with no `=`, the way a plain HTML attribute never is — not a `:disabled="..."` attribute binding, which is ordinary Blade component-prop syntax and compiles like any other bound attribute.** The root cause is narrower too: this is not `livewire/blaze`-specific folding behaviour at all — a bare `@disabled(...)` fails to parse as a component-tag attribute in **plain** Blade's `ComponentTagCompiler`, because `@directive(...)` with no `attr=` prefix is not attribute syntax the tag parser recognises, independent of whether Blaze is installed. The fix already shipped (full `@if`/`@else` tag pairs in `wysiwyg-editor.blade.php`) is unaffected and remains correct; only the scope of what else needed avoiding was too wide. **Do not read this as license to reach for `:disabled="..."` freely on every `<flux:*>` tag without checking** — the two-`@js()` correction on [the entry below](#two-directive-calls-in-one-blade-component-tags-attribute-string-silently-fail-to-compile--2026-08-26) already establishes that this file's own confident mechanism claims have been wrong before and are worth re-verifying by execution rather than trusted at face value.

## One docs pass reported two gaps that were not there, both marked "verified" — 2026-08-29

- **Context**: story 0019's Phase 6 (`docs-keeper`). The pass went looking for what the story had missed — a good instinct, and the one this log's own [bare-negative-claim entry](errors-log-archive.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13) asks for. It reported two findings, framed them as this doc set's sharpest, and marked each as checked against the tree rather than reasoned about. **Neither gap existed.**
- **What happened**: both false claims shipped, spread across four pages, and stood for a whole story until story 0020's Phase 2 INVEST review read the docs as prerequisites and noticed they contradicted the code it was about to extend. (Phase 2 caught the first; this Phase 6 pass found the second while verifying the correction to the first.)
  1. **"`App\Actions\Auth\LogRefusedPrivilegedAttempt` appears nowhere in `app/Livewire/Media/` or `app/Actions/Media/` — verified by grep at this story's Phase 6, not assumed."** `Gallery.php` imports it and calls it twice; `StoreUploadedImage.php` uses it too. Both files carry comments naming *this very docs finding* as the reason (`Phase 6 docs-keeper finding: …`) — so the finding was real when raised, it was acted on, and only the write-up was never revisited.
  2. **"`media` shipped without its `roles.modules.media` translation leaf, so the Roles permission matrix renders the raw key in both locales"** — presented as this page's long-predicted ❌ finally arriving for real. `lang/en/roles.php` has `'media' => 'Media'` and `lang/es/roles.php` has `'media' => 'Medios'`, both in story 0019's own tree, both untouched since. This one was never true at any point.
- **Root cause**: four things, and the first two are what make this a *pair* rather than one slip.
  - **A pass hunting for gaps grades its own findings on how sharp they are, not on whether they hold.** Both were written up as the pass's most valuable output — one "the sharpest ⚠️ of this pass", the other "the exact ❌ this page predicts, arriving for real" — and a finding that lands that well does not get re-read. Note the second was *especially* attractive because the docs already predicted it; a claim that confirms an existing prediction gets less scrutiny, not more.
  - **The claims were hedged in the direction that made them *more* trusted.** "Verified by grep, not assumed" is the sentence a reader stops at. A confident method claim attached to a false conclusion is worse than a bare assertion, because it forecloses the check. Compare [the 2026-08-24 entry](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24), where a *hedge* was the tell that nobody ran the code; here the opposite framing produced the same outcome.
  - **For finding 1, the fix was rebased into the earlier feature commit**, so `git log` shows `feat(media): …` before `docs: Phase 6 sync …`. Anyone checking chronology concludes the doc is the later, authoritative word about the earlier code — the exact inverse of what happened. `git log` order is not evidence about which of two files was written last, and on a rebasing workflow it is systematically misleading.
  - **A docs pass that files a finding has the strongest reason to re-read its own write-up and the weakest prompt to do so** — the finding *succeeded*, which feels like completion. This is [the 2026-08-20 audit-authored-page entry](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) one level over: there a Phase 4 page outlived the Phase 5 fix; here a Phase 6 page outlived a fix *its own author requested*.
- **Fix applied**: the refusal-logging ⚠️ in [architecture/authorization.md](architecture/authorization.md#mediapolicy--the-fourth-policy-and-the-first-behind-no-route-at-all) is now a ✅ describing the shipped state (six gated component methods plus both actions, pinned by `tests/Feature/Media/RefusalLoggingTest.php`); the same page's `mount()`-exclusion list, which named `Media\Gallery::mount()` as a fourth *unlogged* `mount()`, now records it as the deliberate counter-case that **does** log; the translation-leaf ⚠️ is corrected on both [architecture/authorization.md](architecture/authorization.md#the-media-module-and-what-a-catalog-amendment-costs) and [conventions/naming.md](conventions/naming.md#translation-keys). All four rewritten **in place with what they used to say**, never deleted — the underlying rules were correct and are kept; only the claim that this story violated them was wrong. [README.md](README.md)'s index entries corrected in the same pass.
- **How to avoid it next time**: **a docs pass owns re-verifying every gap it reports, before the story closes — including the ones that got fixed, because a successful finding invalidates the sentence that reported it and nothing else will.** Four concrete habits. **(a) Re-run the literal command a doc claims to have run**, at the end of the pass, on every "verified by grep / by execution" claim the pass itself wrote. It takes seconds and it is the only check that catches either of these. **(b)** Write a gap as a ❌/✅ pair from the start, so a fix has a slot to land in rather than requiring the framing sentence to be rewritten — [the 2026-08-20 entry](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20)'s rule, and the one that would have made finding 1 cost a line instead of a story. **(c) Treat a finding that confirms an existing prediction as needing *more* verification, not less.** Finding 2 was appealing precisely because the docs had forecast it for three stories; that is a reason to check it twice. **(d) Never date a doc's claim by commit order on a workflow that rebases.** If a doc and the code disagree, the code is right — read the file, not the log. This is the same family as the [deferred-findings entry](#a-deferred-storys-findings-were-claims-about-a-tree-that-no-longer-existed-and-one-of-them-would-have-reopened-a-bug-in-this-log--2026-08-23) — a claim about a tree, outliving the tree — with the shortest fuse of any of them: one of these was false on the day it was written, and the other never true at all.

## A stale `public/hot` file from an old `npm run dev` session made all 19 browser tests time out, misread as real UI bugs — 2026-08-28

- **Context**: running the full `php artisan test` suite (per docs/workflow.md Phase 5, verifying tasks `0019a`/`0019b` before closure) surfaced 19 failures, every one of them in `tests/Browser/{UsersIndexTest,RolesIndexTest,SalesRegionsIndexTest}.php` (`Timeout 5000ms exceeded`), and none in any Media/Feature/Unit test — a completely different, unrelated failure set from the two tests those tasks actually fixed.
- **What happened**: the saved Playwright screenshot for one timeout showed the real page loading, but with every element rendered at native browser defaults — gigantic unstyled SVG icons, near-invisible default-size text, no layout at all — the unmistakable signature of a page with **zero CSS applied**, not a broken component or a real product bug.
- **Root cause**: `public/hot` existed (dated the same day, `07:24`), left behind by an earlier `npm run dev` invocation that was never cleanly stopped. Laravel's `@vite()` directive checks for this file first and, when present, emits asset tags pointing at the Vite **dev server** (`http://localhost:5173/...`) instead of the built manifest in `public/build/` — confirmed via `curl`: the rendered `<link>`/`<script>` tags all pointed at port 5173, and nothing was listening there. Every page the browser tests visit therefore loaded with no CSS and no JS framework code, so every interaction-dependent assertion (a dropdown populating, a modal opening, Flux's JS initializing) hit its 5-second timeout waiting for a DOM state that could never arrive.
- **Fix applied**: `rm public/hot` (gitignored, per `.gitignore:5` — never a repo file, never something to commit or restore). All 19 browser tests passed immediately afterward with no other change; the full suite went 950/950 with no code touched for this specific failure set.
- **How to avoid it next time**: **when every browser test in a run fails with a timeout and none of them share an obvious functional cause, check for a stale `public/hot` before debugging any of the tests themselves.** `ls public/hot` (or a saved failure screenshot showing completely unstyled markup, which is the visual signature) settles it in seconds — killing an `npm run dev` process by closing a terminal, rather than `Ctrl+C`ing it, is what leaves this file behind, since Vite's own exit hook is what normally deletes it. This is the same failure shape [the Frontend Bundling rule in `CLAUDE.md`](../CLAUDE.md) already names as a first troubleshooting step ("ask the user to run `npm run build`/`npm run dev`") — the addition here is `public/hot` specifically as the file to check, and that a bulk timeout across otherwise-unrelated browser test files is the tell that it's environmental, not a regression in any of them.

## An Imagick resource limit was requested in the wrong unit, masked by the host's own `policy.xml` silently clamping it — 2026-08-28

- **Context**: task 0019a, investigating why `tests/Unit/Actions/Media/GenerateImageConversionsResourceLimitsTest.php` (story 0019) failed consistently — on every run, sequential and parallel, throughout this session — while a companion behavioral test in the same file (a genuine oversized image is actually refused) kept passing.
- **What happened**: `Imagick::getResourceLimit(Imagick::RESOURCETYPE_AREA)` read back `256000000` after `GenerateImageConversions::applyImagickResourceLimits()` requested `1024000000` (`MAX_DIMENSION^2 x BYTES_PER_PIXEL_CEILING`, the same formula also correctly used for `MEMORY`/`MAP`). No exception, no warning — the request was silently satisfied with a smaller value.
- **Root cause**: two facts, neither previously verified against this project's real container. **(1) `RESOURCETYPE_AREA` is a pixel-count resource** (mirroring `WIDTH x HEIGHT`), **not a byte-count one** like `MEMORY`/`MAP` — applying the same "bytes per pixel" formula to it was a category error, present in the code and in [security/image-upload-processing.md](security/image-upload-processing.md)'s docblock quote alike. **(2) `Imagick::setResourceLimit()` can only tighten a resource below the host's `policy.xml` ceiling, never loosen it, and clamps silently** — confirmed by direct reproduction (`tinker`): requesting `1024000000` for `AREA` when the policy ceiling is `256000000` leaves the limit at `256000000`; requesting `100` (below the ceiling) takes effect immediately. This project's `docker/8.5/Dockerfile` installs Debian's packaged **ImageMagick 6.9.12-98 Q16** (via `php8.5-imagick`) — a different major version and build than the `ImageMagick 7.1.2-8 Q16-HDRI` [security/image-upload-processing.md](security/image-upload-processing.md) was measured against — and Debian's package ships a restrictive default `/etc/ImageMagick-6/policy.xml` that page's own "there is no `policy.xml` on this project's ImageMagick install" line said didn't exist. Both facts compounded: the wrong unit (item 1) only produced a visible failure because the host's policy (item 2) happened to be *more* restrictive than the value being wrongly requested — on a policy-less host, the same bug would have silently passed while providing far weaker protection than the docblock claimed.
- **Fix applied**: `app/Actions/Media/GenerateImageConversions.php` now requests `MAX_DIMENSION^2` (a pixel count) for `RESOURCETYPE_AREA`, matching `WIDTH x HEIGHT`; `MEMORY`/`MAP` are unchanged. Test updated to match. [security/image-upload-processing.md](security/image-upload-processing.md) gained a dated correction block rather than a silent rewrite, per [this log's own rule](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) for audit-authored pages. See task `0019a`.
- **How to avoid it next time**: **when a resource-limit or capacity value is requested via a system library, verify the *unit* the library actually uses for that specific resource type — don't assume every same-shaped call in a group (`WIDTH`/`HEIGHT`/`AREA`/`MEMORY`/`MAP`/`DISK`/`TIME` here) shares the same unit just because the code sets them all in one function.** And separately: **a value-clamping ceiling (a system `policy.xml`, a cloud quota, a `ulimit`) that silently accepts a too-large request instead of erroring is exactly the shape that hides a wrong-unit bug for months** — the test that would have caught this immediately (asserting the *effective, read-back* value rather than trusting the call succeeded) is precisely the test story 0019 wrote, and it worked exactly as intended the moment it ran against a host whose policy was strict enough to expose the mismatch. Don't weaken a test like this to make it pass "on some hosts" — fix the unit instead.

## A test suite's own "runs as non-root" claim was re-verified using the wrong `sail` invocation — 2026-08-28

- **Context**: task 0019b, investigating a `chmod(0555)`-based permission test (`tests/Unit/Actions/Media/GenerateImageConversionsFailedWriteTest.php`) that flaked once under full-suite `--parallel` load. Its inline comment claimed the suite "runs as a non-root local user (uid=1000)" as the reason `chmod()` reliably blocks the write.
- **What happened**: `./vendor/bin/sail exec laravel.test whoami` was used to "verify" this and returned `root`, leading to a first-pass conclusion that the comment was stale (the container had started running tests as root since the comment was written) and that the chmod-based mechanism must be working "for a different reason" — Flysystem's `is_writable()` pre-check, reasoned to hold regardless of the real uid. That conclusion was wrong.
- **Root cause**: `sail exec <service> <cmd>` and `sail artisan <cmd>` are **not the same invocation**. Reading `vendor/laravel/sail/bin/sail`: `sail artisan` (and `sail php`, `sail composer`, …) always runs `docker compose exec -u "$APP_USER"` where `APP_USER` defaults to `sail` (uid matching the host user); a bare `sail exec` does not add `-u` and defaults to root. The actual test suite is run with `sail artisan test`, so it genuinely runs as uid 1000 — the original comment was correct. Checking the wrong invocation produced a confident, plausible-sounding but false "environment drift" explanation, exactly the failure mode [the 2026-08-24 hedge entry](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) already warns about — except here the hedge was self-inflicted rather than inherited from a review comment.
- **Fix applied**: reverted the test file's comment to state the correct mechanism (uid 1000 via `sail artisan`, with a citation to the exact line in `vendor/laravel/sail/bin/sail`), verified directly with `posix_getuid()` through `sail artisan tinker` — the same invocation path the real suite uses. See task `0019b`.
- **How to avoid it next time**: **when verifying a fact about "the environment the tests run in," reproduce it through the *exact* command the tests themselves run, not a different one that happens to target the same container.** `sail exec`, `docker exec`, and `sail artisan`/`sail php` can resolve to different users, working directories, or entrypoints even against the identical service — the container is not the unit of truth, the invocation is.

## The full test suite, run repeatedly and heavily in one session, degrades in ways unrelated to any code change — 2026-08-28

- **Context**: the same test-suite performance/parallelization review and follow-up fix session as the two entries above and task `0019a`/`0019b`. Roughly eight full-suite `--parallel --processes=8` (or sequential) runs were executed back-to-back inside one Sail/WSL2 container over about an hour, on top of several `tinker` sessions.
- **What happened**: the first four or five full-suite runs were clean (one known, unrelated pre-existing failure). Later runs in the same session started failing 58–64 unrelated tests at once, almost all `ErrorException: tempnam(): file created in the system's temporary directory` surfacing as 500s on any test that renders a Blade view — none of them related to the code being changed. Restarting the `laravel.test` container reduced container-reported swap usage (1.4Gi → 790Mi) but did **not** clear the failures on the next run; a follow-up non-parallel, narrower run (`tests/Feature/Auth`, `tests/Feature/Users`, `tests/Feature/Media`) then ran in 126.8s — noticeably slower than its share of an earlier clean full run — with one different, also-unrelated Imagick decode failure.
- **Root cause**: not conclusively identified, and not chased further — writing up a specific mechanism here without having proven it would repeat the mistake the entry above just made. Two credible contributing factors, both consistent with what was observed and neither confirmed: (1) `storage/framework/views` (the compiled Blade view cache) is **not** isolated per parallel-test-token the way the database and `Storage::fake()` already are (verified: no `ParallelTesting::setUpTestCase()`/`setUpProcess()` hook anywhere in `app/`), so eight processes compile into the same directory concurrently — a plausible source of a `tempnam()`/rename race under load, though `getResourceLimit` output makes clear the file wasn't actually starved of space or descriptors. (2) This project's own docs already flag WSL2-specific memory behavior (`docs/testing/ci/commands.md`'s PHPStan memory-limit note); a container restart clearing only ~40% of reported swap, not all of it, is consistent with WSL2 not releasing host-level pages promptly, and sustained heavy load from repeated runs plausibly degrades the whole session rather than any single run.
- **Fix applied**: none. This is recorded as an open observation, not a closed finding — no code change addresses it, and it could not be reliably reproduced on a fresh run within this session's remaining time budget.
- **How to avoid it next time**: **don't run many consecutive full-suite `--parallel` executions back-to-back in one long-lived dev container/session when benchmarking or verifying a fix — space them out, or use a fresh container per measurement.** Before recommending `--parallel` for CI (a real, useful recommendation this session also produced — see the test-performance report in this session), pilot it on the actual CI runner across several independent, isolated runs rather than trusting a long local session, since this failure mode is exactly the kind that a single clean CI job (one process lifetime, no accumulated history) may never hit at all. If it recurs on a *fresh* run, the `storage/framework/views` per-token isolation gap above is the first thing to test — add a `ParallelTesting::setUpTestCase()` hook pointing `config('view.compiled')` at a `TEST_TOKEN`-suffixed directory, mirroring how `Storage::fake()` already does it, and see whether the failure stops reproducing.

> **Correction — 2026-08-28, same day, next investigation session.** The predicted fix above (per-token `view.compiled` isolation) was implemented and **empirically disproven as the cause**: a small, targeted slice (`tests/Feature/Auth`, `--parallel --processes=4`) reproduced the *exact same 9 tests* failing, deterministically, across repeated runs — with the per-token isolation hook already in place and its directories confirmed present and writable. Since the failure was 100% reproducible at small scale (not merely "under sustained load"), it was tractable to isolate properly: with the isolation hook reverted, the identical 9 tests failed in the identical way, proving conclusively that shared-directory contention between workers was never the mechanism.
>
> **The real cause: `storage/framework/views` sits inside the project's bind-mounted volume** (`.:/var/www/html` in `compose.yaml`), and concurrent `tempnam()`+`rename()` writes through Sail's WSL2↔Docker bind mount are unreliable — a well-known class of issue with bind-mounted volumes under concurrent I/O, unrelated to disk space, inode count, or file-descriptor limits (all confirmed plentiful). **Fix**: `storage/framework/views` is now a named Docker volume (`sail-views` in `compose.yaml`), native to the container rather than bind-mounted, with `docker/8.5/start-container` `chown`-ing it to the `sail` user on every container start (a fresh named volume is created owned by `root`, and `sail artisan` never runs as root). Verified: the exact reproduction that failed 9/9 consistently now passes 42/42, repeatably; the full 950-test suite passes cleanly under `--parallel --processes=8`, twice in a row (~128s and ~131s, vs. a ~338s sequential baseline — a genuine, stable ~2.6x). The per-token `view.compiled` isolation (`AppServiceProvider::configureParallelTesting()`) was kept as a nested subdirectory *inside* the now-fast volume (`storage/framework/views/test_{token}`) — harmless, and cheap insurance against worker contention even on a filesystem that no longer requires it.
>
> This is the same lesson [the entry above](#a-test-suites-own-runs-as-non-root-claim-was-re-verified-using-the-wrong-sail-invocation--2026-08-28) states in a different shape: an unconfirmed mechanism, however plausible, is a hypothesis to test, not a fact to build a fix around — and here the fastest way to confirm or disprove it was to find a **small, deterministic** reproduction instead of continuing to reason about a rare flake under sustained load. See [testing/ci/commands.md#run-in-parallel](testing/ci/commands.md#run-in-parallel) for the shipped numbers and [testing/worktree-databases.md](testing/worktree-databases.md) for the adjacent per-worktree isolation this does *not* replace (the volume is per-container, not per-worktree — two worktrees sharing one Sail container still need their own database names, exactly as before).

## Two `@directive(...)` calls in one Blade component tag's attribute string silently fail to compile — 2026-08-26

- **Context**: story 0018 (Sales Regions UI), Phase 5 code review finding N8 — a non-blocking "pick one, `@js()` matches `users.blade.php`" style nit against three `wire:click="setActive({{ \Illuminate\Support\Js::from(...) }}, {{ \Illuminate\Support\Js::from(...) }}, '')"` calls on `<flux:switch>` row controls, converted to `wire:click="setActive(@js($region['id']), @js(! $region['isActive']), '')"` for consistency with the file's other single-argument `@js()` calls (`openEditModal(@js(...))`, `setDefault(@js(...))`).
- **What happened**: every row's active/inactive toggle switch became a silent no-op — no PHP error, no JavaScript console error, no failed HTTP request. `Livewire::test(Index::class)->call('setActive', ...)` (direct component call) worked correctly, so the story's own component-level Feature tests never caught it; only a browser test asserting a *rendered outcome* after a real click did, and even that took several rounds to isolate because every earlier symptom (a stale count, an unchanged `is_active`) looked exactly like ordinary async/timing flakiness — the same failure shape this repo has genuinely hit before (see the `->waitForEvent('networkidle')` entries below). Dumping the real compiled HTML settled it: `wire:click="setActive(@js($region['id']), @js(! $region['isActive']), '')"` rendered **byte-for-byte as written**, `@js(...)` completely unprocessed, so Livewire tried to parse a JavaScript-illegal action-call string and dropped it.
- **Root cause — as recorded at the time, and now known to be wrong; see the correction below**: minimally reproduced outside this story's own markup — `<x-button wire:click="foo(@js($id), @js($flag))" />` against a trivial `<button {{ $attributes }}>` component renders the attribute unprocessed, verified via `Blade::render()`. The conclusion drawn was: *a single `@directive(...)` call inside a Blade component tag's plain attribute string compiles correctly; two of them in the same attribute value do not*, attributed to `ComponentTagCompiler::getAttributesFromAttributeString()` regex-scanning the raw attribute string before the general compiler pass runs. That framing also required a carve-out for this repo's own `x-show="matchesFilter(@js(...), @js(...))"` on a `<flux:table.row>`, which has two and works — and the carve-out was a hedge ("not a value fed into the component tag's own attribute list the same way") rather than a mechanism, which is the tell. **What is not in doubt, and is the whole practical content of this entry:** the conversion really did make every row toggle a silent no-op in a real browser, reverting to `{{ \Illuminate\Support\Js::from(...) }}` really did fix it, and a `{{ }}` echo compiles in every shape tested.
- **Fix applied**: reverted the three converted `wire:click="setActive(...)"` attributes in [`resources/views/livewire/sales-regions.blade.php`](../resources/views/livewire/sales-regions.blade.php) to `{{ \Illuminate\Support\Js::from(...) }}` (the pre-existing, proven-correct form), each with an inline comment recording the finding so a future pass doesn't "fix" them back to `@js()` for consistency a second time. `openEditModal(@js(...))` and `setDefault(@js(...))` — each carrying exactly **one** `@js()` call — were left unchanged; they were never the problem.
- **How to avoid it next time**: **prefer `{{ }}` echo expressions over `@directive(...)` calls for any dynamic value inside a Blade *component tag* attribute** (`<x-foo attr="...">`, `<flux:foo attr="...">`) — the correction below narrows *why* (the count is not the discriminator, and an anonymous `<x-…>` tag is the shape that provably breaks), but the safe form is unchanged and is what the repo ships. Use `{{ }}` echo expressions instead when more than one dynamic value must land in the same attribute string (`{{ \Illuminate\Support\Js::from($a) }}, {{ \Illuminate\Support\Js::from($b) }}` is the safe, already-idiomatic form here), or split the attribute across `:wire:click="..."` colon-bound dynamic syntax if the whole value is a single PHP expression. A "pick one style for consistency" cleanup on a component-tag attribute is not risk-free — verify the compiled HTML output (not just the absence of a PHP/console error) before trusting it, exactly as [the entry above](#a-verification-record-that-lists-two-of-three-quality-gates-is-a-record-of-two-gates--2026-08-26) already argues for the quality gates: a passing component-level test here proved nothing, because `Livewire::test()->call(...)` never goes through the compiled Blade view's `wire:click` attribute at all.

> **Correction — 2026-08-26, same day, during story 0018's Phase 6 docs pass.** This entry was verified before being carried forward, per this log's own [re-verify-what-a-story-wrote-about-itself rule](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20), and **the stated rule does not reproduce — in either direction.** Executed against `HEAD` in this project's own container, twice over: once through `Blade::render()` and once through the **real file-view compilation path** (a throwaway view registered with `View::addNamespace()`, so the `ComponentTagCompiler` + cached-compile route the app actually uses, not just the string compiler).
>
> | Shape | Result |
> | --- | --- |
> | `<flux:switch wire:click="setActive(@js($region['id']), @js(! $region['isActive']), '')" />` — **the exact shipped expression**, two `@js()`, array subscripts, leading `!` | **compiles**, both paths → `wire:click="setActive('ID1', true, '')"` |
> | `<flux:button wire:click="openEditModal(@js($region['id']))" />` — one `@js()` | compiles, both paths |
> | `<flux:table.row x-show="matchesFilter(@js(...), @js(...))" />` — the "carve-out" | compiles; **needed no carve-out** |
> | `<x-placeholder-pattern wire:click="foo(@js($a))" />` — an anonymous `x-` component, **one** `@js()` | **UNCOMPILED**, literal `@js(...)` in the output |
> | `<x-app-logo wire:click="foo(@js($a), @js($b))" />` — anonymous, two | UNCOMPILED |
> | any of the above with `{{ \Illuminate\Support\Js::from(...) }}` | compiles, every time |
>
> So the count is not the discriminator; the *component* is. A `flux:` tag compiles `@js()` at any count, and a plain anonymous `<x-…>` tag compiles it at **no** count. Neither half of "one is fine, two are not" survives, and the hedge about `x-show` was explaining something that never needed explaining. **What made the shipped screen go dead is therefore not established**, and it is deliberately not being guessed at here — the broken state was never committed (the Phase 5 commit contains the comment and the already-reverted markup, so `git show` cannot reconstruct it), and this project's own [hedge rule](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) says an unverified mechanism written up confidently is worse than an open question written up plainly.
>
> **What survives unchanged, and is what to act on:** the incident, the symptom (a silent no-op with no PHP error, no console error and no failed request), the fix, and the two rules in the bullet above — prefer `{{ }}` echoes for dynamic values in a component-tag attribute, and **verify the compiled output rather than the absence of an error** before trusting any attribute-syntax cleanup. Add one: **`@js()` inside an anonymous `<x-…>` component's attribute is broken outright** — that half *is* now verified by execution, at any count, and it is the shape the original minimal repro actually hit.

## A verification record that lists two of three quality gates is a record of two gates — 2026-08-26

- **Context**: story 0017 (Sales Region tax configuration backend). [conventions/base-standards.md](conventions/base-standards.md#quality-gates) names three gates every PHP change passes, in order: `php artisan test`, `vendor/bin/pint`, and **Larastan level 7** (`phpstan.neon`, over `app/`, `bootstrap/app.php`, `config/`, `database/`, `routes/`). The story ran three separate, explicitly-recorded verification passes before review — the Phase 3 reconciliation, the Phase 4 fix, and the Phase 4 re-audit round 2 — each ending in a written line of the form *"full suite re-run unscoped: N/N passed; `pint --test --format agent` (unscoped): passed"*.
- **What happened**: **gate 3 was never run.** Not run-and-passed, not run-and-deferred — absent from all three records, and nobody noticed across two `appsec-auditor` rounds and the story author's own re-verification. `code-reviewer` ran it for the first time at Phase 5 and it **failed**: two errors, both in `App\Livewire\SalesRegions\Index::save()`, where `SalesRegion::find($replacementDefaultId)` widens to `SalesRegion|Collection|null` because `Model::find()` accepts an array-ish key and `$replacementDefaultId` came straight out of `$validated[...]` as `mixed`. Real, shipped code, three phases past implementation, in the method the whole story is about.
- **Root cause**: **the record was of what was run, and it was read as a record of what passed.** Two gates reported green, the sentence describing them was long and specific and quantified (`869/869 passed, 2434 assertions`), and a specific, confident record is exactly what stops a reader asking what is *not* in it. Two things made it survivable for so long. The two gates that were run are the two with **loud, immediate, per-file feedback loops** — the ones a developer reaches for while iterating anyway — while Larastan is the one nothing else prompts you to run. And the missing gate is not silent forever: `.github/workflows/tests.yml` runs `composer types:check`, so CI would have failed this eventually. That is the mitigation, not the excuse — it means the omission cost a late catch rather than a shipped bug, and it also means nobody could have inferred the gap from a green local run, because there is no green local run to inspect.
- **Fix applied**: every value read out of `$validated` in `Index::save()` is now narrowed to `(string)` at the point of use, matching `App\Livewire\Users\Index::save()`'s own existing convention for the same read; re-run clean (commit `df377d0`). The story's Phase 5 section records the first clean Larastan run it ever had.
- **How to avoid it next time**: **a verification record must name all three gates and the result of each, including "not run" — a gate absent from the record is a gate that did not run, and should be read that way by whoever reads it next.** Concretely, the closing line of any phase that claims verification is three clauses, never two:

  ```bash
  php artisan test                    # unscoped — not --filter
  vendor/bin/pint --format agent      # unscoped — not --dirty
  vendor/bin/phpstan analyse          # level 7, per phpstan.neon — the one nothing else reminds you of
  ```

  Three notes on why this is a *different* entry from [the scoped-gates one below](#both-of-this-projects-per-change-quality-gates-are-scoped-by-default-and-both-silently-passed--2026-08-20) rather than a restatement of it. **(a)** That entry is about a gate that **ran** and reported "pass" while looking at almost nothing; this one is about a gate that never ran at all, which no amount of widening a `--filter` or dropping a `--dirty` would have caught. **(b)** It generalises past this repo's three commands: **an omission is invisible in a record of positives.** A phase that lists what it did, however precisely, carries no information about what it skipped — so any checklist worth having is enumerated by the checklist, not reconstructed from the report. **(c)** The rule binds a *reviewer* as much as an author: `code-reviewer`'s Phase 5 opening move should be to diff the phases' verification records against the three-gate list, which is the cheapest check in this whole workflow and the one that found this.
## A missing `.env.testing` file let a self-healing `migrate:fresh` wipe the shared dev database — 2026-08-26

- **Context**: no story — routine iteration on the full Pest suite from two separate Claude Code sessions on 2026-08-24, one running `./vendor/bin/sail artisan test` directly and one dispatching a `backend-expert`/`backend-qa` pair to run "revert-checks + full suite".
- **What happened**: the first session's suite run failed with `deleted_at column not found`, diagnosed (reasonably, on the symptom alone) as "the test database is in a corrupted state from a previous concurrent run". It ran `./vendor/bin/sail artisan migrate:fresh --env=testing --force` to reset it — and that command dropped and recreated all 16 tables in the **real development database**, `arospe`, not a testing one. `information_schema.TABLES` showed the table-creation timestamps land in an identical 4-second window twice more that same day (07:29 and again at 23:43, the second during the background suite-run agent's own work), each time wiping whatever had accumulated since — roles, permissions, the sales-region catalog, and every user, including the self-registered account created in between. Nobody noticed until a later session opened the app against an empty `arospe` two days later.
- **Root cause**: this repo's `phpunit.xml` sets `DB_DATABASE=testing`, but that `<env>` block only takes effect when Pest/PHPUnit itself boots the framework — it does nothing for a bare `artisan` command. Laravel's `--env=testing` flag makes `LoadEnvironmentVariables` look for a `.env.testing` file; when none exists (this project never had one), it silently falls back to loading the plain `.env`, whose `DB_DATABASE` is `arospe`. So `--env=testing` looked like the correct, safety-conscious way to reset "the test database" and was in fact indistinguishable from omitting the flag entirely. The agent's self-diagnosis ("the DB is corrupted, let me refresh it") was a reasonable read of the symptom and is not the mistake — the mistake is that the command it reached for to fix that had no guardrail stopping it from targeting production-adjacent data.
- **Fix applied**: created `.env.testing` (gitignored, one per checkout — see `.gitignore`) pointing `DB_DATABASE=testing` at a dedicated database, verified by re-running the exact failing command (`migrate:fresh --env=testing --force`) and confirming it now creates tables in `testing` while `arospe` is untouched. Restored `arospe` with `php artisan db:seed` (roles, the 38-permission catalog, the sales-region catalog, and the Super Admin grant). Documented the per-checkout requirement, and the further requirement that **each `git worktree` needs its own database name** (`testing1`, `testing2`, …) since all worktrees share one MySQL container, in [testing/worktree-databases.md](testing/worktree-databases.md).
- **How to avoid it next time**: **a database-flag whose safety depends on a file that might not exist is not a guardrail — verify the fallback path, not just the happy path, before trusting a `--env=` / `--database=` flag to isolate anything.** For this specific gap: never run `artisan migrate:fresh` (or any other destructive artisan command) against `--env=testing` without first confirming `.env.testing` exists and points somewhere other than the shared dev database — `cat .env.testing | grep DB_DATABASE` before the destructive command, not after. Two corollaries for anyone opening a new worktree: read [testing/worktree-databases.md](testing/worktree-databases.md) before running tests there, and if a suite run ever fails with a symptom that looks like "the schema is stale/corrupted", treat that as a signal to check *which* database actually received the last `migrate:fresh` before running another one — the diagnosis in this incident was right, but nobody checked which database the fix would land on.

## A scope exclusion named screens while the story edited a class those screens share — 2026-08-24

- **Context**: story 0015b, adding a structured `Log::warning` line to every authorization and rate-limit refusal on the Users and Roles admin screens. Its task file carried an explicit exclusion — *"The `settings/*` screens and `App\Livewire\Settings\*` are not in scope"* — written before its own **Q5** decision, which brought five **domain actions** into scope so that a future non-dashboard caller would inherit the logging. Both statements were reviewed and both passed Phase 2's INVEST gate.
- **What happened**: `appsec-auditor`'s Phase 4 audit **FAILed** the first implementation on a Medium (F-1). `App\Actions\Users\RequestEmailChange` — one of the five actions Q5 brought in — is called by `App\Livewire\Users\Index` (in scope, permission-gated) **and** by `App\Livewire\Settings\Profile::updateProfileInformation()` (`settings/profile`, `auth`-only, self-service, no permission gate at all). Instrumenting the action therefore instrumented the excluded caller, and the new log call sat on a rate-limit refusal branch: since `RateLimiter::attempt()` does not consume once the limit is exhausted, every retry past the third re-entered that branch for free. Any authenticated user could drive unbounded log writes at zero cost, from a screen the story had declared out of scope, using a hardening feature as the amplifier.
- **Root cause**: **the exclusion was phrased in terms of *screens*, and the story's unit of change was a *class*.** Those are only the same thing while nothing is shared, and the whole point of Q5 — the [action-owns-the-rule convention](conventions/directory-structure.md#an-authorization-rule-belongs-to-the-action-not-to-one-of-its-callers) — is that these actions are deliberately callable from more than one place. Nobody reconciled the two sentences because each is correct read alone; the contradiction only exists in the intersection, and nothing in the task file, the INVEST review or the implementation brief asks who else calls a file being edited. The consequence direction is what made it a security finding rather than noise: an in-scope caller is permission-gated and self-limiting, while the inherited one is reachable by every authenticated account in the app.
- **Fix applied**: not a scope exclusion — a **log ceiling**, so both callers keep the same trace, bounded. A second, 1-attempt `RateLimiter::attempt()` on a distinct key prefix gates the **log call only**, never the real limit, at `RequestEmailChange`'s three refusal sites and (for symmetry, so the shape cannot silently diverge later) `CreateUser`'s rate-limit site — see [`app/Actions/Users/RequestEmailChange.php`](../app/Actions/Users/RequestEmailChange.php). Phase 5 finding **R-3** then narrowed the aggregate limiter's ceiling key from target-only to `(target, actor)`, matching its two siblings, so a second administrator's refusal against an already-logged target is still recorded. The pattern and its three rules are documented in [architecture/authorization.md](architecture/authorization.md#a-shared-actions-rate-limit-refusal-needs-a-log-ceiling).
- **How to avoid it next time**: **before writing a scope exclusion, resolve it against the *files* the story will edit — `grep -rn "ClassName" app/` for every class in scope — and state the exclusion in terms of classes, not screens.** A screen-shaped exclusion is unenforceable the moment the story touches shared code, and this repo's conventions actively produce shared code: an action exists precisely so more than one caller can reach it. Two corollaries. **(a) Adding an unbounded side effect to a shared class is a capability grant to its *least*-privileged caller, not its most.** Ask who the weakest caller is, then ask what they can now make happen for free — a log write, a mail send, a queued job, a cache write all qualify. **(b) When the answer is "this caller shouldn't get the feature", prefer *bounding* the side effect over *excluding* the caller** — the exclusion re-splits one rule into two implementations, which is the drift this project's conventions spend most of their effort preventing. This is the shared-code half of the family recorded below: [wrapping existing code in a `DB::transaction()`](#wrapping-existing-code-in-a-dbtransaction-moved-a-cache-flush-nobody-had-written--2026-08-21) is about a change the diff hides, where this one is fully visible in the diff — what is hidden is the caller list of the file it lands in.

## A reviewer's "correction" replaced an accurate technical explanation with a wrong one, unverified — 2026-08-24

- **Context**: story 0015a's Phase 5 code review. `code-reviewer` flagged Larastan level 7 failing on `App\Livewire\Users\Index::isAdministratorRoleSelected()`'s `isset($this->roleId)` check (finding F-1), and in the same finding challenged the *docblock's* explanation of why the check is needed — it said a forged `->set('roleId', null)` leaves a direct `$this->roleId` read throwing `Livewire\Exceptions\PropertyNotFoundException`, and the reviewer called this "likely wrong for a public typed property with a default value", without executing anything to confirm.
- **What happened**: acting on that unverified challenge, the docblock was rewritten to say the property is cleared "via reflection" and that a direct read throws PHP's own native "must not be accessed before initialization" `Error` instead. This is what got committed (`30fdecc`). It was wrong on both counts, and worse than the text it replaced: the original `PropertyNotFoundException` claim was **correct**. A second, independent `code-reviewer` pass verified this by execution — `unset()`-ing the property and reading it directly from inside the class threw exactly `PropertyNotFoundException`, because `HandleComponents::setComponentPropertyAwareOfTypes()` does a plain `unset($component->$property)` (no reflection), and PHP routes a read of an `unset()` *declared* property through the magic methods even from inside the declaring class — `Livewire\Component::__get()` throws `PropertyNotFoundException`, and `isset()` observes that state only because `Component::__isset()` catches it internally, not because nothing is thrown.
- **Root cause**: a review finding phrased as "likely wrong" was treated as a confirmed defect and answered with a plausible-sounding replacement, without either side running the one command that would have settled it (a `tinker` call reading the property after `unset()`). The generic mechanism this repo already tracks — [a patch is only valid against the tree it was written on](#a-deferred-storys-findings-were-claims-about-a-tree-that-no-longer-existed-and-one-of-them-would-have-reopened-a-bug-in-this-log--2026-08-23) — applies here in a narrower form: **a review finding phrased with a hedge ("likely", "probably") is a claim to verify, not a claim to act on directly**, and verifying it is cheap when the claim is about concrete runtime behaviour.
- **Fix applied**: a third review pass verified the real mechanism by execution and the docblock in `app/Livewire/Users/Index.php` was corrected back to the accurate explanation (no reflection; `PropertyNotFoundException` via `Component::__get()`/`__isset()`), with a note recording that a Phase 5 pass previously replaced it with a wrong one. The `@phpstan-ignore isset.property` suppression itself was never wrong — only the prose explaining why it is needed.
- **How to avoid it next time**: **when a review finding hedges its own technical claim ("likely", "should be", "I'd expect"), verify it by execution before writing a fix around it — a hedge is a flag that nobody ran the code, not a lower-confidence fact to trust anyway.** For a claim about which PHP exception a specific line throws, the cheapest verification is always the same: reproduce the exact state (`unset()` the property, call the method, read `get_class($e)`) rather than reasoning from how the mechanism *ought* to work. This is a case of the review process itself introducing a defect that the original text did not have — worth watching for specifically when a review's fix is to *documentation or a comment* rather than to behaviour, since no test can catch a wrong sentence the way `phpstan`/`pest` catch a wrong `if`.

## A deferred story's findings were claims about a tree that no longer existed, and one of them would have reopened a bug in this log — 2026-08-23

- **Context**: story 0015, the consolidated follow-up for the eleven non-blocking findings story **0004**'s Phase 4 audit and re-audit raised against the Users CRUD backend (F4–F13, F17/F18). The task file had been written at 0004's closure and sat in `ai-spec/tasks/` while stories 0005, 0006, 0008, 0008a, 0009 and 0013 shipped — five of them editing the exact four files it was about.
- **What happened**: the story reached its Phase 2 INVEST gate and **failed** it (blocking findings B1–B6). Re-verifying each finding against `HEAD` by opening the cited file found that **six of eleven were wrong**, in three different ways. Half-done: F4 asked to lock two properties and one (`$deletingUserName`) was already `#[Locked]`; F17 asked to rename a flag story 0008a had already renamed. Structurally invalid: F12 asked to fix a null-collision inside `Index::authorizeRoleChange()`, a method 0008a **deleted** — along with the `administratorRoleId()` lookup it compared against — so the described comparison existed in no form at all. Ineffective-as-described: F10 asked for a `DB::transaction()` that 0008a had already added, while the atomicity goal it was added for was still unmet, because the email-change delegation ran *after* the commit. And the sharp one — **F8 prescribed retyping `$status` to `public ?UserStatus`, which would have reintroduced [the `<select>` desync bug recorded in this very log](errors-log-archive.md#a-null-livewire-property-bound-to-a-native-select-silently-dropped-the-users-own-pick--2026-08-16)**, where a user's first-option pick is silently discarded while the correct value stays on screen. Its supporting claim was also false: it cited an existing test dataset row that has never existed. F8's *finding* was real; only its prescribed patch was harmful.
- **Root cause**: **a deferred finding is a claim about a tree, and the task file freezes while the tree does not.** Nothing in the workflow re-reads a stored task before Phase 3, so its text arrives carrying the authority of an audit that genuinely was correct — about code from six stories ago. Two properties made it worse than ordinary staleness. First, findings were written as **prescribed patches** ("retype this property to `?string`") rather than as **properties that must hold** ("a forged status value must fail validation, not raise `\ValueError`"); a patch encodes assumptions about the surrounding code, so when that code moves the patch does not merely stop applying — it can become actively harmful, which is exactly what F8 did. Second, the ones that were already fixed fail *silently* on implementation: re-adding `#[Locked]` to an already-locked property is a no-op nobody reviews, so the wasted work is invisible rather than caught.
- **Fix applied**: the whole file was rewritten before any code was written (commit `66a5b88`), with every surviving finding re-verified by reading the real file at `HEAD` and each disposition recorded in a new **"What re-verification changed"** table — F4 halved, F7 re-scoped, F8's instruction explicitly *withdrawn* in writing with the errors-log entry it would have violated cited inline, F10 re-scoped to the ordering with a no-mail-on-rollback constraint, F12 recorded as **closed by 0008a and dropped**, F17 reduced to its test half, F13 split out to its own story `0015a`, F18 deferred by human decision. The line numbers it kept are labelled a reading aid, with Phase 3 required to re-locate each site.
- **How to avoid it next time**: **before a deferred story enters Phase 3, re-verify every finding it carries against `HEAD` by opening the file it cites — and record the disposition of each, including "already closed", in the task file itself.** Four concrete habits. **(a)** Write a finding as the **property that must hold**, never as the patch that would have held it; a patch is only valid against the tree it was written on, and there is no way to tell from reading it. **(b)** Treat a line number in a stored task as a reading aid, never a locator — if the cited line does not contain what the finding describes, the finding is stale until proven otherwise, not merely misnumbered. **(c)** When a deferred instruction contradicts something already in `docs/` — this log, a security page, a convention — **the docs win and the instruction is withdrawn in writing**, so the next reader sees a decision rather than an omission. **(d)** Record the closed findings rather than deleting them; "F12 was closed by 0008a" is the sentence that stops it being re-raised by the next audit. This is a fourth direction of the drift family this log already tracks — [a doc outliving the feature it denied](errors-log-archive.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13), [a doc outliving the bug it described](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20), [code drifting from a rule the doc still states correctly](#wrapping-existing-code-in-a-dbtransaction-moved-a-cache-flush-nobody-had-written--2026-08-21) — and the one with the longest fuse, because a backlog item can sit stale for months while looking exactly as authoritative as the day it was filed.

## Wrapping existing code in a `DB::transaction()` moved a cache flush nobody had written — 2026-08-21

- **Context**: task 0012 (module/sidebar access gating). Its Phase 4 audit re-read `App\Livewire\Roles\Index` because the story's cache-staleness tests exercise it, and found a Medium in code the story neither wrote nor changed. The rule it violates has been documented on this project's own security pages since task 0002 — [security/authorization-patterns.md](security/authorization-patterns.md#flush-the-permission-cache-after-the-transaction-commits-never-inside-it), *"Flush the permission cache after the transaction commits, never inside it"* — complete with the ✅ two-flush code shape and the reason (`config/permission.php` caches the whole roles+permissions graph for 24 hours on the **shared** `database` store).
- **What happened**: `saveRole()` and `deleteRole()` shipped with the ❌ shape that page names — the permission cache flushed only *inside* their `DB::transaction()`, never after the `COMMIT`. On the shared store this fails **open** on a revocation: a concurrent request landing in the window between the in-transaction flush and the commit misses the cache, reads the pre-commit rows, and re-caches the escalated grant for 24 hours. It survived story 0010's three-round Phase 4 audit, the whole of story 0011, and a Phase 5 code review of each.
- **Root cause**: **the flush at issue was never written by this repo.** Neither method contains a flush the author chose to place; `syncPermissions()` fires one internally, and `App\Models\Role`'s `deleted` event fires the other. Both were correctly post-commit until story 0010's own Phase 4 finding F4 — an unrelated atomicity concern ("a failure between the rename and the permission sync must not leave a role persisted with the wrong set") — wrapped the existing statements in `DB::transaction()`. That wrapper relocated a side effect **with no flush line appearing, moving or disappearing anywhere in the diff**, so a reviewer grepping the change for `forgetCachedPermissions` found nothing and correctly concluded there was nothing to check. Compounding it, the regression is untestable by construction: `phpunit.xml` pins `CACHE_STORE=array`, making the permission cache per-process in tests, so the suite was 618/618 green with the window wide open — and stays 618/618 green today if either fix line is deleted.
- **Fix applied**: `app(PermissionRegistrar::class)->forgetCachedPermissions()` added immediately after each `DB::transaction()` call in [`app/Livewire/Roles/Index.php`](../app/Livewire/Roles/Index.php) (commit `0a53acb`), each under a multi-line comment naming the finding and linking the rule — the mitigation for a fix no test can pin, matching `RolePermissionSeeder`'s identical precedent. The security page now also carries the three real call sites and the generalized ⚠️.
- **How to avoid it next time**: **adding a `DB::transaction()` around existing code is a change to every side effect that code already performed — audit each of them, including the ones the diff does not show.** Vendor calls are exactly where this bites, because their side effects are invisible at the call site: a cache flush, an event dispatch, a queued job, a broadcast, a `Storage` write, or a mail send that was outside a transaction is now inside one, and each has its own correct side of the commit. Two habits. **(a)** When a finding's remedy is a *wrapper* rather than a new statement, list what the wrapped code calls and what each of those does beyond returning — this is the cheapest form of the "[audit the remediation as new code](errors-log-archive.md#two-of-the-three-security-audit-rounds-found-the-flaw-in-the-previous-rounds-fix--2026-08-19)" rule already recorded below, applied to the one shape where the diff itself hides the change. **(b)** Before writing the fix, grep `docs/security/` for the call you are about to move — the rule broken here was already written down, with the correct code, on a page the breaking story cited for other reasons. This is the third direction of the doc/code drift family this log records: [a doc outliving the feature it denied](errors-log-archive.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13), [a doc outliving the bug it described](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20), and now **code drifting away from a rule the doc still states correctly** — the only one of the three that a re-read of the doc cannot catch, because the doc is not wrong.

## A count-based assertion over rendered HTML counted a wrapper element it never meant to include — 2026-08-21

- **Context**: task 0011 (Roles & permissions UI). Its Phase 4 security audit established a rule worth keeping — that a **presence** assertion ("this permission's checkbox is still there") cannot catch a *second*, unrelated checkbox also going missing, so the regression test for "the catalog renders in full except one withheld value" must assert a **count**: rendered checkboxes == catalog size, minus exactly the withheld set. Two such tests were added to `tests/Feature/Roles/IndexUiTest.php`.
- **What happened**: both were written as `substr_count($html, 'value="')` — every `value="` attribute in the whole rendered page. Phase 5 flagged it (finding **F-4**) as correct only by coincidence: nothing *else* in the create/edit modal happens to emit that attribute today, so a future `flux:select` or hidden input would move the number with no real omission occurring. The obvious fix — scope it to the checkbox element — then walked into a second trap of its own: the Flux-rendered element is `<ui-checkbox …>`, but `flux:checkbox.group` also emits a `<ui-checkbox-group>` wrapper, whose opening tag **starts with the same string**. Counting `'<ui-checkbox'` therefore over-counts by exactly one, and does so silently: the number is off by a constant, so the test still passes or fails as a block and never looks wrong.
- **Root cause**: the assertion was designed from what the *Blade source* writes (`<flux:checkbox value="…">`) rather than from what the browser receives. Flux components are not one-to-one with their rendered output — a group renders a custom element of its own — and a substring count has no notion of element boundaries, so a child's tag name being a prefix of its parent's is invisible to it. The generic hazard is that a count assertion fails **quietly by a constant**: unlike a presence assertion, which is either satisfied or not, an over-count that is stable across runs reads as the true number and gets baked into whatever expectation makes the test green.
- **Fix applied**: both assertions rescoped to `substr_count($html, '<ui-checkbox ')` — with the **trailing space**, which is what excludes `<ui-checkbox-group` — after tracing the actual rendered markup rather than the stub (commit `48dbf54`). The expected values (`$catalogSize - 1` for a non-Super-Admin, `$catalogSize` for a Super Admin) are unchanged and were re-verified against the real render. The security rule the tests exist for is in [security/authorization-patterns.md](security/authorization-patterns.md#a-control-omitted-from-the-dom-is-safe-only-for-the-one-value-whose-guard-preserves-an-omission).
- **How to avoid it next time**: **a count assertion over rendered HTML must be scoped by a string that cannot match a sibling or wrapper element, and both the selector *and* the expected number must be read off the real rendered output before the test is trusted.** Three concrete habits. **(a)** Dump the HTML once and grep it, rather than deriving the selector from the Blade source — this repo's two 2026-08-16 Flux/Blaze entries below already establish that a Flux component's real output cannot be predicted from its stub, and the same holds for its element names. **(b)** When the selector is an element name, include the delimiter that ends it (`'<ui-checkbox '`, not `'<ui-checkbox'`); custom-element naming makes `<x>` a prefix of `<x-group>` routinely, and Flux's `*.group` components are the standing example. **(c)** Prove the count can be wrong: temporarily remove one item and confirm the number moves by exactly one. A count assertion that has never been observed changing is the same vacuous-coverage failure as [the `arch()` rule below](errors-log-archive.md#a-pest-arch-rule-over-an-array-of-namespaces-shipped-green-while-proving-nothing--2026-08-18), arriving through a different door.

## Both of this project's per-change quality gates are scoped by default, and both silently passed — 2026-08-20

- **Context**: task 0010 (Roles & permissions management — backend). The story's own code was implemented, tested and merged onto `feature-entrega2-ARP`; its Phase 4 security audit and Phase 5 code review then ran against that merged state.
- **What happened**: two independent gates that [conventions/base-standards.md](conventions/base-standards.md#quality-gates) names as mandatory reported success while the thing they exist to catch was present.
  - **The test gate.** Only the Roles-scoped tests were ever run (`php artisan test --filter=…`, which the same conventions page recommends as "the narrowest relevant test(s) first"). The **full** suite was not run once between the story's first merge and the Phase 4 re-audit — and when it finally was, `tests/Feature/Authorization/SuperAdminRoleConfigSourceOfTruthTest.php` was **red**, on a completely unrelated pre-existing test this story had broken.
  - **The formatting gate.** `vendor/bin/pint --dirty --format agent` — the exact command the conventions page mandates — reported clean while three test files carried real violations (`fully_qualified_strict_types`, `ordered_imports`). A plain `vendor/bin/pint --format agent` found them immediately. They reached Phase 5 as finding **F-1**, blocking.
- **Root cause**: the same one twice — **each gate's default scope is narrower than the change it is gating, and a narrowed gate reports "pass", not "not checked".** `--dirty` inspects only files with *uncommitted* changes, so it becomes a no-op the moment the work is committed: the more disciplined the committing, the less the gate sees. `--filter` inspects only the tests whose names match, so it can never observe a change's effect on the rest of the suite. Both are correct as *iteration* tools and both are wrong as *completion* gates, and nothing in their output distinguishes the two uses. The test failure additionally had two compounding causes, each individually invisible: story 0010's own holder-count guard applies to **every** role rather than only to the custom roles the story was about, and the developer's ambient `SUPER_ADMIN_EMAIL` provisioned a second, unaccounted-for Super Admin holder — the identical ambient-config sensitivity [the 2026-08-12 entry below](errors-log-archive.md#a-test-asserted-against-a-fixture-address-that-the-local-env-also-pointed-super_admin_email-at--2026-08-12) already names, in a test written before that entry existed.
- **Fix applied**: the Pint violations were fixed by an unscoped `vendor/bin/pint --format agent` run (commit `fcff6dd`, "style(roles): fix Pint violations and correct three stale docblocks"). [`tests/Feature/Authorization/SuperAdminRoleConfigSourceOfTruthTest.php`](../tests/Feature/Authorization/SuperAdminRoleConfigSourceOfTruthTest.php) gained `config(['auth.super_admin.email' => null]);` in its `beforeEach` (matching what `tests/Feature/Seeders/DatabaseSeederTest.php` already did) and an explicit `removeRole()` before the deletion its scenario is actually about, each with a comment naming the cause. [conventions/base-standards.md](conventions/base-standards.md#quality-gates) now distinguishes the iteration form of each gate from the completion form.
- **How to avoid it next time**: **a gate that takes a scope argument must be run twice — narrow while iterating, and unscoped before the work is declared done — and "the narrow run passed" is never evidence about anything outside its scope.** Concretely, before a story leaves Phase 3: run `vendor/bin/pint --format agent` (no `--dirty`) and `php artisan test` (no `--filter`), in that order, and treat those two runs — not the filtered ones — as the record. Two corollaries. **(a) `--dirty` is inversely coupled to commit hygiene**, so it fails hardest exactly when the workflow is being followed best; never let it be the last formatting check on a committed branch. **(b) A guard that a story adds "for its own feature" almost never applies only to that feature.** `guardAgainstHolders()` was specified as part of the roles-CRUD story and reads as scoped to it, but it is a model-event listener on `App\Models\Role` and therefore binds every role in every test in the repo. When a story registers a model event, an observer, a global scope, or middleware, the blast radius is the whole suite by construction — that alone is sufficient reason to run it.

## A planned test asserted a refusal by `verified`, a middleware that refuses nobody in this app — 2026-08-20

- **Context**: task 0040, moving `users.index` out of `routes/web.php` into its own `routes/users.php`. Phase 1's QA contribution specified one new test to guard the relocation: an unverified user hitting `route('users.index')` should be redirected to `route('verification.notice')`, so that hand-copying the `Route::middleware(['auth', 'verified'])->group(...)` block into a new file could not silently drop `verified`.
- **What happened**: Phase 3's red-test step wrote that test and it did not behave as a red test at all. Proven mechanically rather than argued: with `verified` temporarily removed from the new file's group entirely, the test's outcome was **bitwise identical** to the unmodified group — same 403 from `can:users.view`, same message. The assertion it was built on could never distinguish a correct relocation from one that had lost `verified`, so it was reverted rather than kept, and this story shipped with no new test at all.
- **Root cause**: `App\Models\User` does not implement `Illuminate\Contracts\Auth\MustVerifyEmail` — the import sits commented out at the top of [`app/Models/User.php`](../app/Models/User.php) — and Laravel's stock `EnsureEmailIsVerified` only acts when `$request->user() instanceof MustVerifyEmail`. That condition is `false` for every actor in this app, so `verified` is a **structural no-op on every route that carries it**; account usability is enforced at sign-in through `users.status` instead. None of that was newly discovered here: [architecture/authentication.md](architecture/authentication.md#account-status-and-activation) has stated it in one plain sentence since task 0007. The test was designed from the middleware's *name* and from the route group's *appearance*, and nobody opened the doc that already answered the question.
- **Fix applied**: no code change — the test was reverted and the story's "Tests to perform" section rewritten with a Phase 3 correction note recording why the case was dropped rather than left as dead coverage (`ai-spec/tasks/.../0040-move-users-route-to-own-file.md`). The four pre-existing HTTP-level tests in `tests/Feature/Users/IndexTest.php` (guest → login redirect, no permission → 403, permission → 200, Super Admin bypass → 200) already cover every outcome that actually holds, and re-running them unmodified *is* the regression check for a pure relocation.
- **How to avoid it next time**: **before writing a test that asserts a middleware refuses someone, confirm that middleware can refuse anyone in this app — a middleware being present on a route is not evidence that it is live.** The cheap proof is the one Phase 3 ran: delete the middleware, re-run the test, and see whether the outcome changes; if it does not, the test carries no signal no matter which way it is written. `verified` is the known dead one here and will stay dead until `User` implements `MustVerifyEmail`, so any future story that copies an `auth` + `verified` group into a new area file — task 0010's `routes/roles.php` is the next one — must not plan a `verified` test either. This is the same class as the vacuous `arch()` rule two entries below (an assertion that cannot fail is not coverage); the added angle is that the control under test may not exist at all, and that the answer was already written down in the doc that owns it — check [architecture/authentication.md](architecture/authentication.md) before designing an auth-middleware test case.

## A guard took the state it was guarding as a parameter, reopening its own hole one level up — 2026-08-20

- **Context**: task 0009, `App\Actions\Roles\EnforceAdministratorPermissionGrant` — the action that refuses a role-save payload newly granting `roles.manage-administrators` to anyone but the Super Admin. Three Phase 4 audit rounds; each round's findings were against the **previous round's fix**, not against the original implementation.
- **What happened**: round 1 found F1 — the guard only asked whether the submitted payload *contained* the protected permission. Since `Role::syncPermissions()` replaces a role's entire permission set, and story 0011's toggle is never rendered to a non-Super-Admin at all, a broad administrator editing an unrelated field on a role that legitimately held the permission submitted a payload **omitting** it, silently revoking a Super Admin's grant. The fix introduced a "before" snapshot so the action could diff. Round 2 then failed that fix: the snapshot arrived as a caller-supplied `array $currentPermissionNames` parameter (N2 — a caller asserting an untrue "before" makes a new grant look pre-existing), the two snapshots were normalised asymmetrically so the diff silently mismatched (N3), and the membership check did not flatten nested arrays/Collections the way `syncPermissions()`'s own vendor code does, proven live to bypass the guard entirely (N1). Round 3 passed, after one more: the replacement `?Role $role` parameter had shipped with a `= null` default, so a forgotten third argument at a future call site would silently mean "nothing is currently granted" (NR1).
- **Root cause**: two, compounding. (1) **The guard trusted its caller for the very state it existed to protect.** "What does this role currently hold" is the input the whole decision turns on; accepting it as a parameter makes the guard only as strong as every present and future call site. (2) **Each fix was reviewed against the finding it answered rather than as new code with its own attack surface** — the diff logic was read as "does this close F1?", and it did, so nobody asked where its *new* inputs came from. F1 itself had a third root cause worth naming separately: nothing in the payload distinguishes "the actor chose to remove this" from "the actor was never shown this", and a full-replace `sync*()` collapses the two.
- **Fix applied**: the `array $currentPermissionNames` parameter was replaced by `?Role $role` (nullable, **no default**), and [`app/Actions/Roles/EnforceAdministratorPermissionGrant.php`](../app/Actions/Roles/EnforceAdministratorPermissionGrant.php) now loads the "before" state itself with `$role->load('permissions')` — removing N2 and N3 structurally rather than validating around them. `normalizeNames()` applies the identical `Collection::make(...)->flatten()` pass `HasPermissions::collectPermissions()` uses, so no shape can be invisible to the check while still being honoured by the sync. F1's own resolution — *preserve* an omitted-but-already-granted permission rather than deny the request — was **not** derivable from the spec, and was decided by the human mid-audit (asked explicitly, chose preserve); the choice is recorded in the code's docblock, in [architecture/authorization.md](architecture/authorization.md#who-may-grant-a-permission--the-meta-rule-layer), and in the task file's implementation record. Both durable rules are in [security/authorization-patterns.md](security/authorization-patterns.md#a-full-set-sync-behind-a-partially-visible-form-must-preserve-what-the-actor-cannot-see).
- **How to avoid it next time**: three concrete rules. **(1) A guard must derive the state it guards, never accept it** — take the model and read it (`load()`, not `loadMissing()`), and treat any "current state" parameter in an authorization signature as the finding itself. **(2) The "audit the remediation as new code" rule from the entry below is not a one-off — it is now the observed norm.** Two consecutive stories have needed three Phase 4 rounds, each catching the previous round's own fix, so budget for the extra rounds rather than treating round 2 as a formality. What this story adds to it: the highest-risk remediation is one that introduces a **new input** (a parameter, a snapshot, a cache), because that input's provenance was never in scope for the original review. **(3) When a full-replace `sync*()` is driven by a form that shows the actor only part of the set, absence is not a decision** — diff both directions and authorize adding and removing separately, and when "preserve" versus "deny" is a product judgement rather than a security one, stop and ask the human instead of picking; an unrecorded choice reads as an oversight to the next auditor.

## A security page documented the vulnerable code as current, because it was written before its own fix — 2026-08-20

- **Context**: task 0016 (the `sales_regions` catalog). Phase 4's `appsec-auditor` wrote a new section into [security/seeder-safety.md](security/seeder-safety.md#a-catalog-seeder-must-fail-loudly-rather-than-commit-a-structurally-invalid-catalog) — "A catalog seeder must fail loudly rather than commit a structurally invalid catalog" — quoting the two fail-open shapes it had just found in `SalesRegionSeeder`: `'parent_id' => $spain?->id`, and a repair `update()` whose zero-row outcome went unchecked.
- **What happened**: those findings were fixed later in the same story (both lookups now `throw_if(...)`), but the page was not revisited. It shipped stating *"Two shapes to look for, both present in `SalesRegionSeeder` today"* above a verbatim quote of code that no longer existed, described as accepted low risk. Phase 5's `code-reviewer` caught it as finding F-1. A reader following the page's own citation to `database/seeders/SalesRegionSeeder.php` would have found neither the `?->` nor the unchecked `update()` there.
- **Root cause**: the security pages are authored **during** Phase 4, from the auditor's vantage point, where the vulnerable shape genuinely *is* the current code — so the present tense is accurate at the moment it is written and silently false by the time the story closes. Nothing in the workflow re-reads a Phase 4 page after Phase 5's fixes land: the change→doc mapping routes Phase 6 to the docs describing *the story's* change, and this page had already been written, so it read as done. The failure is invisible in review because the page is internally coherent — only opening the cited file disproves it.
- **Fix applied**: the section was rewritten as an explicit ❌ before / ✅ after pair — the pre-fix shape labelled as no longer in the repo, the shipped `throw_if(...)` guards quoted from the real file, plus the third guard (`assertValidCountryFixture()`) the same audit added. The underlying rule was correct and is unchanged.
- **How to avoid it next time**: **a security page written during Phase 4 documents a state that Phase 5 is expected to change, so Phase 6 must re-verify every code quote in it against the file it cites — treat an audit-authored page as unsynced by default, not as already done.** Two concrete habits: write a vulnerability write-up as a ❌/✅ pair from the start, so the fix has a slot waiting for it instead of needing the framing sentence rewritten; and never describe a *found* finding in the present tense ("both present today") — say "as found" and name the state it is in now. This is the [bare-negative-claim entry](errors-log-archive.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13)'s failure class arriving from the opposite direction: there a doc outlived the feature it denied, here a doc outlived the bug it described.

_Last updated: 2026-09-11 — Doc growth management pass (docs/contracts.md#doc-growth-management-rule): no entry added, changed or removed in this pass. Evaluated moving the log's oldest entries (2026-08-20 through 2026-08-26) into [errors-log-archive.md](errors-log-archive.md), matching the project's own prior archive split — and decided **not to**, because `ai-spec/tasks/` (pending and done) and `app/` code-comment cross-references into this file's per-entry anchors were found to number in the dozens, and this docs-maintainer role's write scope does not extend to `ai-spec/tasks/` or application code, so moving an entry would leave broken links this pass could not repair. Folded this single line into the existing one-line `_Previously:` fold (no chain accumulated), per this same rule.

_Previously: 2026-09-11 — Ad-hoc bugfix pass on the shipping carriers/rates screen (`App\Livewire\Shipping\Index`, story 0037's screen). Grew the log from forty-five entries to forty-six with the CSS Grid `align-items: stretch` entry and two related shipping-rates bugfixes. This file's longer prior `_Previously:` chain is folded into this single line per [contracts.md](contracts.md#doc-growth-management-rule) — no content changed or lost; see git history for the full prior chain if needed._
