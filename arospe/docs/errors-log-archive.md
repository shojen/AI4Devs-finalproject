# Errors Log Archive

Archived entries from [errors-log.md](errors-log.md) — moved here **byte-for-byte, unedited**, once that file grew past a manageable size. This is not a second, independent log: the entry format, the rule that history is never rewritten in place (only appended to via a dated "Correction —"/"Narrowed —" blockquote), and every other convention are defined once, in [errors-log.md](errors-log.md#entry-format) — read that file's intro first if you haven't.

These are the **oldest** entries (newest-first, like the main log), covering 2026-07-21 through 2026-08-26. Newer entries live in [errors-log.md](errors-log.md).

## Browse by topic

An agent working on a specific domain can jump straight to the 1-3 relevant entries below instead of reading the whole log top to bottom — see [contracts.md](contracts.md#token-efficient-reading-and-dispatch-rule)'s Token-Efficient Reading and Dispatch Rule. Covers all 46 entries across this file and [errors-log.md](errors-log.md); an entry touching more than one domain is listed under each. `(main log)` marks an entry that lives in [errors-log.md](errors-log.md).

**Livewire/Blade/Flux rendering & compilation quirks**
- [A `null` Livewire property bound to a native `<select>` silently dropped the user's own pick](#a-null-livewire-property-bound-to-a-native-select-silently-dropped-the-users-own-pick--2026-08-16) — 2026-08-16
- [`disabled:cursor-not-allowed` on a Flux button was never the cursor the user saw](#disabledcursor-not-allowed-on-a-flux-button-was-never-the-cursor-the-user-saw--2026-08-16) — 2026-08-16
- [A conditionally-bound `tooltip` prop rendered an empty tooltip on every enabled row](#a-conditionally-bound-fluxbutton-tooltip-prop-rendered-an-empty-tooltip-on-every-enabled-row--2026-08-16) — 2026-08-16
- [Absolutely-positioned dropdown occludes a sibling via DOM order, not CSS](errors-log.md#an-absolutely-positioned-element-with-no-explicit-top-takes-its-static-position-from-dom-order-and-can-silently-occlude-a-sibling--2026-08-31) — 2026-08-31 (main log)
- [A Livewire directive modifier's duration can't be interpolated into an attribute NAME](errors-log.md#a-livewire-directive-modifiers-duration-cannot-be-interpolated-as-part-of-a-component-tags-attribute-name--2026-08-31) — 2026-08-31 (main log)
- [A bare `@disabled(...)` inside a `<flux:button>` tag corrupts the whole compiled view](errors-log.md#a-bare-disableddisabled-inside-a-fluxbutton-tags-attribute-list-corrupts-the-whole-compiled-view--2026-08-31) — 2026-08-31 (main log)
- [Two `@directive(...)` calls in one component-tag attribute string silently fail to compile](#two-directive-calls-in-one-blade-component-tags-attribute-string-silently-fail-to-compile--2026-08-26) — 2026-08-26

**Testing/QA process & infrastructure**
- [A Pest `arch()` rule over an array of namespaces shipped green while proving nothing](#a-pest-arch-rule-over-an-array-of-namespaces-shipped-green-while-proving-nothing--2026-08-18) — 2026-08-18
- [Two agents dispatched in parallel both wrote to the same Blade view](#two-agents-dispatched-in-parallel-both-wrote-to-the-same-blade-view--2026-08-16) — 2026-08-16
- [A test asserted against a fixture address the local `.env` also pointed `SUPER_ADMIN_EMAIL` at](#a-test-asserted-against-a-fixture-address-that-the-local-env-also-pointed-super_admin_email-at--2026-08-12) — 2026-08-12
- [A verification record listing two of three quality gates is a record of two gates](#a-verification-record-that-lists-two-of-three-quality-gates-is-a-record-of-two-gates--2026-08-26) — 2026-08-26
- [A count-based HTML assertion counted a wrapper element it never meant to include](#a-count-based-assertion-over-rendered-html-counted-a-wrapper-element-it-never-meant-to-include--2026-08-21) — 2026-08-21
- [Both per-change quality gates are scoped by default, and both silently passed](#both-of-this-projects-per-change-quality-gates-are-scoped-by-default-and-both-silently-passed--2026-08-20) — 2026-08-20
- [A planned test asserted a refusal by `verified`, a middleware that refuses nobody in this app](#a-planned-test-asserted-a-refusal-by-verified-a-middleware-that-refuses-nobody-in-this-app--2026-08-20) — 2026-08-20

**Environment & infrastructure gotchas** (stale build artifacts, container/db config, resource limits)
- [A stale `public/hot` file made all 19 browser tests time out, misread as real UI bugs](errors-log.md#a-stale-publichot-file-from-an-old-npm-run-dev-session-made-all-19-browser-tests-time-out-misread-as-real-ui-bugs--2026-08-28) — 2026-08-28 (main log)
- [An Imagick resource limit was requested in the wrong unit, masked by `policy.xml` clamping it](errors-log.md#an-imagick-resource-limit-was-requested-in-the-wrong-unit-masked-by-the-hosts-own-policyxml-silently-clamping-it--2026-08-28) — 2026-08-28 (main log)
- [A "runs as non-root" claim was re-verified using the wrong `sail` invocation](errors-log.md#a-test-suites-own-runs-as-non-root-claim-was-re-verified-using-the-wrong-sail-invocation--2026-08-28) — 2026-08-28 (main log)
- [The full test suite degrades under repeated heavy runs in one session, unrelated to any code change](errors-log.md#the-full-test-suite-run-repeatedly-and-heavily-in-one-session-degrades-in-ways-unrelated-to-any-code-change--2026-08-28) — 2026-08-28 (main log)
- [A missing `.env.testing` let a self-healing `migrate:fresh` wipe the shared dev database](#a-missing-envtesting-file-let-a-self-healing-migratefresh-wipe-the-shared-dev-database--2026-08-26) — 2026-08-26

**Database/transactions/migrations**
- [A redundant `users_uuid_unique` index survived the UUID primary-key conversion](#a-redundant-users_uuid_unique-index-survived-the-uuid-primary-key-conversion--2026-08-12) — 2026-08-12
- [`DB::transaction($fn, attempts: N)` retried a closure that mutated a model created outside it](errors-log.md#dbtransactionfn-attempts-n-retried-a-closure-that-mutated-a-model-created-outside-it-producing-a-silent-lost-update-reported-as-success--2026-09-04) — 2026-09-04 (main log)
- [Wrapping existing code in `DB::transaction()` moved a cache flush nobody had written](#wrapping-existing-code-in-a-dbtransaction-moved-a-cache-flush-nobody-had-written--2026-08-21) — 2026-08-21

**Authorization/security**
- [Two of three security-audit rounds found the flaw in the previous round's fix](#two-of-the-three-security-audit-rounds-found-the-flaw-in-the-previous-rounds-fix--2026-08-19) — 2026-08-19
- [A listener read the pre-save value with `getOriginal()`, already overwritten by `save()`](#a-listener-read-the-pre-save-value-with-getoriginal-which-save-had-already-overwritten--2026-08-17) — 2026-08-17
- [Freeing a deleted user's email left their password-reset token live](#freeing-a-deleted-users-email-left-their-password-reset-token-live--2026-08-14) — 2026-08-14
- [A row carrying the configured email was treated as proof of mailbox ownership](#a-row-carrying-the-configured-email-was-treated-as-proof-of-mailbox-ownership--2026-08-09) — 2026-08-09
- [A scope exclusion named screens while the story edited a class those screens share](#a-scope-exclusion-named-screens-while-the-story-edited-a-class-those-screens-share--2026-08-24) — 2026-08-24
- [Wrapping existing code in `DB::transaction()` moved a permission-cache flush nobody had written](#wrapping-existing-code-in-a-dbtransaction-moved-a-cache-flush-nobody-had-written--2026-08-21) — 2026-08-21
- [A guard took the state it was guarding as a parameter, reopening its own hole one level up](#a-guard-took-the-state-it-was-guarding-as-a-parameter-reopening-its-own-hole-one-level-up--2026-08-20) — 2026-08-20
- [A security page documented the vulnerable code as current, because it was written before its own fix](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) — 2026-08-20

**Actions/API contract design** (parameter defaults, ambiguity)
- [An action's own parameter default reintroduced the omission ambiguity its stricter collaborator was built to close](errors-log.md#an-actions-own-parameter-default-reintroduced-the-omission-ambiguity-its-stricter-collaborator-was-built-to-close--2026-09-01) — 2026-09-01 (main log)
- [A guard took the state it was guarding as a parameter, reopening its own hole one level up](#a-guard-took-the-state-it-was-guarding-as-a-parameter-reopening-its-own-hole-one-level-up--2026-08-20) — 2026-08-20

**Docs/process/review quality**
- [A task file's relative links broke silently when it moved to `in-progress/`/`done/`](#a-task-files-relative-links-broke-silently-when-it-moved-to-in-progressdone--2026-08-17) — 2026-08-17
- [A doc's "this app has no X yet" claim outlived the X by two tasks](#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13) — 2026-08-13
- [Gherkin scenarios written with a generic "I" and bundled multi-action steps](#gherkin-scenarios-written-with-a-generic-i-and-bundled-multi-action-steps--2026-07-21) — 2026-07-21
- [One docs pass reported two gaps that were not there, both marked "verified"](errors-log.md#one-docs-pass-reported-two-gaps-that-were-not-there-both-marked-verified--2026-08-29) — 2026-08-29 (main log)
- [A reviewer's "correction" replaced an accurate technical explanation with a wrong one, unverified](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) — 2026-08-24
- [A deferred story's findings were claims about a tree that no longer existed](#a-deferred-storys-findings-were-claims-about-a-tree-that-no-longer-existed-and-one-of-them-would-have-reopened-a-bug-in-this-log--2026-08-23) — 2026-08-23
- [A security page documented the vulnerable code as current, because it was written before its own fix](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) — 2026-08-20

## Two `@directive(...)` calls in one Blade component tag's attribute string silently fail to compile — 2026-08-26

- **Context**: story 0018 (Sales Regions UI), Phase 5 code review finding N8 — a non-blocking "pick one, `@js()` matches `users.blade.php`" style nit against three `wire:click="setActive({{ \Illuminate\Support\Js::from(...) }}, {{ \Illuminate\Support\Js::from(...) }}, '')"` calls on `<flux:switch>` row controls, converted to `wire:click="setActive(@js($region['id']), @js(! $region['isActive']), '')"` for consistency with the file's other single-argument `@js()` calls (`openEditModal(@js(...))`, `setDefault(@js(...))`).
- **What happened**: every row's active/inactive toggle switch became a silent no-op — no PHP error, no JavaScript console error, no failed HTTP request. `Livewire::test(Index::class)->call('setActive', ...)` (direct component call) worked correctly, so the story's own component-level Feature tests never caught it; only a browser test asserting a *rendered outcome* after a real click did, and even that took several rounds to isolate because every earlier symptom (a stale count, an unchanged `is_active`) looked exactly like ordinary async/timing flakiness — the same failure shape this repo has genuinely hit before (see the `->waitForEvent('networkidle')` entries below). Dumping the real compiled HTML settled it: `wire:click="setActive(@js($region['id']), @js(! $region['isActive']), '')"` rendered **byte-for-byte as written**, `@js(...)` completely unprocessed, so Livewire tried to parse a JavaScript-illegal action-call string and dropped it.
- **Root cause — as recorded at the time, and now known to be wrong; see the correction below**: minimally reproduced outside this story's own markup — `<x-button wire:click="foo(@js($id), @js($flag))" />` against a trivial `<button {{ $attributes }}>` component renders the attribute unprocessed, verified via `Blade::render()`. The conclusion drawn was: *a single `@directive(...)` call inside a Blade component tag's plain attribute string compiles correctly; two of them in the same attribute value do not*, attributed to `ComponentTagCompiler::getAttributesFromAttributeString()` regex-scanning the raw attribute string before the general compiler pass runs. That framing also required a carve-out for this repo's own `x-show="matchesFilter(@js(...), @js(...))"` on a `<flux:table.row>`, which has two and works — and the carve-out was a hedge ("not a value fed into the component tag's own attribute list the same way") rather than a mechanism, which is the tell. **What is not in doubt, and is the whole practical content of this entry:** the conversion really did make every row toggle a silent no-op in a real browser, reverting to `{{ \Illuminate\Support\Js::from(...) }}` really did fix it, and a `{{ }}` echo compiles in every shape tested.
- **Fix applied**: reverted the three converted `wire:click="setActive(...)"` attributes in [`resources/views/livewire/sales-regions.blade.php`](../resources/views/livewire/sales-regions.blade.php) to `{{ \Illuminate\Support\Js::from(...) }}` (the pre-existing, proven-correct form), each with an inline comment recording the finding so a future pass doesn't "fix" them back to `@js()` for consistency a second time. `openEditModal(@js(...))` and `setDefault(@js(...))` — each carrying exactly **one** `@js()` call — were left unchanged; they were never the problem.
- **How to avoid it next time**: **prefer `{{ }}` echo expressions over `@directive(...)` calls for any dynamic value inside a Blade *component tag* attribute** (`<x-foo attr="...">`, `<flux:foo attr="...">`) — the correction below narrows *why* (the count is not the discriminator, and an anonymous `<x-…>` tag is the shape that provably breaks), but the safe form is unchanged and is what the repo ships. Use `{{ }}` echo expressions instead when more than one dynamic value must land in the same attribute string (`{{ \Illuminate\Support\Js::from($a) }}, {{ \Illuminate\Support\Js::from($b) }}` is the safe, already-idiomatic form here), or split the attribute across `:wire:click="..."` colon-bound dynamic syntax if the whole value is a single PHP expression. A "pick one style for consistency" cleanup on a component-tag attribute is not risk-free — verify the compiled HTML output (not just the absence of a PHP/console error) before trusting it, exactly as [the entry above](#a-verification-record-that-lists-two-of-three-quality-gates-is-a-record-of-two-gates--2026-08-26) already argues for the quality gates: a passing component-level test here proved nothing, because `Livewire::test()->call(...)` never goes through the compiled Blade view's `wire:click` attribute at all.

> **Correction — 2026-08-26, same day, during story 0018's Phase 6 docs pass.** This entry was verified before being carried forward, per this log's own [re-verify-what-a-story-wrote-about-itself rule](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20), and **the stated rule does not reproduce — in either direction.** Executed against `HEAD` in this project's own container, twice over: once through `Blade::render()` and once through the **real file-view compilation path** (a throwaway view registered with `View::addNamespace()`, so the `ComponentTagCompiler` + cached-compile route the app actually uses, not just the string compiler).
>
> | Shape | Result |
> | --- | --- |
> | `<flux:switch wire:click="setActive(@js($region['id']), @js(! $region['isActive']), '')" />` — **the exact shipped expression**, two `@js()`, array subscripts, leading `!` | **compiles**, both paths → `wire:click="setActive('ID1', true, '')"` |
> | `<flux:button wire:click="openEditModal(@js($region['id']))" />` — one `@js()` | compiles, both paths |
> | `<flux:table.row x-show="matchesFilter(@js(...), @js(...))" />` — the "carve-out" | compiles; **needed no carve-out** |
> | `<x-placeholder-pattern wire:click="foo(@js($a))" />` — an anonymous `x-` component, **one** `@js()` | **UNCOMPILED**, literal `@js(...)` in the output |
> | `<x-app-logo wire:click="foo(@js($a), @js($b))" />` — anonymous, two | UNCOMPILED |
> | any of the above with `{{ \Illuminate\Support\Js::from(...) }}` | compiles, every time |
>
> So the count is not the discriminator; the *component* is. A `flux:` tag compiles `@js()` at any count, and a plain anonymous `<x-…>` tag compiles it at **no** count. Neither half of "one is fine, two are not" survives, and the hedge about `x-show` was explaining something that never needed explaining. **What made the shipped screen go dead is therefore not established**, and it is deliberately not being guessed at here — the broken state was never committed (the Phase 5 commit contains the comment and the already-reverted markup, so `git show` cannot reconstruct it), and this project's own [hedge rule](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) says an unverified mechanism written up confidently is worse than an open question written up plainly.
>
> **What survives unchanged, and is what to act on:** the incident, the symptom (a silent no-op with no PHP error, no console error and no failed request), the fix, and the two rules in the bullet above — prefer `{{ }}` echoes for dynamic values in a component-tag attribute, and **verify the compiled output rather than the absence of an error** before trusting any attribute-syntax cleanup. Add one: **`@js()` inside an anonymous `<x-…>` component's attribute is broken outright** — that half *is* now verified by execution, at any count, and it is the shape the original minimal repro actually hit.

## A verification record that lists two of three quality gates is a record of two gates — 2026-08-26

- **Context**: story 0017 (Sales Region tax configuration backend). [conventions/base-standards.md](conventions/base-standards.md#quality-gates) names three gates every PHP change passes, in order: `php artisan test`, `vendor/bin/pint`, and **Larastan level 7** (`phpstan.neon`, over `app/`, `bootstrap/app.php`, `config/`, `database/`, `routes/`). The story ran three separate, explicitly-recorded verification passes before review — the Phase 3 reconciliation, the Phase 4 fix, and the Phase 4 re-audit round 2 — each ending in a written line of the form *"full suite re-run unscoped: N/N passed; `pint --test --format agent` (unscoped): passed"*.
- **What happened**: **gate 3 was never run.** Not run-and-passed, not run-and-deferred — absent from all three records, and nobody noticed across two `appsec-auditor` rounds and the story author's own re-verification. `code-reviewer` ran it for the first time at Phase 5 and it **failed**: two errors, both in `App\Livewire\SalesRegions\Index::save()`, where `SalesRegion::find($replacementDefaultId)` widens to `SalesRegion|Collection|null` because `Model::find()` accepts an array-ish key and `$replacementDefaultId` came straight out of `$validated[...]` as `mixed`. Real, shipped code, three phases past implementation, in the method the whole story is about.
- **Root cause**: **the record was of what was run, and it was read as a record of what passed.** Two gates reported green, the sentence describing them was long and specific and quantified (`869/869 passed, 2434 assertions`), and a specific, confident record is exactly what stops a reader asking what is *not* in it. Two things made it survivable for so long. The two gates that were run are the two with **loud, immediate, per-file feedback loops** — the ones a developer reaches for while iterating anyway — while Larastan is the one nothing else prompts you to run. And the missing gate is not silent forever: `.github/workflows/tests.yml` runs `composer types:check`, so CI would have failed this eventually. That is the mitigation, not the excuse — it means the omission cost a late catch rather than a shipped bug, and it also means nobody could have inferred the gap from a green local run, because there is no green local run to inspect.
- **Fix applied**: every value read out of `$validated` in `Index::save()` is now narrowed to `(string)` at the point of use, matching `App\Livewire\Users\Index::save()`'s own existing convention for the same read; re-run clean (commit `df377d0`). The story's Phase 5 section records the first clean Larastan run it ever had.
- **How to avoid it next time**: **a verification record must name all three gates and the result of each, including "not run" — a gate absent from the record is a gate that did not run, and should be read that way by whoever reads it next.** Concretely, the closing line of any phase that claims verification is three clauses, never two:

  ```bash
  php artisan test                    # unscoped — not --filter
  vendor/bin/pint --format agent      # unscoped — not --dirty
  vendor/bin/phpstan analyse          # level 7, per phpstan.neon — the one nothing else reminds you of
  ```

  Three notes on why this is a *different* entry from [the scoped-gates one below](#both-of-this-projects-per-change-quality-gates-are-scoped-by-default-and-both-silently-passed--2026-08-20) rather than a restatement of it. **(a)** That entry is about a gate that **ran** and reported "pass" while looking at almost nothing; this one is about a gate that never ran at all, which no amount of widening a `--filter` or dropping a `--dirty` would have caught. **(b)** It generalises past this repo's three commands: **an omission is invisible in a record of positives.** A phase that lists what it did, however precisely, carries no information about what it skipped — so any checklist worth having is enumerated by the checklist, not reconstructed from the report. **(c)** The rule binds a *reviewer* as much as an author: `code-reviewer`'s Phase 5 opening move should be to diff the phases' verification records against the three-gate list, which is the cheapest check in this whole workflow and the one that found this.
## A missing `.env.testing` file let a self-healing `migrate:fresh` wipe the shared dev database — 2026-08-26

- **Context**: no story — routine iteration on the full Pest suite from two separate Claude Code sessions on 2026-08-24, one running `./vendor/bin/sail artisan test` directly and one dispatching a `backend-expert`/`backend-qa` pair to run "revert-checks + full suite".
- **What happened**: the first session's suite run failed with `deleted_at column not found`, diagnosed (reasonably, on the symptom alone) as "the test database is in a corrupted state from a previous concurrent run". It ran `./vendor/bin/sail artisan migrate:fresh --env=testing --force` to reset it — and that command dropped and recreated all 16 tables in the **real development database**, `arospe`, not a testing one. `information_schema.TABLES` showed the table-creation timestamps land in an identical 4-second window twice more that same day (07:29 and again at 23:43, the second during the background suite-run agent's own work), each time wiping whatever had accumulated since — roles, permissions, the sales-region catalog, and every user, including the self-registered account created in between. Nobody noticed until a later session opened the app against an empty `arospe` two days later.
- **Root cause**: this repo's `phpunit.xml` sets `DB_DATABASE=testing`, but that `<env>` block only takes effect when Pest/PHPUnit itself boots the framework — it does nothing for a bare `artisan` command. Laravel's `--env=testing` flag makes `LoadEnvironmentVariables` look for a `.env.testing` file; when none exists (this project never had one), it silently falls back to loading the plain `.env`, whose `DB_DATABASE` is `arospe`. So `--env=testing` looked like the correct, safety-conscious way to reset "the test database" and was in fact indistinguishable from omitting the flag entirely. The agent's self-diagnosis ("the DB is corrupted, let me refresh it") was a reasonable read of the symptom and is not the mistake — the mistake is that the command it reached for to fix that had no guardrail stopping it from targeting production-adjacent data.
- **Fix applied**: created `.env.testing` (gitignored, one per checkout — see `.gitignore`) pointing `DB_DATABASE=testing` at a dedicated database, verified by re-running the exact failing command (`migrate:fresh --env=testing --force`) and confirming it now creates tables in `testing` while `arospe` is untouched. Restored `arospe` with `php artisan db:seed` (roles, the 38-permission catalog, the sales-region catalog, and the Super Admin grant). Documented the per-checkout requirement, and the further requirement that **each `git worktree` needs its own database name** (`testing1`, `testing2`, …) since all worktrees share one MySQL container, in [testing/worktree-databases.md](testing/worktree-databases.md).
- **How to avoid it next time**: **a database-flag whose safety depends on a file that might not exist is not a guardrail — verify the fallback path, not just the happy path, before trusting a `--env=` / `--database=` flag to isolate anything.** For this specific gap: never run `artisan migrate:fresh` (or any other destructive artisan command) against `--env=testing` without first confirming `.env.testing` exists and points somewhere other than the shared dev database — `cat .env.testing | grep DB_DATABASE` before the destructive command, not after. Two corollaries for anyone opening a new worktree: read [testing/worktree-databases.md](testing/worktree-databases.md) before running tests there, and if a suite run ever fails with a symptom that looks like "the schema is stale/corrupted", treat that as a signal to check *which* database actually received the last `migrate:fresh` before running another one — the diagnosis in this incident was right, but nobody checked which database the fix would land on.

## A scope exclusion named screens while the story edited a class those screens share — 2026-08-24

- **Context**: story 0015b, adding a structured `Log::warning` line to every authorization and rate-limit refusal on the Users and Roles admin screens. Its task file carried an explicit exclusion — *"The `settings/*` screens and `App\Livewire\Settings\*` are not in scope"* — written before its own **Q5** decision, which brought five **domain actions** into scope so that a future non-dashboard caller would inherit the logging. Both statements were reviewed and both passed Phase 2's INVEST gate.
- **What happened**: `appsec-auditor`'s Phase 4 audit **FAILed** the first implementation on a Medium (F-1). `App\Actions\Users\RequestEmailChange` — one of the five actions Q5 brought in — is called by `App\Livewire\Users\Index` (in scope, permission-gated) **and** by `App\Livewire\Settings\Profile::updateProfileInformation()` (`settings/profile`, `auth`-only, self-service, no permission gate at all). Instrumenting the action therefore instrumented the excluded caller, and the new log call sat on a rate-limit refusal branch: since `RateLimiter::attempt()` does not consume once the limit is exhausted, every retry past the third re-entered that branch for free. Any authenticated user could drive unbounded log writes at zero cost, from a screen the story had declared out of scope, using a hardening feature as the amplifier.
- **Root cause**: **the exclusion was phrased in terms of *screens*, and the story's unit of change was a *class*.** Those are only the same thing while nothing is shared, and the whole point of Q5 — the [action-owns-the-rule convention](conventions/directory-structure.md#an-authorization-rule-belongs-to-the-action-not-to-one-of-its-callers) — is that these actions are deliberately callable from more than one place. Nobody reconciled the two sentences because each is correct read alone; the contradiction only exists in the intersection, and nothing in the task file, the INVEST review or the implementation brief asks who else calls a file being edited. The consequence direction is what made it a security finding rather than noise: an in-scope caller is permission-gated and self-limiting, while the inherited one is reachable by every authenticated account in the app.
- **Fix applied**: not a scope exclusion — a **log ceiling**, so both callers keep the same trace, bounded. A second, 1-attempt `RateLimiter::attempt()` on a distinct key prefix gates the **log call only**, never the real limit, at `RequestEmailChange`'s three refusal sites and (for symmetry, so the shape cannot silently diverge later) `CreateUser`'s rate-limit site — see [`app/Actions/Users/RequestEmailChange.php`](../app/Actions/Users/RequestEmailChange.php). Phase 5 finding **R-3** then narrowed the aggregate limiter's ceiling key from target-only to `(target, actor)`, matching its two siblings, so a second administrator's refusal against an already-logged target is still recorded. The pattern and its three rules are documented in [architecture/authorization.md](architecture/authorization.md#a-shared-actions-rate-limit-refusal-needs-a-log-ceiling).
- **How to avoid it next time**: **before writing a scope exclusion, resolve it against the *files* the story will edit — `grep -rn "ClassName" app/` for every class in scope — and state the exclusion in terms of classes, not screens.** A screen-shaped exclusion is unenforceable the moment the story touches shared code, and this repo's conventions actively produce shared code: an action exists precisely so more than one caller can reach it. Two corollaries. **(a) Adding an unbounded side effect to a shared class is a capability grant to its *least*-privileged caller, not its most.** Ask who the weakest caller is, then ask what they can now make happen for free — a log write, a mail send, a queued job, a cache write all qualify. **(b) When the answer is "this caller shouldn't get the feature", prefer *bounding* the side effect over *excluding* the caller** — the exclusion re-splits one rule into two implementations, which is the drift this project's conventions spend most of their effort preventing. This is the shared-code half of the family recorded below: [wrapping existing code in a `DB::transaction()`](#wrapping-existing-code-in-a-dbtransaction-moved-a-cache-flush-nobody-had-written--2026-08-21) is about a change the diff hides, where this one is fully visible in the diff — what is hidden is the caller list of the file it lands in.

## A reviewer's "correction" replaced an accurate technical explanation with a wrong one, unverified — 2026-08-24

- **Context**: story 0015a's Phase 5 code review. `code-reviewer` flagged Larastan level 7 failing on `App\Livewire\Users\Index::isAdministratorRoleSelected()`'s `isset($this->roleId)` check (finding F-1), and in the same finding challenged the *docblock's* explanation of why the check is needed — it said a forged `->set('roleId', null)` leaves a direct `$this->roleId` read throwing `Livewire\Exceptions\PropertyNotFoundException`, and the reviewer called this "likely wrong for a public typed property with a default value", without executing anything to confirm.
- **What happened**: acting on that unverified challenge, the docblock was rewritten to say the property is cleared "via reflection" and that a direct read throws PHP's own native "must not be accessed before initialization" `Error` instead. This is what got committed (`30fdecc`). It was wrong on both counts, and worse than the text it replaced: the original `PropertyNotFoundException` claim was **correct**. A second, independent `code-reviewer` pass verified this by execution — `unset()`-ing the property and reading it directly from inside the class threw exactly `PropertyNotFoundException`, because `HandleComponents::setComponentPropertyAwareOfTypes()` does a plain `unset($component->$property)` (no reflection), and PHP routes a read of an `unset()` *declared* property through the magic methods even from inside the declaring class — `Livewire\Component::__get()` throws `PropertyNotFoundException`, and `isset()` observes that state only because `Component::__isset()` catches it internally, not because nothing is thrown.
- **Root cause**: a review finding phrased as "likely wrong" was treated as a confirmed defect and answered with a plausible-sounding replacement, without either side running the one command that would have settled it (a `tinker` call reading the property after `unset()`). The generic mechanism this repo already tracks — [a patch is only valid against the tree it was written on](#a-deferred-storys-findings-were-claims-about-a-tree-that-no-longer-existed-and-one-of-them-would-have-reopened-a-bug-in-this-log--2026-08-23) — applies here in a narrower form: **a review finding phrased with a hedge ("likely", "probably") is a claim to verify, not a claim to act on directly**, and verifying it is cheap when the claim is about concrete runtime behaviour.
- **Fix applied**: a third review pass verified the real mechanism by execution and the docblock in `app/Livewire/Users/Index.php` was corrected back to the accurate explanation (no reflection; `PropertyNotFoundException` via `Component::__get()`/`__isset()`), with a note recording that a Phase 5 pass previously replaced it with a wrong one. The `@phpstan-ignore isset.property` suppression itself was never wrong — only the prose explaining why it is needed.
- **How to avoid it next time**: **when a review finding hedges its own technical claim ("likely", "should be", "I'd expect"), verify it by execution before writing a fix around it — a hedge is a flag that nobody ran the code, not a lower-confidence fact to trust anyway.** For a claim about which PHP exception a specific line throws, the cheapest verification is always the same: reproduce the exact state (`unset()` the property, call the method, read `get_class($e)`) rather than reasoning from how the mechanism *ought* to work. This is a case of the review process itself introducing a defect that the original text did not have — worth watching for specifically when a review's fix is to *documentation or a comment* rather than to behaviour, since no test can catch a wrong sentence the way `phpstan`/`pest` catch a wrong `if`.

## A deferred story's findings were claims about a tree that no longer existed, and one of them would have reopened a bug in this log — 2026-08-23

- **Context**: story 0015, the consolidated follow-up for the eleven non-blocking findings story **0004**'s Phase 4 audit and re-audit raised against the Users CRUD backend (F4–F13, F17/F18). The task file had been written at 0004's closure and sat in `ai-spec/tasks/` while stories 0005, 0006, 0008, 0008a, 0009 and 0013 shipped — five of them editing the exact four files it was about.
- **What happened**: the story reached its Phase 2 INVEST gate and **failed** it (blocking findings B1–B6). Re-verifying each finding against `HEAD` by opening the cited file found that **six of eleven were wrong**, in three different ways. Half-done: F4 asked to lock two properties and one (`$deletingUserName`) was already `#[Locked]`; F17 asked to rename a flag story 0008a had already renamed. Structurally invalid: F12 asked to fix a null-collision inside `Index::authorizeRoleChange()`, a method 0008a **deleted** — along with the `administratorRoleId()` lookup it compared against — so the described comparison existed in no form at all. Ineffective-as-described: F10 asked for a `DB::transaction()` that 0008a had already added, while the atomicity goal it was added for was still unmet, because the email-change delegation ran *after* the commit. And the sharp one — **F8 prescribed retyping `$status` to `public ?UserStatus`, which would have reintroduced [the `<select>` desync bug recorded in this very log](errors-log-archive.md#a-null-livewire-property-bound-to-a-native-select-silently-dropped-the-users-own-pick--2026-08-16)**, where a user's first-option pick is silently discarded while the correct value stays on screen. Its supporting claim was also false: it cited an existing test dataset row that has never existed. F8's *finding* was real; only its prescribed patch was harmful.
- **Root cause**: **a deferred finding is a claim about a tree, and the task file freezes while the tree does not.** Nothing in the workflow re-reads a stored task before Phase 3, so its text arrives carrying the authority of an audit that genuinely was correct — about code from six stories ago. Two properties made it worse than ordinary staleness. First, findings were written as **prescribed patches** ("retype this property to `?string`") rather than as **properties that must hold** ("a forged status value must fail validation, not raise `\ValueError`"); a patch encodes assumptions about the surrounding code, so when that code moves the patch does not merely stop applying — it can become actively harmful, which is exactly what F8 did. Second, the ones that were already fixed fail *silently* on implementation: re-adding `#[Locked]` to an already-locked property is a no-op nobody reviews, so the wasted work is invisible rather than caught.
- **Fix applied**: the whole file was rewritten before any code was written (commit `66a5b88`), with every surviving finding re-verified by reading the real file at `HEAD` and each disposition recorded in a new **"What re-verification changed"** table — F4 halved, F7 re-scoped, F8's instruction explicitly *withdrawn* in writing with the errors-log entry it would have violated cited inline, F10 re-scoped to the ordering with a no-mail-on-rollback constraint, F12 recorded as **closed by 0008a and dropped**, F17 reduced to its test half, F13 split out to its own story `0015a`, F18 deferred by human decision. The line numbers it kept are labelled a reading aid, with Phase 3 required to re-locate each site.
- **How to avoid it next time**: **before a deferred story enters Phase 3, re-verify every finding it carries against `HEAD` by opening the file it cites — and record the disposition of each, including "already closed", in the task file itself.** Four concrete habits. **(a)** Write a finding as the **property that must hold**, never as the patch that would have held it; a patch is only valid against the tree it was written on, and there is no way to tell from reading it. **(b)** Treat a line number in a stored task as a reading aid, never a locator — if the cited line does not contain what the finding describes, the finding is stale until proven otherwise, not merely misnumbered. **(c)** When a deferred instruction contradicts something already in `docs/` — this log, a security page, a convention — **the docs win and the instruction is withdrawn in writing**, so the next reader sees a decision rather than an omission. **(d)** Record the closed findings rather than deleting them; "F12 was closed by 0008a" is the sentence that stops it being re-raised by the next audit. This is a fourth direction of the drift family this log already tracks — [a doc outliving the feature it denied](errors-log-archive.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13), [a doc outliving the bug it described](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20), [code drifting from a rule the doc still states correctly](#wrapping-existing-code-in-a-dbtransaction-moved-a-cache-flush-nobody-had-written--2026-08-21) — and the one with the longest fuse, because a backlog item can sit stale for months while looking exactly as authoritative as the day it was filed.

## Wrapping existing code in a `DB::transaction()` moved a cache flush nobody had written — 2026-08-21

- **Context**: task 0012 (module/sidebar access gating). Its Phase 4 audit re-read `App\Livewire\Roles\Index` because the story's cache-staleness tests exercise it, and found a Medium in code the story neither wrote nor changed. The rule it violates has been documented on this project's own security pages since task 0002 — [security/authorization-patterns.md](security/authorization-patterns.md#flush-the-permission-cache-after-the-transaction-commits-never-inside-it), *"Flush the permission cache after the transaction commits, never inside it"* — complete with the ✅ two-flush code shape and the reason (`config/permission.php` caches the whole roles+permissions graph for 24 hours on the **shared** `database` store).
- **What happened**: `saveRole()` and `deleteRole()` shipped with the ❌ shape that page names — the permission cache flushed only *inside* their `DB::transaction()`, never after the `COMMIT`. On the shared store this fails **open** on a revocation: a concurrent request landing in the window between the in-transaction flush and the commit misses the cache, reads the pre-commit rows, and re-caches the escalated grant for 24 hours. It survived story 0010's three-round Phase 4 audit, the whole of story 0011, and a Phase 5 code review of each.
- **Root cause**: **the flush at issue was never written by this repo.** Neither method contains a flush the author chose to place; `syncPermissions()` fires one internally, and `App\Models\Role`'s `deleted` event fires the other. Both were correctly post-commit until story 0010's own Phase 4 finding F4 — an unrelated atomicity concern ("a failure between the rename and the permission sync must not leave a role persisted with the wrong set") — wrapped the existing statements in `DB::transaction()`. That wrapper relocated a side effect **with no flush line appearing, moving or disappearing anywhere in the diff**, so a reviewer grepping the change for `forgetCachedPermissions` found nothing and correctly concluded there was nothing to check. Compounding it, the regression is untestable by construction: `phpunit.xml` pins `CACHE_STORE=array`, making the permission cache per-process in tests, so the suite was 618/618 green with the window wide open — and stays 618/618 green today if either fix line is deleted.
- **Fix applied**: `app(PermissionRegistrar::class)->forgetCachedPermissions()` added immediately after each `DB::transaction()` call in [`app/Livewire/Roles/Index.php`](../app/Livewire/Roles/Index.php) (commit `0a53acb`), each under a multi-line comment naming the finding and linking the rule — the mitigation for a fix no test can pin, matching `RolePermissionSeeder`'s identical precedent. The security page now also carries the three real call sites and the generalized ⚠️.
- **How to avoid it next time**: **adding a `DB::transaction()` around existing code is a change to every side effect that code already performed — audit each of them, including the ones the diff does not show.** Vendor calls are exactly where this bites, because their side effects are invisible at the call site: a cache flush, an event dispatch, a queued job, a broadcast, a `Storage` write, or a mail send that was outside a transaction is now inside one, and each has its own correct side of the commit. Two habits. **(a)** When a finding's remedy is a *wrapper* rather than a new statement, list what the wrapped code calls and what each of those does beyond returning — this is the cheapest form of the "[audit the remediation as new code](errors-log-archive.md#two-of-the-three-security-audit-rounds-found-the-flaw-in-the-previous-rounds-fix--2026-08-19)" rule already recorded below, applied to the one shape where the diff itself hides the change. **(b)** Before writing the fix, grep `docs/security/` for the call you are about to move — the rule broken here was already written down, with the correct code, on a page the breaking story cited for other reasons. This is the third direction of the doc/code drift family this log records: [a doc outliving the feature it denied](errors-log-archive.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13), [a doc outliving the bug it described](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20), and now **code drifting away from a rule the doc still states correctly** — the only one of the three that a re-read of the doc cannot catch, because the doc is not wrong.

## A count-based assertion over rendered HTML counted a wrapper element it never meant to include — 2026-08-21

- **Context**: task 0011 (Roles & permissions UI). Its Phase 4 security audit established a rule worth keeping — that a **presence** assertion ("this permission's checkbox is still there") cannot catch a *second*, unrelated checkbox also going missing, so the regression test for "the catalog renders in full except one withheld value" must assert a **count**: rendered checkboxes == catalog size, minus exactly the withheld set. Two such tests were added to `tests/Feature/Roles/IndexUiTest.php`.
- **What happened**: both were written as `substr_count($html, 'value="')` — every `value="` attribute in the whole rendered page. Phase 5 flagged it (finding **F-4**) as correct only by coincidence: nothing *else* in the create/edit modal happens to emit that attribute today, so a future `flux:select` or hidden input would move the number with no real omission occurring. The obvious fix — scope it to the checkbox element — then walked into a second trap of its own: the Flux-rendered element is `<ui-checkbox …>`, but `flux:checkbox.group` also emits a `<ui-checkbox-group>` wrapper, whose opening tag **starts with the same string**. Counting `'<ui-checkbox'` therefore over-counts by exactly one, and does so silently: the number is off by a constant, so the test still passes or fails as a block and never looks wrong.
- **Root cause**: the assertion was designed from what the *Blade source* writes (`<flux:checkbox value="…">`) rather than from what the browser receives. Flux components are not one-to-one with their rendered output — a group renders a custom element of its own — and a substring count has no notion of element boundaries, so a child's tag name being a prefix of its parent's is invisible to it. The generic hazard is that a count assertion fails **quietly by a constant**: unlike a presence assertion, which is either satisfied or not, an over-count that is stable across runs reads as the true number and gets baked into whatever expectation makes the test green.
- **Fix applied**: both assertions rescoped to `substr_count($html, '<ui-checkbox ')` — with the **trailing space**, which is what excludes `<ui-checkbox-group` — after tracing the actual rendered markup rather than the stub (commit `48dbf54`). The expected values (`$catalogSize - 1` for a non-Super-Admin, `$catalogSize` for a Super Admin) are unchanged and were re-verified against the real render. The security rule the tests exist for is in [security/authorization-patterns.md](security/authorization-patterns.md#a-control-omitted-from-the-dom-is-safe-only-for-the-one-value-whose-guard-preserves-an-omission).
- **How to avoid it next time**: **a count assertion over rendered HTML must be scoped by a string that cannot match a sibling or wrapper element, and both the selector *and* the expected number must be read off the real rendered output before the test is trusted.** Three concrete habits. **(a)** Dump the HTML once and grep it, rather than deriving the selector from the Blade source — this repo's two 2026-08-16 Flux/Blaze entries below already establish that a Flux component's real output cannot be predicted from its stub, and the same holds for its element names. **(b)** When the selector is an element name, include the delimiter that ends it (`'<ui-checkbox '`, not `'<ui-checkbox'`); custom-element naming makes `<x>` a prefix of `<x-group>` routinely, and Flux's `*.group` components are the standing example. **(c)** Prove the count can be wrong: temporarily remove one item and confirm the number moves by exactly one. A count assertion that has never been observed changing is the same vacuous-coverage failure as [the `arch()` rule below](errors-log-archive.md#a-pest-arch-rule-over-an-array-of-namespaces-shipped-green-while-proving-nothing--2026-08-18), arriving through a different door.

## Both of this project's per-change quality gates are scoped by default, and both silently passed — 2026-08-20

- **Context**: task 0010 (Roles & permissions management — backend). The story's own code was implemented, tested and merged onto `feature-entrega2-ARP`; its Phase 4 security audit and Phase 5 code review then ran against that merged state.
- **What happened**: two independent gates that [conventions/base-standards.md](conventions/base-standards.md#quality-gates) names as mandatory reported success while the thing they exist to catch was present.
  - **The test gate.** Only the Roles-scoped tests were ever run (`php artisan test --filter=…`, which the same conventions page recommends as "the narrowest relevant test(s) first"). The **full** suite was not run once between the story's first merge and the Phase 4 re-audit — and when it finally was, `tests/Feature/Authorization/SuperAdminRoleConfigSourceOfTruthTest.php` was **red**, on a completely unrelated pre-existing test this story had broken.
  - **The formatting gate.** `vendor/bin/pint --dirty --format agent` — the exact command the conventions page mandates — reported clean while three test files carried real violations (`fully_qualified_strict_types`, `ordered_imports`). A plain `vendor/bin/pint --format agent` found them immediately. They reached Phase 5 as finding **F-1**, blocking.
- **Root cause**: the same one twice — **each gate's default scope is narrower than the change it is gating, and a narrowed gate reports "pass", not "not checked".** `--dirty` inspects only files with *uncommitted* changes, so it becomes a no-op the moment the work is committed: the more disciplined the committing, the less the gate sees. `--filter` inspects only the tests whose names match, so it can never observe a change's effect on the rest of the suite. Both are correct as *iteration* tools and both are wrong as *completion* gates, and nothing in their output distinguishes the two uses. The test failure additionally had two compounding causes, each individually invisible: story 0010's own holder-count guard applies to **every** role rather than only to the custom roles the story was about, and the developer's ambient `SUPER_ADMIN_EMAIL` provisioned a second, unaccounted-for Super Admin holder — the identical ambient-config sensitivity [the 2026-08-12 entry below](errors-log-archive.md#a-test-asserted-against-a-fixture-address-that-the-local-env-also-pointed-super_admin_email-at--2026-08-12) already names, in a test written before that entry existed.
- **Fix applied**: the Pint violations were fixed by an unscoped `vendor/bin/pint --format agent` run (commit `fcff6dd`, "style(roles): fix Pint violations and correct three stale docblocks"). [`tests/Feature/Authorization/SuperAdminRoleConfigSourceOfTruthTest.php`](../tests/Feature/Authorization/SuperAdminRoleConfigSourceOfTruthTest.php) gained `config(['auth.super_admin.email' => null]);` in its `beforeEach` (matching what `tests/Feature/Seeders/DatabaseSeederTest.php` already did) and an explicit `removeRole()` before the deletion its scenario is actually about, each with a comment naming the cause. [conventions/base-standards.md](conventions/base-standards.md#quality-gates) now distinguishes the iteration form of each gate from the completion form.
- **How to avoid it next time**: **a gate that takes a scope argument must be run twice — narrow while iterating, and unscoped before the work is declared done — and "the narrow run passed" is never evidence about anything outside its scope.** Concretely, before a story leaves Phase 3: run `vendor/bin/pint --format agent` (no `--dirty`) and `php artisan test` (no `--filter`), in that order, and treat those two runs — not the filtered ones — as the record. Two corollaries. **(a) `--dirty` is inversely coupled to commit hygiene**, so it fails hardest exactly when the workflow is being followed best; never let it be the last formatting check on a committed branch. **(b) A guard that a story adds "for its own feature" almost never applies only to that feature.** `guardAgainstHolders()` was specified as part of the roles-CRUD story and reads as scoped to it, but it is a model-event listener on `App\Models\Role` and therefore binds every role in every test in the repo. When a story registers a model event, an observer, a global scope, or middleware, the blast radius is the whole suite by construction — that alone is sufficient reason to run it.

## A planned test asserted a refusal by `verified`, a middleware that refuses nobody in this app — 2026-08-20

- **Context**: task 0040, moving `users.index` out of `routes/web.php` into its own `routes/users.php`. Phase 1's QA contribution specified one new test to guard the relocation: an unverified user hitting `route('users.index')` should be redirected to `route('verification.notice')`, so that hand-copying the `Route::middleware(['auth', 'verified'])->group(...)` block into a new file could not silently drop `verified`.
- **What happened**: Phase 3's red-test step wrote that test and it did not behave as a red test at all. Proven mechanically rather than argued: with `verified` temporarily removed from the new file's group entirely, the test's outcome was **bitwise identical** to the unmodified group — same 403 from `can:users.view`, same message. The assertion it was built on could never distinguish a correct relocation from one that had lost `verified`, so it was reverted rather than kept, and this story shipped with no new test at all.
- **Root cause**: `App\Models\User` does not implement `Illuminate\Contracts\Auth\MustVerifyEmail` — the import sits commented out at the top of [`app/Models/User.php`](../app/Models/User.php) — and Laravel's stock `EnsureEmailIsVerified` only acts when `$request->user() instanceof MustVerifyEmail`. That condition is `false` for every actor in this app, so `verified` is a **structural no-op on every route that carries it**; account usability is enforced at sign-in through `users.status` instead. None of that was newly discovered here: [architecture/authentication.md](architecture/authentication.md#account-status-and-activation) has stated it in one plain sentence since task 0007. The test was designed from the middleware's *name* and from the route group's *appearance*, and nobody opened the doc that already answered the question.
- **Fix applied**: no code change — the test was reverted and the story's "Tests to perform" section rewritten with a Phase 3 correction note recording why the case was dropped rather than left as dead coverage (`ai-spec/tasks/.../0040-move-users-route-to-own-file.md`). The four pre-existing HTTP-level tests in `tests/Feature/Users/IndexTest.php` (guest → login redirect, no permission → 403, permission → 200, Super Admin bypass → 200) already cover every outcome that actually holds, and re-running them unmodified *is* the regression check for a pure relocation.
- **How to avoid it next time**: **before writing a test that asserts a middleware refuses someone, confirm that middleware can refuse anyone in this app — a middleware being present on a route is not evidence that it is live.** The cheap proof is the one Phase 3 ran: delete the middleware, re-run the test, and see whether the outcome changes; if it does not, the test carries no signal no matter which way it is written. `verified` is the known dead one here and will stay dead until `User` implements `MustVerifyEmail`, so any future story that copies an `auth` + `verified` group into a new area file — task 0010's `routes/roles.php` is the next one — must not plan a `verified` test either. This is the same class as the vacuous `arch()` rule two entries below (an assertion that cannot fail is not coverage); the added angle is that the control under test may not exist at all, and that the answer was already written down in the doc that owns it — check [architecture/authentication.md](architecture/authentication.md) before designing an auth-middleware test case.

## A guard took the state it was guarding as a parameter, reopening its own hole one level up — 2026-08-20

- **Context**: task 0009, `App\Actions\Roles\EnforceAdministratorPermissionGrant` — the action that refuses a role-save payload newly granting `roles.manage-administrators` to anyone but the Super Admin. Three Phase 4 audit rounds; each round's findings were against the **previous round's fix**, not against the original implementation.
- **What happened**: round 1 found F1 — the guard only asked whether the submitted payload *contained* the protected permission. Since `Role::syncPermissions()` replaces a role's entire permission set, and story 0011's toggle is never rendered to a non-Super-Admin at all, a broad administrator editing an unrelated field on a role that legitimately held the permission submitted a payload **omitting** it, silently revoking a Super Admin's grant. The fix introduced a "before" snapshot so the action could diff. Round 2 then failed that fix: the snapshot arrived as a caller-supplied `array $currentPermissionNames` parameter (N2 — a caller asserting an untrue "before" makes a new grant look pre-existing), the two snapshots were normalised asymmetrically so the diff silently mismatched (N3), and the membership check did not flatten nested arrays/Collections the way `syncPermissions()`'s own vendor code does, proven live to bypass the guard entirely (N1). Round 3 passed, after one more: the replacement `?Role $role` parameter had shipped with a `= null` default, so a forgotten third argument at a future call site would silently mean "nothing is currently granted" (NR1).
- **Root cause**: two, compounding. (1) **The guard trusted its caller for the very state it existed to protect.** "What does this role currently hold" is the input the whole decision turns on; accepting it as a parameter makes the guard only as strong as every present and future call site. (2) **Each fix was reviewed against the finding it answered rather than as new code with its own attack surface** — the diff logic was read as "does this close F1?", and it did, so nobody asked where its *new* inputs came from. F1 itself had a third root cause worth naming separately: nothing in the payload distinguishes "the actor chose to remove this" from "the actor was never shown this", and a full-replace `sync*()` collapses the two.
- **Fix applied**: the `array $currentPermissionNames` parameter was replaced by `?Role $role` (nullable, **no default**), and [`app/Actions/Roles/EnforceAdministratorPermissionGrant.php`](../app/Actions/Roles/EnforceAdministratorPermissionGrant.php) now loads the "before" state itself with `$role->load('permissions')` — removing N2 and N3 structurally rather than validating around them. `normalizeNames()` applies the identical `Collection::make(...)->flatten()` pass `HasPermissions::collectPermissions()` uses, so no shape can be invisible to the check while still being honoured by the sync. F1's own resolution — *preserve* an omitted-but-already-granted permission rather than deny the request — was **not** derivable from the spec, and was decided by the human mid-audit (asked explicitly, chose preserve); the choice is recorded in the code's docblock, in [architecture/authorization.md](architecture/authorization.md#who-may-grant-a-permission--the-meta-rule-layer), and in the task file's implementation record. Both durable rules are in [security/authorization-patterns.md](security/authorization-patterns.md#a-full-set-sync-behind-a-partially-visible-form-must-preserve-what-the-actor-cannot-see).
- **How to avoid it next time**: three concrete rules. **(1) A guard must derive the state it guards, never accept it** — take the model and read it (`load()`, not `loadMissing()`), and treat any "current state" parameter in an authorization signature as the finding itself. **(2) The "audit the remediation as new code" rule from the entry below is not a one-off — it is now the observed norm.** Two consecutive stories have needed three Phase 4 rounds, each catching the previous round's own fix, so budget for the extra rounds rather than treating round 2 as a formality. What this story adds to it: the highest-risk remediation is one that introduces a **new input** (a parameter, a snapshot, a cache), because that input's provenance was never in scope for the original review. **(3) When a full-replace `sync*()` is driven by a form that shows the actor only part of the set, absence is not a decision** — diff both directions and authorize adding and removing separately, and when "preserve" versus "deny" is a product judgement rather than a security one, stop and ask the human instead of picking; an unrecorded choice reads as an oversight to the next auditor.

## A security page documented the vulnerable code as current, because it was written before its own fix — 2026-08-20

- **Context**: task 0016 (the `sales_regions` catalog). Phase 4's `appsec-auditor` wrote a new section into [security/seeder-safety.md](security/seeder-safety.md#a-catalog-seeder-must-fail-loudly-rather-than-commit-a-structurally-invalid-catalog) — "A catalog seeder must fail loudly rather than commit a structurally invalid catalog" — quoting the two fail-open shapes it had just found in `SalesRegionSeeder`: `'parent_id' => $spain?->id`, and a repair `update()` whose zero-row outcome went unchecked.
- **What happened**: those findings were fixed later in the same story (both lookups now `throw_if(...)`), but the page was not revisited. It shipped stating *"Two shapes to look for, both present in `SalesRegionSeeder` today"* above a verbatim quote of code that no longer existed, described as accepted low risk. Phase 5's `code-reviewer` caught it as finding F-1. A reader following the page's own citation to `database/seeders/SalesRegionSeeder.php` would have found neither the `?->` nor the unchecked `update()` there.
- **Root cause**: the security pages are authored **during** Phase 4, from the auditor's vantage point, where the vulnerable shape genuinely *is* the current code — so the present tense is accurate at the moment it is written and silently false by the time the story closes. Nothing in the workflow re-reads a Phase 4 page after Phase 5's fixes land: the change→doc mapping routes Phase 6 to the docs describing *the story's* change, and this page had already been written, so it read as done. The failure is invisible in review because the page is internally coherent — only opening the cited file disproves it.
- **Fix applied**: the section was rewritten as an explicit ❌ before / ✅ after pair — the pre-fix shape labelled as no longer in the repo, the shipped `throw_if(...)` guards quoted from the real file, plus the third guard (`assertValidCountryFixture()`) the same audit added. The underlying rule was correct and is unchanged.
- **How to avoid it next time**: **a security page written during Phase 4 documents a state that Phase 5 is expected to change, so Phase 6 must re-verify every code quote in it against the file it cites — treat an audit-authored page as unsynced by default, not as already done.** Two concrete habits: write a vulnerability write-up as a ❌/✅ pair from the start, so the fix has a slot waiting for it instead of needing the framing sentence rewritten; and never describe a *found* finding in the present tense ("both present today") — say "as found" and name the state it is in now. This is the [bare-negative-claim entry](errors-log-archive.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13)'s failure class arriving from the opposite direction: there a doc outlived the feature it denied, here a doc outlived the bug it described.

## Two of the three security-audit rounds found the flaw in the *previous* round's fix — 2026-08-19

- **Context**: task 0008a, which relocated the Administrator-tier authorization out of `App\Livewire\Users\Index` and into [`App\Actions\Users\CreateUser`](../app/Actions/Users/CreateUser.php) / [`UpdateUser`](../app/Actions/Users/UpdateUser.php). Phase 3 implemented the Phase 1 spec exactly, and the full suite was green.
- **What happened**: Phase 4 took **three** rounds to reach PASS, and the second round's findings were all against code the *first* round had just written. Round 1 found that the relocation was partial — the guard covered the Administrator tier, but assigning the **Super Admin** role through either action was completely ungated, and a name-only `UpdateUser` edit reached `$user->save()` with no authorization at all (F1). Round 2 then found two Mediums in F1's own fix: the new `Gate::authorize('update', $user)` ran *above* the `roles` reload that had been added for a different finding, so a caller passing a stale `->with('roles')`-hydrated instance could evade `UserPolicy::update()`'s Super Admin-target exclusion (N1); and the new Super Admin refusal checked only the **submitted** role, never whether the target *currently held* Super Admin — so a Super Admin actor, whose `Gate::before` bypass makes any `Gate`-mediated check moot, could demote another Super Admin through `syncRoles()` (N2). Only round 3 passed.
- **Root cause**: three compounding causes, none of them carelessness. **(1) A relocation is scoped by what moved, not by what the destination now owns.** The spec said "move the Administrator guard", so the Administrator guard moved — and nobody asked what *else* an action that now owns its own authorization is responsible for. The Super Admin tier had never been the component's job, so it was nobody's. **(2) A fix is new code, written under the framing of the finding it answers, and inherits none of the original review.** N1's reload existed and was correct; it was added to answer "read the whole roles collection, not an unordered `first()`", so its *placement* was chosen for that question and never re-examined against the `Gate` call sitting above it. **(3) Both new flaws fail open and silently.** A stale relation is indistinguishable from a role the target genuinely does not hold, and a `Gate`-mediated refusal reads as a refusal in every test whose actor is not a Super Admin.
- **Fix applied**: `Role::isSuperAdminRoleRow()` added as the row-shaped counterpart to `isAdministratorRole()`; both actions now `Gate::authorize()` the whole operation and refuse the Super Admin tier by **direct throw** rather than through `Gate`; `UpdateUser` refuses a target that currently holds Super Admin as well as a submission that assigns it, moves `$user->load('roles')` to the literal first statement of `__invoke()`, compares status against `getRawOriginal()`, and wraps its writes in `DB::transaction()`. The two durable rules are written up in [security/authorization-patterns.md](security/authorization-patterns.md#a-rule-that-must-bind-a-super-admin-actor-must-be-a-direct-throw-not-a-gate-check) and [the section below it](security/authorization-patterns.md#authorization-that-consults-a-relation-must-reload-it-before-the-first-check-reads-it); the shipped placement is documented in [architecture/authorization.md](architecture/authorization.md#the-guard-belongs-to-the-action-not-to-the-caller).
- **How to avoid it next time**: **re-audit a security fix as new code, not as a diff against a finding — and keep re-auditing until a round finds nothing.** A round that only re-tests the reported finding will confirm the fix and miss what the fix introduced; budget for the extra rounds rather than treating round 2 as a formality. Two concrete checks that would each have caught a finding here on the first pass. **(a) When a guard moves into a class, enumerate every privileged value that class can now write — not only the one the story is about.** "Which role can this action assign?" has three answers here (ordinary, Administrator, Super Admin) and the story's title only named one. **(b) For every authorization check, name the statement that last wrote the state it reads, and confirm it runs above it.** Relation hydration, `getOriginal()` vs `getRawOriginal()`, and an in-memory attribute a caller may have pre-staged are all state a check silently trusts; ordering is part of the guard, not an implementation detail. See also the `getPrevious()`-not-`getOriginal()` entry below — the same class of "which source is this reading, and when?" mistake, from a third direction.

## A Pest `arch()` rule over an array of namespaces shipped green while proving nothing — 2026-08-18

- **Context**: task 0008. The story's central invariant is that `App\Models\Role` is the *only* role model class application code may use — a direct `Spatie\Permission\Models\Role` import reaches the same `roles` table carrying none of the Super Admin guards, so it is a live bypass. That invariant was to be enforced by an architecture test rather than by convention, covering the two PSR-4 roots the story touches (`App` and `Database\Seeders`).
- **What happened**: the obvious form shipped first — `expect(['App', 'Database\Seeders'])->not->toUse(Role::class)` — and it passed. It would have gone on passing if `Database\Seeders` alone had imported the raw Spatie model, which is exactly the regression the test exists to catch. A test whose whole job is to fail on a specific import was structurally incapable of failing on half its stated scope.
- **Root cause**: Pest's `expect(array $targets)` evaluates the rule **disjunctively** across the given targets — it is satisfied as soon as *any one* target passes, not when all of them do. Nothing in the API signals this; an array of namespaces reads as "apply this rule to each of these". Verified against the real repo rather than reasoned about: `expect(['App', 'Database\Seeders'])->not->toUse(Gate::class)` passes today even though `Gate` **is** imported in `app/Livewire/Users/Index.php` — `Database\Seeders` doesn't import it, and that one satisfied target carries the whole assertion.
- **Fix applied**: split into two separate single-namespace rules in [`tests/Unit/ArchitectureTest.php`](../tests/Unit/ArchitectureTest.php), one per PSR-4 root, with the counter-example above recorded in the file as a comment so nobody re-merges them. The convention it enforces is documented in [architecture/authorization.md](architecture/authorization.md#one-role-model-class-in-application-code).
- **How to avoid it next time**: **write one `arch()` rule per namespace — never pass an array of targets to `expect()` — and prove every architecture test can actually fail before trusting it.** A `->not->toUse()` assertion is a *negative* claim, so it is green both when the invariant holds and when the test is vacuous, and nothing in the output distinguishes the two. The proof is cheap and takes one run: temporarily add the forbidden import to a file in each namespace the rule claims to cover, confirm the test goes red for *each* one separately, then revert — the same regression-proof discipline this project already applies to browser tests (see [testing/frontend/README.md](testing/frontend/README.md)). This generalises past `arch()`: any assertion that passes by default deserves a deliberate failure before it is counted as coverage.

## A listener read the pre-save value with `getOriginal()`, which `save()` had already overwritten — 2026-08-17

- **Context**: task 0007 Phase 4 (`appsec-auditor`). The story turned `users.status` from a badge label into an access-control state, which made [`App\Listeners\ActivateVerifiedUser`](../app/Listeners/ActivateVerifiedUser.php) — the single `Inactive` → `Active` transition — a privilege-granting control for the first time. Its only guard was `$user->status !== UserStatus::Inactive`, and both the listener and [architecture/authentication.md](architecture/authentication.md#account-status-and-activation) described that guard as what "stops a verification from silently undoing an administrator's suspension".
- **What happened**: two mistakes, one on top of the other. First, the guard was written against the wrong half of the problem: it does cover `Suspended`, but `Inactive` is *also* a state an administrator sets deliberately from the Users editor, and for that state the condition is not a guard — it is the trigger. Since `App\Actions\Users\ConfirmEmailChange` fires `Verified` from the deliberately session-less `email-change.confirm` route, a deactivated user could reactivate themselves with nothing but a 60-minute-old pending-email link (verified by execution: `BEFORE: status=inactive` → `AFTER: status=active`). Second, the audit's own first fix — `is_null($user->getOriginal('email_verified_at'))` — was **wrong**, and only broke `EmailVerificationTest` and `PasswordResetTest` once it was actually run; reading it, it looks obviously correct.
- **Root cause**: `Model::save()` ends in `finishSave()`, which calls `syncOriginal()` unconditionally after *every* successful save. Every caller that fires `Verified` writes `email_verified_at` and calls `save()` before firing the event on that same instance, so by the time the listener runs, `getOriginal()` already holds the value that was just written. A `getOriginal()`-based guard therefore always finds a non-null "original" and refuses every legitimate first verification — it fails closed, which is why it produced test failures rather than a security hole, but it would have silently disabled activation entirely. What does survive is `$this->previous`, populated by `syncChanges()` from inside `performUpdate()` *before* `finishSave()` runs, and read with `getPrevious()`.
- **Fix applied**: `ActivateVerifiedUser` now requires the user to have *never* been verified, read from `getPrevious()` and failing closed when the key is absent. Both producers — [`app/Actions/Fortify/ResetUserPassword.php`](../app/Actions/Fortify/ResetUserPassword.php) and [`app/Actions/Users/ConfirmEmailChange.php`](../app/Actions/Users/ConfirmEmailChange.php) — carry a comment recording that their `save()` must remain the last dirty write before the event. The full rule and its four constraints are in [security/login-status-enforcement.md](security/login-status-enforcement.md#status-is-now-an-access-control-state--every-inactive--active-transition-is-a-privilege-grant).
- **How to avoid it next time**: two rules, both concrete. **(1) In a listener or observer that runs after `save()`, the pre-change value is `getPrevious()`, never `getOriginal()`** — `getOriginal()` means "as of the last sync", and a successful save *is* a sync. It comes with constraints worth checking before relying on it: it only carries attributes that were dirty on the last save (so `array_key_exists()` first and fail closed), `syncChanges()` replaces it wholesale on each dirty save, `performInsert()` never populates it, and a `ShouldQueue` listener loses it entirely because `SerializesModels` re-fetches the model. **(2) When a column stops being descriptive and starts denying access, re-audit every write to it as a privilege grant** — including the ones that were reviewed and approved while it was still cosmetic. Ask what the state *denies* and who can undo it, not what it is called; a single enum case carrying two meanings ("never proved their mailbox" and "an administrator turned this off") cannot be guarded by testing the case alone.

## A task file's relative links broke silently when it moved to `in-progress/`/`done/` — 2026-08-17

- **Context**: reviewing `ai-spec/tasks/done/0006-users-list-editor-ui.md` after closure, following up on a user-reported broken link: `[PRD Epic 1](../../docs/PRD/PRD.md#epic-1--users-roles--permissions)`.
- **What happened**: the link's anchor fragment was correct (`docs/PRD/PRD.md`'s own generated table of contents uses the identical `#epic-1--users-roles--permissions` slug), but the path resolved to `ai-spec/docs/PRD/PRD.md` — a file that doesn't exist. Auditing every other `done/` task file for the same `](../../docs/...)` pattern found the identical break in five more: `0002`, `0003`, `0004`, `0005`, and `0006b` (20 broken links in `0006b` alone).
- **Root cause**: a task file's relative links are written once, at Phase 1, from `./ai-spec/tasks/<file>.md` — two directory levels below the repo root, so `../../docs/...` correctly reaches `docs/`. Phase 3 step 0 and Phase 7 then move the file to `./ai-spec/tasks/in-progress/<file>.md` or `./ai-spec/tasks/done/<file>.md` — **three** levels below the repo root — without anyone revisiting the links already written inside it. `../../docs/...` now resolves one directory too shallow. Nothing in the move itself surfaces this: `git mv`/a plain file move changes only the file's location, never its content, so the broken link sits silently until someone actually clicks it.
- **Fix applied**: `sed 's#\.\./\.\./docs/#../../../docs/#g'` across the six affected files in `ai-spec/tasks/done/`, verified afterward by resolving every `](...)` target in each file against the filesystem from the file's real (post-move) location. `docs/workflow.md` now documents this as a mandatory step — see its new [Link-integrity check on every stage move](workflow.md#link-integrity-check-on-every-stage-move) section, cross-referenced from Phase 3 step 0 and Phase 7.
- **How to avoid it next time**: **a file move that changes directory depth invalidates every relative link the file contains, even though nothing about the move touches the file's content.** Whenever `product-owner` moves a task file between `./ai-spec/tasks/`, `in-progress/`, and `done/` (a depth change from two to three directory levels), `docs-keeper` must re-resolve every relative link in the moved file — the path against the filesystem, and any `#fragment` against a real heading in the target — before considering the move complete. The same check applies to any other file this project ever moves between directories at different depths, not just task files.

## A `null` Livewire property bound to a native `<select>` silently dropped the user's own pick — 2026-08-16

- **Context**: story 0006 (Users list + create/edit modal UI), after the story had already been reviewed and closed. The create/edit form's Role and Status fields are `flux:select` elements bound with a bare `wire:model` to `App\Livewire\Users\Index::$roleId` / `$status`, which were declared `public ?string $roleId = null;` and `public ?UserStatus $status = null;`.
- **What happened**: a real user filling in the create form picked "Administrator" for Role and "Active" for Status, saw both dropdowns displaying exactly those values, clicked Save — and got `The role id field is required.` and `The status field is required.`. The server had received neither value. It reproduced 100% of the time whenever the option the user picked happened to be the **first** non-placeholder option — which is precisely the one a user naturally reaches for — and not at all otherwise.
- **Root cause**: `null` is not an "empty value" to a native `<select>`. Livewire's `wire:model` (the Alpine `x-model` get/set installed by `js/directives/wire-model.js` in `vendor/livewire/livewire/dist/livewire.js`) assigns the property's dehydrated value straight onto the element's `.value`, and `HTMLSelectElement.value = null` coerces to the literal string `"null"`, which matches no `<option>` — including the `disabled selected` placeholder, whose value is `""`. Verified directly in a headless browser rather than reasoned about: that assignment moves `selectedIndex` from `0` (the placeholder) to `-1`, after which the real rendered page visually settled on the first non-placeholder option ("Administrator", first role alphabetically). Because the browser had **already** selected that option before the user ever touched the control, the user's click on that same option produced no value delta, so no native `change` event fired, so Livewire's `dataSet(component.$wire, 'roleId', ...)` never ran, and the property stayed `null` server-side while the UI showed the "right" answer. Nothing errored anywhere in the stack, which is exactly why nothing caught it: `Livewire::test()->set('roleId', ...)` writes the property directly and never touches a DOM element at all, and the browser-automation `selectOption()` API used during earlier manual checking does not reproduce the initial-value quirk either. Only genuine keyboard/mouse interaction against the real rendered DOM exposes it, so "the two selects render and can be picked" being eyeballed as working during review was consistent with the bug.
- **Fix applied**: both properties lost their nullability in [`app/Livewire/Users/Index.php`](../app/Livewire/Users/Index.php) — `public string $roleId = '';`, where the empty string is the "nothing chosen yet" sentinel and matches the placeholder `<option value="">` exactly, and `public UserStatus $status = UserStatus::Inactive;`, since a status enum has no empty-string equivalent and a user always has *some* real status (a new one defaults to the same value `users.status`'s own column default carries — see [database/schema.md](database/schema-users-auth.md#users)). `openEditModal()` now assigns `''` rather than `null` for a target with no role, and the Status `<flux:select>` in [`resources/views/livewire/users.blade.php`](../resources/views/livewire/users.blade.php) dropped its `:placeholder` prop, which had become unreachable markup. Both properties carry a docblock stating why they must never be null.
- **How to avoid it next time**: **a Livewire property bound with a bare `wire:model` to a native form control must never actually be `null` — give it a real "empty" value in the same type the DOM expects** (an empty string for a nullable string, a real default case for an enum), because the framework assigns the dehydrated value into `.value` and a JS `null` stringifies to `"null"`, desynchronising the browser's own selection state from what the server believes. Two corollaries, both of which are why this survived review: `Livewire::test()->set()` can never detect it (it bypasses the DOM entirely), and neither can a browser test driven through a `selectOption()`-style API — the failure mode is a **missing** `change` event, so it takes either a test that drives the control the way a person does and then asserts the *server-side* value, or a human clicking the first option.
- **Update — 2026-08-24 (task 0015): `$status` has since changed *type*, and the rule above is exactly what constrained how.** It is now `public string $status = UserStatus::Inactive->value;`, retyped because Livewire's `EnumSynth` hydrates a forged backing value through `$type::from($value)` **before** validation runs, so a typed enum property turned a tampered `status` into an unhandled `\ValueError` instead of a validation error (finding F8). The story's own first draft prescribed `public ?UserStatus $status` — which would have reopened this entry's bug — and that instruction was withdrawn in writing once this entry was re-read. The shipped form is non-nullable and defaults to a **real backing value**, never `''`: the rule holds, only the type moved. See [the deferred-findings entry at the top of this log](#a-deferred-storys-findings-were-claims-about-a-tree-that-no-longer-existed-and-one-of-them-would-have-reopened-a-bug-in-this-log--2026-08-23).

## `disabled:cursor-not-allowed` on a Flux button was never the cursor the user saw — 2026-08-16

- **Context**: the same follow-up work as the entry below — styling the Users list's newly disabled row actions so the pointer signals "not clickable".
- **What happened**: `disabled:cursor-not-allowed` placed directly on the disabled `<flux:button>` had no visible effect whatsoever; hovering it still showed the default arrow.
- **Root cause**: Flux's own button classes already include `disabled:opacity-75 dark:disabled:opacity-75 disabled:cursor-default disabled:pointer-events-none` (`vendor/livewire/flux/stubs/resources/views/flux/button/index.blade.php`, `livewire/flux` v2.15.0). `pointer-events: none` removes the disabled button from hit-testing entirely, so the browser never resolves the hovered element — and therefore never resolves the cursor — against it. Confirmed with `document.elementFromPoint()` at the button's real on-screen coordinates in a headless browser: the element actually returned is the wrapping `<ui-tooltip>`, not the `<button>` inside it. A cursor utility on an element the browser never hit-tests is dead CSS, and reading the compiled class list would not have shown that.
- **Fix applied**: the cursor class moved onto the wrapper — `<flux:tooltip :content="…" class="cursor-not-allowed!">` in [`resources/views/livewire/users.blade.php`](../resources/views/livewire/users.blade.php) — which forwards `{{ $attributes }}` onto the rendered `<ui-tooltip>` (`vendor/livewire/flux/stubs/resources/views/flux/tooltip/index.blade.php`), i.e. onto the element that actually receives the pointer. Enabled row actions keep `cursor-pointer!` on the button itself, which is hit-testable. The view carries a comment recording why.
- **How to avoid it next time**: **before styling a `disabled` element's cursor — or attaching any hover-dependent behaviour to it — check whether it or an ancestor carries `pointer-events-none`; a disabled Flux button always does.** If it does, the rule belongs on the nearest ancestor that still receives pointer events, and the way to identify that ancestor is `document.elementFromPoint(x, y)` at the element's real screen position, not reading the class list. The same reasoning applies to a native `title` attribute or a JS `mouseenter` handler on a disabled control: neither will ever fire.

## A conditionally-bound `flux:button` `tooltip` prop rendered an empty tooltip on every enabled row — 2026-08-16

- **Context**: adding the per-row disabled edit/delete actions to the Users list ([`resources/views/livewire/users.blade.php`](../resources/views/livewire/users.blade.php)), which needed an "Action not allowed" tooltip on the disabled branch only.
- **What happened**: the obvious form — `<flux:button :tooltip="$user['canEdit'] ? null : __('users.index.action_not_allowed')" />` — wrapped **every** row action, enabled ones included, in a `<ui-tooltip>` plus an empty `<div data-flux-tooltip-content></div>`, so hovering an enabled button popped a blank tooltip bubble. Confirmed by dumping the actually-rendered HTML, not inferred from the stub.
- **Root cause**: `livewire/blaze` (v1.0.12) is installed in this project, and it really does perform compile-time folding of the Flux Blade components that carry a `@blaze(...)` annotation — `vendor/livewire/flux/stubs/resources/views/flux/with-tooltip.blade.php` opens with `@blaze(fold: true, unsafe: ['tooltip:position', 'tooltip:kbd', 'tooltip'])`, and those annotations are **not** the no-ops they would be in a Blaze-less install. That stub decides whether to wrap the slot (`if ($tooltip): <flux:tooltip :content="$tooltip">`) from a value produced by `Flux::forwardedAttributes()` plus Blaze's compiled attribute handling, which treats the prop as **present whenever `tooltip=` / `:tooltip=` is written on the tag at all**, regardless of what the bound expression evaluates to at runtime. A second attempt — `@unless(…) tooltip="…" @endunless` inside the `<flux:button …>` attribute list — failed harder, with a PHP syntax error in the compiled view: Blaze parses a component tag's raw attribute list at compile time and does not tolerate a Blade directive splitting it mid-tag.
- **Fix applied**: stopped binding `tooltip` conditionally at all. The Blade source now branches the whole element with `@if ($user['canEdit']) … @else … @endif` and writes the wrapper itself — `<flux:tooltip :content="__('users.index.action_not_allowed')">` around the disabled button only — so the tooltip markup is **structurally absent** from the compiled template on the enabled branch rather than merely falsy at runtime. Both branches keep the same `data-test="edit-user-{id}"` / `data-test="delete-user-{id}"` hook, so browser tests are unaffected either way; see [api/routes.md](api/users-and-roles.md#usersindex--the-first-permission-gated-route). The view carries a comment recording why the straightforward binding is not used.
- **How to avoid it next time**: **in this codebase a Flux prop that controls whether a wrapping element renders — `tooltip` on `flux:button` is the known one — cannot be driven by a conditionally-`null` bound value, because Blaze's compiled output does not re-evaluate the prop's *presence* per render the way plain Blade would.** Express the condition in the template's structure (two branches, one that writes the attribute or wrapper and one that omits it entirely), never as `:prop="$cond ? … : null"`, and never by wrapping an attribute in a Blade directive inside the tag. And verify by rendering the page and reading the real HTML: a Blaze-folded component's behaviour cannot be predicted from the stub's own `@if` alone.

## Two agents dispatched in parallel both wrote to the same Blade view — 2026-08-16

- **Context**: story 0006 (Users list + create/edit modal UI), Phase 5 re-fix round. The orchestrator dispatched two subagents in the same tool-call batch: `frontend-expert` to fix finding F9 (a status-badge guard) in `resources/views/livewire/users.blade.php`, and `frontend-qa` to fix findings F8/F10 in `tests/Feature/Users/IndexRenderingTest.php`.
- **What happened**: the test-fixing agent's own verification step required a **temporary** edit to that same view — flipping `@if ($user['pendingEmail'])` to `@if (true)` to prove the assertion could genuinely fail, the "regression-proof" pattern this project already uses elsewhere (story 0006b injected a deliberate JS error the same way). Because the two agents ran concurrently with no coordination or locking, that mid-edit transient state showed up in the *other* agent's tool result. That agent could not explain a diff it had not made and briefly suspected an injected instruction. It was a false alarm — a benign race between two of the orchestrator's own subagents — but it cost real investigation time, and had both edits been persistent rather than one transient, it would have been a genuine last-write-wins lost-edit bug.
- **Root cause**: the two tasks were scoped by **file ownership at the instruction level** (one owns the view, one owns the test file), and that scoping looked disjoint. It was not, because it accounted only for what each agent's task was *about*, never for what each agent's **verification method** would touch. A test-fix agent that proves an assertion can fail by temporarily editing application code crosses into the other agent's file by construction — "different files" was never true once the verification steps are counted.
- **Fix applied**: no code-level fix was needed; `resources/views/livewire/users.blade.php` ended up correct. The confusion was resolved by the orchestrator discarding both concurrent agents' contaminated runs and performing one fresh, single, isolated full-suite verification itself — the same isolation requirement [contracts.md](contracts.md)'s Full Test Suite Gate Rule already imposes for closure evidence. The preventive rule was written up as the **Parallel Agent File-Ownership Rule** in [contracts.md](contracts.md#parallel-agent-file-ownership-rule).
- **How to avoid it next time**: **before dispatching two or more agents in parallel, enumerate every file each one might write — including files its verification or regression-proof steps touch transiently — not just its stated target.** If those sets intersect at all, either run the agents **sequentially** (one completes and reports before the next starts), or state the ownership boundary explicitly in *both* agents' instructions: which one may write the shared file, and that the other must only read it. A transient edit is not exempt — concurrency makes an intermediate state observable, and an observer who cannot account for it will (correctly) treat an unexplained edit as suspicious.

## Freeing a deleted user's email left their password-reset token live — 2026-08-14

- **Context**: task 0005 Phase 4 (`appsec-auditor`) auditing the shipped soft-delete implementation, whose whole point is that `App\Models\User::delete()` obfuscates `users.email` so the address becomes immediately reusable by a new account.
- **What happened**: the story carried a Definition-of-Done bullet asserting that `password_reset_tokens` was *inert* after the obfuscation. It was false, and the audit produced a working account-takeover path: delete a user who (or whose administrator invitation) had an outstanding reset token, register a new account on the freed address, then open the **old** user's reset link — it sets the **new** account's password, within the 60-minute token window from `config/auth.php`.
- **Root cause**: `password_reset_tokens` is keyed by the email **string** with no foreign key to `users`, and `DatabaseTokenRepository::exists()` matches a token by `where('email', $user->getEmailForPasswordReset())` — the string alone. Obfuscating `users.email` therefore moved the row's identity while leaving every artefact keyed by the *old* value pointing at whoever holds that value next. The false claim survived planning because the reasoning stopped at the `users` table: nothing there references the address once it is rewritten.
- **Fix applied**: `App\Models\User::delete()` now deletes any `password_reset_tokens` row bound to the account's real, pre-obfuscation address — captured with `getRawOriginal('email')` before the `forceFill()`, and matched with `whereIn` on both the raw and the lowercased spelling — inside the same transaction that obfuscates it (see [`app/Models/User.php`](../app/Models/User.php)). Re-audited and confirmed closed; the full rule, including why the case normalisation is explicit rather than left to the connection collation, is in [security/soft-delete-patterns.md](security/soft-delete-patterns.md#freeing-an-identifier-means-revoking-everything-keyed-by-its-string-not-just-the-row).
- **How to avoid it next time**: **freeing an identifier for reuse is never a single-table operation.** Before declaring an address (or username, or slug) released, ask "what else in this database joins on the *string* rather than on the row's key?" and invalidate all of it in the same transaction — here `password_reset_tokens`, which carries both password resets and administrator invitations; a future audit-log or notification-routing table keyed by address inherits the same obligation. Two corollaries: never let a case-insensitive collation be the thing that makes a revocation query match (it is a security control, and it must keep working on SQLite, Postgres, or a `_bin` collation), and drive the test for such a revocation through the real producer (`Password::broker()->createToken($user)`) rather than a hand-written fixture row, so it fails if the producer's key format and the revoker's ever drift apart.

## A doc's "this app has no X yet" claim outlived the X by two tasks — 2026-08-13

- **Context**: task 0004 Phase 5 (`code-reviewer`) reviewing the Users list/editor backend, which introduced `app/Policies/UserPolicy.php`.
- **What happened**: [`testing/backend/feature-integration-tests.md`](testing/backend/feature-integration-tests.md) carried a section headed "Authorization: today vs. once Policies exist" asserting two things that were false: "This app has no `app/Policies/` yet" (true only until this task) and that `spatie/laravel-permission` is "**not yet attached** to `App\Models\User`" — which had been false since **task 0002**, two tasks earlier. The second claim actively misled: it told a test author that role-based authorization was untestable when `HasRoles`, two seeded roles and a 38-permission catalog had been live for weeks. Grepping the tree during the fix turned up **two more copies of the same false claim**, in [`testing/qa/what-not-to-test.md`](testing/qa/what-not-to-test.md) and [`testing/qa/risk-based-testing.md`](testing/qa/risk-based-testing.md) — three independent statements of it, all in `docs/testing/`, none of which the task-0002 docs pass touched.
- **Root cause**: the claim was phrased as a **negative statement about the whole repo** ("there is no X"), inside documents nobody would think to open when adding X. The docs-sync pass after task 0002 followed the change→doc mapping table — `HasRoles` usage maps to `architecture/authorization.md`, which *was* updated correctly — and a mapping table can only route you to the docs that **describe** a change, never to the unrelated docs that happen to **assert the change hasn't happened**. Nothing failed and no test broke, so all three copies stayed reachable only by whoever happened to read those files.
- **Fix applied**: rewrote the section as "Authorization tests" in [`testing/backend/feature-integration-tests.md`](testing/backend/feature-integration-tests.md), stating the three real layers (route middleware, permissions, policies) with the rules that follow, and corrected the Authorization bullet in [`testing/qa/what-not-to-test.md`](testing/qa/what-not-to-test.md) and question 4 of [`testing/qa/risk-based-testing.md`](testing/qa/risk-based-testing.md).
- **How to avoid it next time**: **do not write a bare negative claim about the repo in a doc that does not own that subject** — say "authorization today is X (see [authorization.md])" rather than "there is no authorization yet", so the sentence goes stale into a *link* instead of into a lie. When a negative claim is genuinely the point (as in [api/routes.md](api/routes.md)'s "no REST API yet"), it belongs in the doc that owns the topic and must name what would replace it. And in every docs-sync pass, before relying on the change→doc mapping table alone, grep the whole tree for the thing you just introduced — `grep -rn "no app/Policies\|not yet\|does not exist yet\|todavía" docs/` — because the docs that need fixing are precisely the ones that never mentioned the feature by name.

## A redundant `users_uuid_unique` index survived the UUID primary-key conversion — 2026-08-12

- **Context**: task 0003 (`ai-spec/tasks/.../0003-users-status-and-email-verification-lifecycle.md`) investigating index cost before deciding whether `users.status` needed one.
- **What happened**: `php artisan db:table users` shows the `id` column covered **twice** — once by `primary`, and again by a leftover `users_uuid_unique` (`btree, unique`). Every insert therefore writes a wholly redundant `CHAR(36)` unique index.
- **Root cause**: the UUID conversion set (see [database/migrations.md](database/migrations.md#the-real-users-conversion-alteration-not-create_table)) created the transient column in `2026_07_22_100001_convert_id_to_uuid_in_users_table.php` as `$table->uuid('uuid')->unique()`, and `2026_07_22_100005_finalize_uuid_primary_key_on_users_table.php` renamed it to `id` and promoted it to PRIMARY **without dropping that unique index**. A rename carries indexes along with the column, and promoting a column to PRIMARY does not absorb or replace an existing unique index on it — so the two coexist silently. Nothing fails; it is pure write amplification, which is exactly why it survived review.
- **Fix applied**: dropped by task 0014 (`ai-spec/tasks/done/0014-drop-redundant-users-uuid-unique-index.md`) via [`database/migrations/2026_08_17_132646_drop_redundant_uuid_unique_index_from_users_table.php`](../database/migrations/2026_08_17_132646_drop_redundant_uuid_unique_index_from_users_table.php), whose `up()` drops `users_uuid_unique` by its explicit name (not the column-derived form, which would target a name that never existed) and whose `down()` restores it exactly. `users.id` now carries a single `PRIMARY` index — reverified with `php artisan db:table users`.
- **How to avoid it next time**: **in a multi-step column conversion, the finalize migration owns the cleanup of every index the intermediate steps created.** When a transient column is given `->unique()` only to guarantee uniqueness *during* the transition, the step that promotes it to PRIMARY must `dropUnique([...])` first. Verify with `php artisan db:table <table>` — not by reading the migrations — that the resulting index list is exactly what you intended; the migration diff will not show you an index that nobody removed.

## A test asserted against a fixture address that the local `.env` also pointed `SUPER_ADMIN_EMAIL` at — 2026-08-12

- **Context**: task 0003, running the full suite locally (`sail test`) against this repo's real developer `.env`.
- **What happened**: two tests in `tests/Feature/Seeders/DatabaseSeederTest.php` — "seeding a production/staging environment creates no `test@example.com` fixture user" — failed locally while passing in CI. A `test@example.com` row existed after seeding, but not from the fixture gate they were testing.
- **Root cause**: this repo's local `.env` sets `SUPER_ADMIN_EMAIL` to the same address as the fixture account. With that config ambient, `RolePermissionSeeder::bootstrapSuperAdmin()`'s **provision** branch created a `test@example.com` row through a completely different code path than the one under test. The tests read config from the developer's environment instead of pinning the value they depend on, so their result varied with `.env`.
- **Fix applied**: both tests now neutralize the ambient value first — `config(['auth.super_admin.email' => null])` — which makes `bootstrapSuperAdmin()` a documented no-op (`! filled($email)`) and leaves only `DatabaseSeeder`'s own environment gate under test. See the two commented calls in [`tests/Feature/Seeders/DatabaseSeederTest.php`](../tests/Feature/Seeders/DatabaseSeederTest.php).
- **How to avoid it next time**: **a test that depends on a config key must set that key, including setting it to `null`/empty when "unset" is the state it assumes.** Never rely on a key being absent from the environment the suite happens to run in — `.env` is developer-specific and CI's is not the same file. This applies to every seeder and bootstrap test that branches on config; see [testing/qa/risk-based-testing.md](testing/qa/risk-based-testing.md) for the wider "what varies between environments?" question.

## A row carrying the configured email was treated as proof of mailbox ownership — 2026-08-09

- **Context**: task 0002 (`ai-spec/tasks/.../0002-seed-roles-permissions-catalog.md`), bootstrapping the `Super Admin` role from `SUPER_ADMIN_EMAIL` in `database/seeders/RolePermissionSeeder.php`.
- **What happened**: the Phase 3 implementation looked up `User::where('email', $email)->first()` and granted `Super Admin` to whatever row came back (warning and skipping when nothing matched). `appsec-auditor` flagged it as a privilege-escalation path: a `users` row merely *carrying* an address proves nothing about who controls that mailbox.
- **Root cause**: leaving `SUPER_ADMIN_EMAIL` unset at first deploy is a documented, supported state, and self-registration is enabled — so during that window anyone can register the address the operator intends to use later. Worse, `App\Livewire\Settings\Profile::updateProfileInformation()` lets any signed-in user point their **existing** account at an arbitrary address: it nulls `email_verified_at` but writes the new address to `users.email` immediately, with no re-verification. Either way the squatter is handed Super Admin the moment an operator sets the config and reseeds.
- **Fix applied**: the lookup in [`database/seeders/RolePermissionSeeder.php`](../database/seeders/RolePermissionSeeder.php) now carries `whereNotNull('email_verified_at')`, so verification is part of the **match condition** rather than a check bolted on afterwards, and a fifth branch was added for the address being occupied by an *unverified* account: abort loudly (console error + `Log::warning`), grant the role to nobody, insert no second account, send no mail, and `return` rather than throw so the roles and the 38-permission catalog still commit. The full decision tree is documented in [architecture/authorization.md](architecture/authorization.md#super-admin-bootstrap); the general rule is in [security/seeder-safety.md](security/seeder-safety.md#a-matching-row-is-not-proof-of-ownership).
- **How to avoid it next time**: **an existing row with the right email is not proof of mailbox ownership.** Any code that grants privilege by matching a configured or user-supplied address must require proof of control — `whereNotNull('email_verified_at')` — as part of the query, not as a follow-up `if`. When neither granting (unproven ownership) nor creating (address taken) is safe, abort with a `return` and an operator-actionable message; never throw from inside a seeder transaction that also writes required application data, and never resolve the ambiguity on the seeder's own initiative by verifying, renaming, or deleting the occupying account.
- **Update — 2026-08-12 (task 0003): the second half of this vector is now closed at the source.** The profile screen no longer writes an unverified address into `users.email` at all: `App\Livewire\Settings\Profile::updateProfileInformation()` routes every email change through `App\Actions\Users\RequestEmailChange`, which parks the new address in `users.pending_email` and mails a signed, address-bound, 60-minute link to *that* address; only `App\Actions\Users\ConfirmEmailChange` — reached by using the link — writes `users.email` and sets `email_verified_at`. The `if ($user->isDirty('email')) { $user->email_verified_at = null; }` branch is gone. **The rule above does not relax.** `users.email` alone still is not proof of anything (an administrator may create a user with an unverified initial address); what task 0003 guarantees is narrower and is exactly what the seeder relies on: **`users.email` together with a non-null `email_verified_at`** means the address was proven, because no *change* to `users.email` can land without its own verification link being used. Keep the `whereNotNull('email_verified_at')` in the lookup. See [architecture/authentication.md](architecture/authentication.md#pending-email-changes).

## Gherkin scenarios written with a generic "I" and bundled multi-action steps — 2026-07-21

- **Context**: `product-owner` agent writing the first project-level PRD (`docs/PRD/PRD.md`), covering five epics with Gherkin scenarios per capability.
- **What happened**: several scenarios used a generic, roleless `Given I have permission to manage products` / `When I create, rename, and delete...` style — no named business-role actor, and multiple distinct actions (create + rename + delete) bundled into a single scenario instead of one scenario per action.
- **Root cause**: `docs/workflow.md`'s User Story template shows a bare `Given <context> / When <action> / Then <expected result>` Gherkin skeleton with no actor or single-action guidance. This project already had the right rules written down — rule 1 ("Imperative vs. declarative scenarios", which models good examples as third-person business-role actors like "a registered user", never "I") and rule 3 ("Single When per scenario") in [testing/frontend/gherkin-guidelines.md](testing/frontend/gherkin-guidelines.md#1-imperative-vs-declarative-scenarios) — but that file frames itself as a browser-test-translation guide, and nothing in `workflow.md` or the `product-owner` agent definition pointed there when writing PRD/User-Story-level Gherkin, so the existing rules weren't applied outside `tests/Browser/`.
- **Fix applied**: rewrote every scenario in `docs/PRD/PRD.md` to open with a named business-role actor (e.g. `Given a catalog administrator`, not `Given I ...`) and to cover exactly one action per scenario, splitting bundled CRUD scenarios into one scenario per operation (e.g. "Create a product category" / "Rename a product category" / "Delete a product category" instead of one scenario doing all three). Added a cross-reference from `docs/workflow.md`'s User Story template to `testing/frontend/gherkin-guidelines.md`.
- **How to avoid it next time**: [testing/frontend/gherkin-guidelines.md](testing/frontend/gherkin-guidelines.md)'s rules 1 and 3 apply to **every** Gherkin scenario written in this project, not just browser-test translations — PRDs (`docs/PRD/`) and per-task User Stories (`ai-spec/tasks/`) included. Before writing or reviewing any scenario: open with a named business-role actor (`a catalog administrator`, `a store customer`, never `I`), and if a scenario needs a second `When`, split it into two scenarios instead.


_Previously: 2026-09-04 — Story 0027 (Products list + editor UI), Phase 6 docs sync. No new entry — the log still has thirty-five — but the [`@disabled(...)`/`:disabled="..."` entry](errors-log.md#a-bare-disableddisabled-inside-a-fluxbutton-tags-attribute-list-corrupts-the-whole-compiled-view--2026-08-31) is **narrowed in place, with a dated correction block**, per this project's own audit-authored-page convention: this story's own `resources/views/livewire/products/editor.blade.php` uses the colon-bound `:disabled="..."` form the entry had named as broken, on two real `<flux:button>` tags, verified by `code-reviewer` to render correctly across the whole suite — and re-verified here by direct `Blade::render()` execution in both directions. Only the bare `@disabled(...)` **directive call** breaks; a `:prop="..."` colon-bound attribute expression is safe on a `<flux:*>` tag at any value, the same distinction [the "Two `@directive(...)` calls" entry](#two-directive-calls-in-one-blade-component-tags-attribute-string-silently-fail-to-compile--2026-08-26) already drew for `@js()`. The title and the original "What happened"/"Root cause" text are left as written, per convention, with the correction appended rather than the claim silently rewritten.

_Previously: 2026-09-01 — Story 0024 (Products — core CRUD backend). Grew the log from thirty-four entries to thirty-five with [a genuinely new angle on an existing family](errors-log.md#an-actions-own-parameter-default-reintroduced-the-omission-ambiguity-its-stricter-collaborator-was-built-to-close--2026-09-01): `CreateProduct`/`UpdateProduct` gave ordinary PHP defaults to two parameters they forward to `SyncProductGallery`, a collaborator whose own contract treats its input as the complete, authoritative gallery state — so an omitted argument silently detached every image and nulled the featured image, with no error (Phase 4 finding F-1), and the identical shape recurred on `UpdateProduct`'s `$description` parameter, whose `= null` default was safe on `CreateProduct` (a new product genuinely starts empty) but not on an update, where omission does not mean "clear it" (re-audit finding F-6). Filed as the "absence in a payload is not a decision" family arriving one layer lower than its first instance — in an action's own parameter defaults rather than in a payload a partially-visible UI form submits.

**Everything else from this story was evaluated against this file's own bar and deliberately not logged.** The Phase 2 split's **C-1** (a Three Amigos debate decision reversed after Phase 2 review found its supporting claim — "`CreateUser`/`UpdateUser` contain no `Gate` call" — was false, overruling a QA dissent that had been correct all along) is the same root-cause family already covered twice on this page — [a reviewer's "correction" replacing accurate prose with wrong prose, unverified](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) and [a deferred story's findings claiming a tree that no longer existed](#a-deferred-storys-findings-were-claims-about-a-tree-that-no-longer-existed-and-one-of-them-would-have-reopened-a-bug-in-this-log--2026-08-23) — both about an unverified claim about the code standing in for a fact until someone actually opened the file, and neither entry's rule needs restating for a third instance. **C-2/C-3/C-4** are bookkeeping corrections (a since-fixed CI gap, two dependencies that shipped, a translation precedent that already existed) with no mistake of this project's own to record. Phase 4's F-2 through F-5 produced mechanical security rules (an over-broad `23000` catch narrowed to MySQL error 1062; `SyncProductGallery`'s reachability pinned by a test) that this project's precedent keeps on the domain's own docs pages ([database/schema.md](database/schema-products.md#products), [architecture/authorization.md](architecture/authorization.md#productpolicy--the-sixth-policy-and-the-second-built-entirely-for-a-screen-that-does-not-exist-yet)) rather than duplicated here.

_Previously: 2026-08-31 — Story 0022 (Shared searchable, server-side-filtered multi-select component). Grew the log from thirty-two entries to thirty-four with two new entries, both found only through real-browser testing rather than the component-level Feature suite, which was green throughout. **[An absolutely-positioned element with no explicit `top` takes its static position from DOM order](errors-log.md#an-absolutely-positioned-element-with-no-explicit-top-takes-its-static-position-from-dom-order-and-can-silently-occlude-a-sibling--2026-08-31)**: the results dropdown, written before the chip row in source order, silently covered the chip area's remove controls the moment it had any rendered height, hanging a real Playwright click on its own actionability timeout with no error anywhere — found with `document.elementFromPoint()`, fixed by reordering the markup rather than adding a `top` value. **[A Livewire directive modifier's duration cannot be interpolated as part of a component tag's attribute NAME](errors-log.md#a-livewire-directive-modifiers-duration-cannot-be-interpolated-as-part-of-a-component-tags-attribute-name--2026-08-31)**: `wire:model.live.debounce.{{ $debounceMs }}ms` cannot compile because Blade's `ComponentTagCompiler` parses attribute *names* with a regex that excludes `{`/`}`/`$`, a different and previously-undocumented failure from the three attribute-*value* Flux/Blaze traps already in this log — caught at Phase 2 review before real code was written, and notable because the same shape had already been silently worked around twice before (`Gallery`'s hard-coded `300ms` literal, `WysiwygEditor`'s Alpine `setTimeout()`) with neither prior file stating why.

**Two other findings from this story were evaluated against this file's own bar** — "only entries that produced a lasting convention" — **and deliberately not logged as separate entries.** The D10 forced deviation from `flux:dropdown` to a hand-assembled `x-show`/`x-cloak`/`click.outside` popover (`ui-dropdown`'s `this.querySelector("button")` trigger requirement, unmet by a bare `flux:input`, throwing on every page load) is not a new root cause — it is this codebase's **second** confirmed instance of the exact popover substitution `wysiwyg-editor.blade.php`'s own D8 link popover already established for an identical reason, and is recorded as a ✅ confirmation on [conventions/base-standards.md](conventions/directory-structure.md#directory-structure) and [architecture/authorization.md](architecture/authorization.md) rather than as a third narrative incident. And the seven Phase 4 security findings closed in this story (`#[Locked]` gaps on six consumer-set-once props, plus F-2's shared-resolution-method fix) are exactly the kind of mechanical security rule this project's precedent keeps on the security pages — [security/livewire-authorization.md](security/livewire-authorization.md) — rather than duplicating here._

_Previously: 2026-08-31 — Story 0021 (Shared WYSIWYG rich-text editor component — frontend), Phase 6. Grew the log from thirty-one to thirty-two entries with the **third** documented Flux/Blaze markup trap sharing one root cause with the two already below it: a bare `@disabled(...)`/`:disabled="..."` written directly inside a `<flux:button>` tag's attribute list does not compile the way it would on a plain HTML tag — `livewire/blaze`'s compile-time folding corrupts the whole attribute string into invalid PHP, breaking the entire compiled view rather than merely that button's disabled state (`code-reviewer`'s Phase 5 finding N4, reproduced via direct `Blade::render()` execution). Fixed by writing every one of the toolbar's eight controls as a full `@if`/`@else` pair of separately-compiled tags rather than one tag carrying a conditional `disabled` attribute — the same rule the tooltip-presence and `cursor-not-allowed!` entries below already establish for other props on a Flux component tag, now with a third instance to point at.

**Three other findings from this story were evaluated against this file's own bar — "only entries that produced a lasting convention" — and deliberately not logged as separate entries**, per the precedent this log already sets for judgement calls handled inside the phase that produced them. **(a)** Phase 4's caret-capture re-entrancy fix (B1: `saveCaret()` captured the document-global `Selection` with no check that it belonged to the editor instance capturing it) is a component-specific bug with a component-specific regression test, not a mechanical rule with a second application anywhere else in this codebase yet. **(b)** Phase 5's B3 root-cause (a stale `public/build/` bundle silently exercising pre-fix JavaScript across an entire review round, discovered only because the reviewer measured a doubled flake rate and refused to accept it as noise) is the same failure class as [the stale-`public/hot`-file entry](errors-log.md#a-stale-publichot-file-from-an-old-npm-run-dev-session-made-all-19-browser-tests-time-out-misread-as-real-ui-bugs--2026-08-28) two entries below — a stale build artifact masquerading as a code regression — and adds no new mechanism beyond "check for a stale build before trusting a browser-test failure count," which that entry already states. **(c)** The two environment findings F5/F6 from that same Phase 5 round (a host-native `php artisan test` fataling at PHP CLI's default 128M memory limit; Playwright's orphaned `run-server` processes re-accumulating on every browser-test run rather than only under the `->waitForEvent('networkidle')` incident already logged) are process/tooling documentation gaps rather than mistakes with a root cause to record here — both are now addressed directly in [testing/ci/commands.md](testing/ci/commands.md#environment-note-php-memory-limits-phpstan-and-php-artisan-test) and [testing/frontend/playwright-setup.md](testing/frontend/playwright-setup.md#orphaned-playwright-processes-re-accumulate-on-every-browser-test-run-in-this-environment), which is where a reader hits them, rather than duplicated into a narrative incident here._

_Previously: 2026-08-29 — Story 0020 (Shared media gallery modal — frontend), Phase 6. Grew the log from thirty to thirty-one entries with **a docs pass that reported a gap, got it fixed, and shipped its own report of the gap** — story 0019's Phase 6 wrote that `LogRefusedPrivilegedAttempt` "appears nowhere" under `app/{Livewire,Actions}/Media/`, *"verified by grep … not assumed"*, into two pages, while the code it describes imports the class and calls it twice and carries comments naming that very docs finding as the reason. Recorded as its own entry rather than folded into [the 2026-08-20 audit-authored-page entry](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) it obviously resembles, because two of its three causes are new: the fix was **rebased into the earlier commit**, so `git log` order actively argues the doc is the later word (systematically misleading on any rebasing workflow), and the "verified by grep" hedge made the false claim *more* trusted rather than less — the inverse of [the 2026-08-24 hedge entry](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24), where the hedge was the tell.

**Three other candidates from this story were evaluated and deliberately not logged**, per this log's stated bar of *"only entries that produced a lasting convention"* and the precedents in [README.md](README.md)'s narrative history for what has been judged out. **(a)** Phase 4's F-1 (a routeless component gating only `mount()`, leaving `tiles()`/`toggleSelect()`/`confirmSelection()` disclosing the library after a mid-session revocation) is a mechanical security rule, which this project's precedent keeps on the security page — [security/livewire-authorization.md](security/livewire-authorization.md#the-routeless-case-a-component-with-no-route-has-no-per-request-backstop-at-all), with its architectural placement on [architecture/authorization.md](architecture/authorization.md#a-routeless-livewire-component-has-no-per-request-authorization-backstop). **(b)** The `->wait(5)`/`Playwright::$timeout` finding and the two file-upload impossibilities are browser-testing **environment** rules, which is exactly where the `->waitForEvent('networkidle')` incident was filed despite costing far more (15-minute hangs, ~60 leaked processes, an OOM-killed database container) — they are now in [testing/frontend/playwright-setup.md](testing/frontend/playwright-setup.md#a-bare-waitn-is-not-a-polling-primitive-and-a-longer-one-can-fail-because-it-is-longer). **(c)** The mid-story scope change (D8/D9 deferred, then implemented in full after a Phase 5 review, at the cost of two extra Phase 4 rounds and a third Phase 5 round) was caught by the phase designed to catch it, produced no convention beyond "a deferral is not a deferral until the task file records it", and is the same judgement call as story 0015b's Phase 3 loop bound — recorded in the story file, not here.

_Previously: 2026-09-11 — Doc growth management pass, continued: received the twelve entries dated 2026-08-20 through 2026-08-26, moved here byte-for-byte and unedited from [errors-log.md](errors-log.md) (a move that file's own footer records the reasoning and verification for). Grew this file from fourteen entries to twenty-six; the date range this file covers widened from "2026-07-21 through 2026-08-19" to "2026-07-21 through 2026-08-26". Both files' "Browse by topic" indices were updated in both directions — the twelve topics' entries here dropped their `errors-log.md#... (main log)` form for a bare local anchor, and the reciprocal entries in the main log gained the `errors-log-archive.md#... (archive)` form._

_Previously: 2026-08-28 — Test-suite parallelization (making `--parallel` fast and reliable, then enabling it in CI): added a dated correction to the "full test suite degrades under repeated `--parallel` runs" entry, closing it from an open observation to a confirmed, fixed root cause — a small, deterministic reproduction (`tests/Feature/Auth`, 4 processes, the exact same 9 tests failing every run) disproved the entry's own leading hypothesis (per-token `view.compiled` isolation, implemented and tested with no effect) and found the real cause: `storage/framework/views` sitting inside the project's bind-mounted volume, where concurrent `tempnam()`/`rename()` writes through Sail's WSL2↔Docker bind mount are unreliable. Fixed with a named Docker volume (`compose.yaml`) plus a container-start `chown` (`docker/8.5/start-container`) and a per-`ParallelTesting`-token subdirectory (`AppServiceProvider::configureParallelTesting()`) nested inside it. Verified stable: 950/950 twice in a row under `--parallel --processes=8` (~128–131s vs. a ~338s sequential baseline, a real ~2.6x). No new entry added — this is a closure of an existing one, per this log's own rule that a corrected mechanism gets a dated correction block, not a duplicate entry._

_Previously: 2026-08-28 — Tasks 0019a/0019b, then a full-workflow follow-up pass (`appsec-auditor` + `code-reviewer` dispatched for the Phase 4/5 these tasks initially skipped) and a full-suite closure check: grew the log from twenty-six entries to thirty. The Phase 4/5 pass found a real boundary regression in 0019a's own fix — `RESOURCETYPE_AREA` set to exactly `MAX_DIMENSION^2` refused a legitimate `MAX_DIMENSION x MAX_DIMENSION` upload, because ImageMagick's AREA check is a strict `<` rather than inclusive like `WIDTH`/`HEIGHT` — found independently by both agents and closed with a `+ 1` fix plus a new regression test; three other non-blocking findings (a test assertion coupled to one host's exact policy value, a stale entry count in `docs/README.md`, an unre-measured HDRI/non-HDRI basis note) were closed in the same pass. A fourth entry, found during the closure suite run itself, records a stale `public/hot` file (left by an unclean `npm run dev` exit) that made all 19 browser tests time out with no code defect at all — the visual tell (a screenshot showing completely unstyled markup) and the one-command fix (`rm public/hot`) are recorded so the next bulk browser-test timeout is diagnosed in seconds rather than debugged test-by-test. The full suite closed this pass at **950/950**, zero failures. The three entries added by the *first* 0019a/0019b pass remain as originally recorded below (an Imagick resource limit requested in the wrong unit; a self-inflicted false lead from checking a container's user via the wrong `sail` invocation; an open, unresolved observation about full-suite `--parallel` runs degrading under sustained session load) — only the AREA entry's underlying code changed further (the `+ 1`), and its entry text is left as-is since it already correctly identified the unit mismatch as the root cause; the boundary refinement is recorded in the task files rather than duplicated here.

_Previously: 2026-08-26 — Task 0018 (Sales Regions & Taxes screen — UI), Phase 6. The story's own entry — two `@directive(...)` calls in one Blade component-tag attribute — was committed mid-story and **is what this pass corrected**, which is the point worth recording: it grew the log from twenty-five entries to twenty-six, and the footer was never updated to say so, so the count in [docs/README.md](README.md) had also gone stale. **The entry's root cause did not survive verification.** Carrying it forward meant re-checking it against `HEAD`, per this log's own [audit-authored-page rule](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20), and the stated rule — *one `@js()` in a component-tag attribute compiles, two do not* — reproduces in **neither** direction: executed through `Blade::render()` **and** through the real file-view compilation path, the exact shipped two-`@js()` `<flux:switch>` expression compiles correctly, while an anonymous `<x-…>` component leaves `@js()` uncompiled at **any** count, including one. The count was never the discriminator; the component is. The tell was in the entry itself — it needed a carve-out for this repo's own two-`@js()` `x-show`, and that carve-out was a **hedge** rather than a mechanism, which is precisely what [the 2026-08-24 entry](#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) says to treat as "nobody ran this". Added a dated correction block with the executed results as a table, the one half that *is* now verified (`@js()` in an anonymous `<x-…>` attribute is broken outright), and an explicit refusal to guess at what actually killed the shipped screen — the broken state was never committed, so it cannot be reconstructed, and replacing one confident wrong mechanism with a second would repeat the mistake being corrected. The incident, the symptom, the fix and the safe rule are untouched. **No new entry was added by this pass** — the story's remaining findings are covered elsewhere by this project's precedent: its Phase 4 F-1 (a deactivated parent dropping its children group from the screen) is a product bug fixed within its own story with a blanket regression test, its F-6 is a **confirmed-safe** verdict living on [security/livewire-authorization.md](security/livewire-authorization.md), and its `->waitForEvent('networkidle')` hang and bounded-`->wait()` mitigation are browser-testing environment rules, now owned by [testing/frontend/playwright-setup.md](testing/frontend/playwright-setup.md#waiting-one-call-is-banned-in-this-repo-and-one-is-bounded)._

_Previously: 2026-08-26 — Task 0017 (Sales Region tax configuration — backend): added the entry for three consecutive verification records that named two of this project's three quality gates and were read as records of all three — Larastan level 7 was never run against the story until Phase 5's code review ran it and found two real errors in the component's own `save()`. Grew the log from twenty-three entries to twenty-four. Recorded as its **own** entry rather than folded into the 2026-08-20 scoped-gates entry directly below it, because the two failure modes are opposites: that one is a gate that **ran** and reported "pass" while inspecting almost nothing, this one is a gate that **never ran**, which no amount of unscoping the other two would have caught. The generalisable half is not about these three commands at all — **an omission is invisible in a record of positives**, so a checklist has to be enumerated by the checklist rather than reconstructed from the report. **The story's other findings are deliberately not logged here.** Its two Phase 4 Mediums (a caller-supplied model instance being untrusted on both the read and the write side) produced mechanical security rules, which this project's precedent keeps on [security/model-instance-trust.md](security/model-instance-trust.md) rather than duplicating; its Phase 4 re-audit round 2 (R-1's fix-introduced deadlock, R-2's lying return value) is the "[audit the remediation as new code](#two-of-the-three-security-audit-rounds-found-the-flaw-in-the-previous-rounds-fix--2026-08-19)" rule working exactly as already recorded, and R-4 is the "[a re-raised instruction contradicting an existing decision is withdrawn in writing](#a-deferred-storys-findings-were-claims-about-a-tree-that-no-longer-existed-and-one-of-them-would-have-reopened-a-bug-in-this-log--2026-08-23)" rule doing the same; and Phase 5's F-2 (a security page's own claim going stale one day after it was written) is [the audit-authored-page entry](#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20)'s failure mode recurring with its prescribed fix applied, not a new lesson._

_Previously: 2026-08-26 — Infrastructure incident, no story: added the entry for a missing `.env.testing` letting `--env=testing` silently fall back to the real `.env` and wipe the shared `arospe` development database via `migrate:fresh`, twice in one day, across two separate sessions. Grew the log from twenty-three entries to twenty-four. Fixed by creating a per-checkout `.env.testing` and documenting the further per-worktree database-naming requirement in the new [testing/worktree-databases.md](testing/worktree-databases.md)._

_Previously: 2026-08-24 — Task 0015b: added the entry for a scope exclusion phrased in terms of **screens** while the story's unit of change was a **class** those screens share — `App\Actions\Users\RequestEmailChange` is called by both the in-scope admin screen and the explicitly-excluded `settings/profile`, so instrumenting it handed every authenticated user an unbounded log-write primitive (Phase 4 finding F-1, Medium). Grew the log from twenty-two entries to twenty-three. **One candidate was evaluated and deliberately not added:** the off-by-one loop bound `backend-qa` found and fixed in their own new test during Phase 3 (a sibling test's "N-1 successes then the Nth refused" shape copy-adapted without re-deriving it against a different `maxAttempts`). It was caught and closed inside the phase that produced it, appears nowhere in the task file's own record, and yields nothing beyond "re-derive a copied loop bound against its new constant" — below this log's stated bar of *"only entries that produced a lasting convention"*. **The story's other findings are cross-referenced rather than re-logged**, per this project's precedent: F-3 (two message strings, so "a single filter covers all of them" was false) is a documentation correction now carried as a ⚠️ in [architecture/authorization.md](architecture/authorization.md#-a-refusal-on-these-screens-produces-one-of-two-message-strings), and the Phase 4 re-audit's L-1/L-2 and Phase 5's R-1/R-2/R-3 are the "[audit the remediation as new code](#two-of-the-three-security-audit-rounds-found-the-flaw-in-the-previous-rounds-fix--2026-08-19)" rule working as already recorded, not a new lesson._

_Previously: 2026-08-24 — Task 0015a, Phase 5 re-review: added the entry for a code review's own "correction" replacing an accurate docblock explanation with a wrong one — a hedged finding ("likely wrong") acted on without verification, then verified and reverted by a later review pass. Grew the log from twenty-one entries to twenty-two._

_Previously: 2026-08-24 — Task 0015: added the entry for a deferred story whose findings had become claims about a tree that no longer existed — six of eleven wrong after five intervening stories, one of them (F8) prescribing a retype that would have **reintroduced a bug already recorded in this log**. Grew the log from twenty entries to twenty-one. Deliberately recorded as its own entry rather than folded into the doc/code-drift family below: those are about **documentation** going stale, this is about a **backlog artifact** going stale, which behaves differently — a task file is read as an instruction rather than as a description, so a stale one produces work instead of confusion, and its "already fixed" findings fail silently on implementation. **One candidate was evaluated and deliberately not added:** the Phase 4 re-audit's **L-1** (the F-A fix exempted a self-service caller from the aggregate throttle, removing a backstop with no test proving the surviving composite limiter still bound that caller, under a comment that falsely claimed coverage lived in `EmailChangeTest.php`). It is a restatement of [the 2026-08-19 entry](#two-of-the-three-security-audit-rounds-found-the-flaw-in-the-previous-rounds-fix--2026-08-19) and [the 2026-08-20 one](#a-guard-took-the-state-it-was-guarding-as-a-parameter-reopening-its-own-hole-one-level-up--2026-08-20) — the re-audit caught it by doing exactly what those entries prescribe — so per this project's precedent it is cross-referenced rather than duplicated; its mechanical half (an exemption that removes a backstop needs a dedicated test for the surviving control) is recorded on [security/authorization-patterns.md](security/authorization-patterns.md#a-rate-limit-keyed-on-the-target-alone-becomes-an-attack-on-the-target-the-moment-a-second-caller-exists) instead. The story's F10 ordering hazard is likewise a *forward-looking* application of the `DB::transaction()` entry below, cited in the shipped code's own comment rather than re-logged._

_Previously: 2026-08-21 — Task 0012: added the entry for a `DB::transaction()` wrapper that relocated a cache flush **nobody had written** — story 0010 wrapped `saveRole()` / `deleteRole()` for an unrelated atomicity finding, pulling the vendor's own post-`syncPermissions()` / post-`deleted`-event permission-cache flush inside the transaction, with no flush line appearing anywhere in the diff. Recorded as its own entry rather than folded into the two "audit the remediation as new code" entries below, because the distinguishing property is that **the diff hides the change**: those entries are about a fix introducing a new flaw a reviewer could read, this one is about a fix whose effect is invisible at the call site. It is also the third direction of this log's doc/code drift family — the rule broken here was already documented, correctly, on a page the breaking story cited. Grew the log from nineteen entries to twenty._

_Previously: 2026-08-21 — Task 0011: added the entry for a count-based assertion over rendered HTML that counted a wrapper element it never meant to include — `substr_count($html, 'value="')` unscoped (Phase 5 finding F-4), and the corrected `<ui-checkbox` prefix silently absorbing `flux:checkbox.group`'s own `<ui-checkbox-group>` wrapper. Recorded as its own entry rather than folded into the two 2026-08-16 Flux/Blaze entries, because those are about a component's **rendering** behaviour while this is about a **test assertion** designed from Blade source instead of real output — and because the failure mode is specific to counting: an over-count by a constant reads as the true number. Grew the log from eighteen entries to nineteen._

_Previously: 2026-08-20 — Task 0010: added the entry for this project's two per-change quality gates both being scoped by default and both silently reporting success — `pint --dirty` no-ops once a tree is committed (Phase 5 blocking finding F-1), and `test --filter` never observed that this story's model-event guard had broken an unrelated pre-existing test. Recorded as **one** entry rather than two because the root cause is identical (a narrowed gate reports "pass", not "not checked") and the rule is one rule: run each gate unscoped before declaring the work done. Deliberately framed as a project-wide gate weakness rather than this story's own mistake; the ambient-`SUPER_ADMIN_EMAIL` half of the test failure is cross-referenced to the 2026-08-12 entry rather than restated. The matching correction to [conventions/base-standards.md](conventions/base-standards.md#quality-gates)'s gate list landed in the same pass. Grew the log from seventeen entries to eighteen._

_Previously: 2026-08-20 — Task 0040: added the entry for the planned `verified`-middleware test that could carry no signal, because `App\Models\User` does not implement `MustVerifyEmail` and `EnsureEmailIsVerified` therefore refuses nobody on any route in this app. Grew the log from sixteen entries to seventeen. The fact itself is not new and is not restated here — [architecture/authentication.md](architecture/authentication.md#account-status-and-activation) has owned it since task 0007; what this entry records is that a test case was designed from a middleware's name and a route group's appearance without opening the doc that already answered the question._

_Previously: 2026-08-20 — Task 0009: added the entry for a guard that accepted, as a caller-supplied parameter, the very state it existed to protect — the fix for one finding reopening the same class of hole one level up, across three consecutive Phase 4 rounds — together with the full-replace-`sync*()` omission trap that started it (absence in a partially-visible form is not a decision) and the human product decision its resolution required. Deliberately does **not** restate the "audit the remediation as new code" rule from the 2026-08-19 entry below; it points there and records that two consecutive stories have now needed three rounds. The story's mechanical rules live in [security/authorization-patterns.md](security/authorization-patterns.md)._

_Previously: 2026-08-20 — Task 0016: added the entry for a security page that documented the vulnerable code as current — `security/seeder-safety.md`'s "fail loudly" section was authored during the Phase 4 audit that found the two fail-open lookups and never revisited after the same story fixed them, so it shipped quoting code that no longer existed (Phase 5 finding F-1). The rules: treat an audit-authored page as unsynced by default and re-verify its code quotes at Phase 6, and write a vulnerability up as a ❌/✅ pair from the start. It is the bare-negative-claim entry's failure class from the opposite direction — there a doc outlived the feature it denied, here a doc outlived the bug it described._

_Previously: 2026-08-19 — Task 0008a: added the entry for the three-round Phase 4 audit whose second round found two Mediums in the first round's own fix (N1, a `Gate::authorize()` sitting above the relation reload it depended on; N2, a Super Admin refusal that checked the submitted role but never the target's current one), with the rules that follow — re-audit a fix as new code, enumerate every privileged value a relocated guard's new owner can write, and name the statement that last wrote the state each check reads. The story's two mechanical rules live in [security/authorization-patterns.md](security/authorization-patterns.md); this entry owns the process lesson and is not a duplicate of them._

_Previously: 2026-08-18 — Task 0008: added the entry for the vacuous Pest `arch()` rule — `expect(['App', 'Database\Seeders'])` evaluates disjunctively, so the test enforcing this story's central "one role model class" invariant shipped green while structurally unable to fail on half its stated scope. The story's other notable mistake (a rename bypass that survived its own first fix, Phase 4 finding R1) is owned by [security/authorization-patterns.md](security/authorization-patterns.md#a-guard-that-reads-a-rows-protected-identity-must-distinguish-not-hydrated-from-hydrated-but-null), which already cross-references the `getOriginal()` entry below as the same trap from a different direction — not duplicated here._

_Previously: 2026-08-17 — Task 0014: updated the "A redundant `users_uuid_unique` index survived the UUID primary-key conversion" entry's "Fix applied" line — no longer "not fixed"; the follow-up task it explicitly asked for has landed, dropping the index via a new migration with an exact-inverse `down()`._

_Previously: 2026-08-17 — Task 0007: added the entry for `ActivateVerifiedUser` reading the pre-save `email_verified_at` with `getOriginal()` (which `save()`'s own `syncOriginal()` had already overwritten) and for its `Inactive`-only guard covering `Suspended` while treating an administrator's *deactivation* as a trigger — with the two rules that follow: `getPrevious()` is the post-`save()` pre-change value, and a column that starts denying access must have every write to it re-audited as a privilege grant._

_Previously: 2026-08-17 — Added the entry for a task file's relative links breaking silently when it moves from `./ai-spec/tasks/` to `in-progress/`/`done/` (a directory-depth change from two to three levels below the repo root); found and fixed the real break across six already-`done` task files, and cross-referenced the new mandatory check `docs/workflow.md` now runs on every stage move._

_Previously: 2026-08-16 — Task 0006 follow-up: added three entries from post-closure work on the same screen — the `null`-valued Livewire property that desynchronised a native `<select>` and silently dropped the user's own pick, and the two Flux/Blaze rendering traps found while building the per-row disabled actions (a `tooltip` prop that cannot be bound conditionally, and a `disabled:cursor-*` utility on an element `pointer-events-none` removes from hit-testing)._

_Previously: 2026-08-16 — Task 0006: added the entry for the two subagents dispatched in parallel that both wrote to `resources/views/livewire/users.blade.php` (one persistently, one transiently as a regression-proof), with the rule that parallel dispatch must account for every file an agent's *verification* steps touch, not only its stated target._

_Previously: 2026-08-14 — Task 0005: added the entry for the recycled-email account-takeover path found in Phase 4 (a freed address kept its `password_reset_tokens` row live), with the rule that freeing an identifier must revoke everything keyed by its string value in the same transaction._
