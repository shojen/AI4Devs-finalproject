# Database Schema — Orders

Part of [Database Schema](schema.md) — see [schema.md](schema.md#er-diagram) for the full ER diagram and [schema.md#notes](schema.md#notes) for the UUID/ADR-0001 status notes. This file covers `orders` and `order_items` (story 0045) plus `refunds` (story 0051, PRD [§3.2 Orders](../PRD/PRD.md#32-orders)). `orders`/`order_items` are the first pair in this codebase whose *only* reason to ship together is a single cross-table invariant: an order is a customer plus one or more priced line items, and every price is frozen at the moment of ordering. Split into its own file rather than appended to [schema-other.md](schema-other.md) because the invariant needs enough prose (the price snapshot, the address snapshot, the three-way delete-behaviour split, a security fix on the write path) to be worth a dedicated page, per [contracts.md](../contracts.md#doc-growth-management-rule)'s doc growth management rule — and because Epic 3's remaining Orders stories (0046–0055) will all extend this same domain rather than `payment_methods`/`customers`/`notifications`.

## Table of Contents

- [`orders`](#orders)
- [`order_items`](#order_items)
- [`refunds`](#refunds)
- [The price snapshot — read from the variant, never the parent, when one is named](#the-price-snapshot--read-from-the-variant-never-the-parent-when-one-is-named)
- [Totals are derived and re-derived, not write-once](#totals-are-derived-and-re-derived-not-write-once)
- [The address snapshot](#the-address-snapshot)
- [`order_number` generation](#order_number-generation)
- [Delete behaviour — the same three-way rule stated once](#delete-behaviour--the-same-three-way-rule-stated-once)
- [Nothing is resolved that this story does not own](#nothing-is-resolved-that-this-story-does-not-own)
- [Authorization](#authorization)

### `orders`

Source: `database/migrations/2026_09_11_162629_create_orders_table.php` (story 0045) — a greenfield UUID `create_*` migration following [`create_sales_regions_table`](migrations.md#uuid-primary-keys)'s established pattern: `uuid('id')->primary()`, every string column length-capped, every money-like column `decimal`, and no hand-written index on any column `constrained()` already indexes.

Model: [`App\Models\Order`](../../app/Models/Order.php). Columns in real physical order (verified with `php artisan db:table orders` against a live, migrated instance):

| Column | Type | Notes |
| --- | --- | --- |
| `id` | uuid (v7) PK | `CHAR(36)`; generated by `HasUuids`. A plain UUIDv7 business entity under [ADR 0001 Amendment 1](../decisions/0001-uuid-primary-keys.md#amendment-1-2026-08-27--the-scope-is-the-policy-not-the-list-of-seven)'s general policy — Orders is not one of the ADR's original seven named entities, and needs no further amendment |
| `order_number` | `VARCHAR(20)`, **unique**, **not** mass-assignable | a support-usable reference distinct from the UUID (**D-1**) — see [`order_number` generation](#order_number-generation) below |
| `customer_id` | `CHAR(36)` FK → `customers.id`, `restrictOnDelete()` | required; the customer this order belongs to |
| `status` | `VARCHAR(20)`, default `'pending'` | cast to [`App\Enums\OrderStatus`](../../app/Enums/OrderStatus.php). No index — the same low-cardinality-token cardinality argument [`users.status`](schema-users-auth.md#users) and [`sales_regions.kind`](schema-products.md#sales_regions) already establish |
| `payment_status` | `VARCHAR(20)`, default `'pending_payment'` | cast to [`App\Enums\PaymentStatus`](../../app/Enums/PaymentStatus.php). Same no-index reasoning as `status` |
| `sales_region_id` | `CHAR(36)` FK → `sales_regions.id`, nullable, `restrictOnDelete()` | `NULL` at creation, always — resolving it is stories 0053/0054's job, not this one's (**D-9**). Written once by `ResolveOrderTaxRegion` (story 0053, physical orders, from the order's own frozen shipping address, never the customer's live one) or `ResolveVirtualOrderSalesRegion` (story 0054, virtual orders, from the order's own frozen **billing** address instead); a resolved order is never re-resolved (0053 **D-10**) |
| `shipping_rate_id` | `CHAR(36)` FK → `shipping_rates.id`, nullable, `restrictOnDelete()` | `NULL` at creation, always — resolving it is stories 0037/0054's job (**D-9**) |
| `payment_method_id` | `CHAR(36)` FK → `payment_methods.id`, **not** nullable, `restrictOnDelete()` | required — PRD §3.2 requires every order to reference a configured payment method |
| `tax_rate` | `DECIMAL(6,3)`, nullable | a snapshot mirroring [`sales_regions.rate`](schema-products.md#sales_regions)'s own precision — copied once at resolution by `ResolveOrderTaxRegion` (story 0053) or `ResolveVirtualOrderSalesRegion` (story 0054), never a live join, so a later rate change never re-taxes a placed order. `NULL` at creation, **never** `0.000` — "not configured" and "a legitimate 0%" must not share a representation, the identical rule `sales_regions.rate` already establishes |
| `subtotal` | `DECIMAL(10,2)` | derived from the line items at creation (**D-8**), and **re-derived** — not write-once — on every line-item add/remove/quantity edit since story 0048; see [Totals are derived and re-derived, not write-once](#totals-are-derived-and-re-derived-not-write-once) below |
| `tax_amount` | `DECIMAL(10,2)` | `0.00` at creation. **Derived, not write-once:** `ResolveOrderTaxRegion`/`ResolveVirtualOrderSalesRegion` (stories 0053/0054) set it to `subtotal × (tax_rate ÷ 100)` (rounded half-up, `0.00` when `tax_rate` is `NULL`, via the one shared `App\Actions\Orders\CalculateTaxAmount`) in the same write as `sales_region_id`/`tax_rate`/`total`, and it is re-derived alongside `subtotal` on every line-item edit since story 0048, only when `tax_rate` is already resolved (D-8's own conditional, unchanged) |
| `shipping_amount` | `DECIMAL(10,2)` | `0.00` at creation, always — shipping selection is stories 0037/0054's. Read and summed, never itself recomputed, by story 0048's re-derivation |
| `total` | `DECIMAL(10,2)` | `subtotal + tax_amount + shipping_amount`, written out as that arithmetic identity rather than as a copy of `subtotal`, so it stays true unchanged once a later story populates the other two terms (**D-8**). Re-derived alongside `subtotal`/`tax_amount` since story 0048 |
| `refunded_amount` | `DECIMAL(10,2)`, default `0.00` | story 0051 — the running total `App\Actions\Orders\RecordRefund` keeps consistent with the SUM of this order's `refunds` rows' `amount`; a **merchandise-only** total (`quantity × unit_price`), excluding tax and shipping, which are both `0.00` on every order today (R-2) — see [`refunds`](#refunds) below. No backfill on the adding migration: no refund mechanism existed before this story, so `0.00` is the true value for every pre-existing row |
| `flagged_for_review` | `BOOLEAN`, default `false` | the geo/fraud review flag PRD §3.2 defines for tax resolution (**D-10**); `ResolveOrderTaxRegion` (story 0053, physical) sets it `true` for the ambiguity cases: an inactive/absent/unmatched region, a missing or unmapped Spanish postal code, a resolved region with no rate, or a mixed/unresolvable-product order. `ResolveVirtualOrderSalesRegion` (story 0054, virtual) sets it `true` for a mixed/unresolvable-product order too, and for its own geo/fraud check — see `ip_address`/`ip_derived_country`/`flag_reason` below. On a freshly seeded catalog every non-Spanish physical order is therefore flagged (only Spain is active); a virtual order is never flagged for that reason, since 0054 falls through to the catalog default instead (its own **D-3**) |
| `ip_address` | `VARCHAR(45)`, nullable | story 0054 — the purchaser's IP address, as captured by whatever channel created the order. **`NULL` on every row today**, and means "no IP was captured," never "an IP was captured and was empty": this application has no storefront or checkout, so nothing in the request that creates an order currently carries one. 45 characters is the IPv6-safe maximum (an IPv4-mapped IPv6 address is 45 characters). Read but not itself compared — the geo/fraud check below keys on `ip_derived_country`, never on this column directly, so a channel that starts recording addresses without deriving a country from them does not accidentally activate the check |
| `ip_derived_country` | `VARCHAR(2)`, nullable | story 0054 — an ISO 3166-1 alpha-2 code derived from `ip_address` by a future channel; **deriving one is out of scope of every story shipped so far**, so this column is `NULL` on every row today, exactly like `ip_address`. Matches `billing_country`/`customers.*_country` column-for-column so the comparison below is like-for-like |
| `flag_reason` | `VARCHAR(64)`, nullable | story 0054 — a snake_case token naming *why* an order was flagged, resolved through `lang/{en,es}/orders.php`'s `flag_reasons` group. `NULL` on every row `flagged_for_review` is `false` on, and on most rows it is `true` on too — `ResolveOrderTaxRegion` (0053) sets `flagged_for_review` without ever writing this column, so a flagged **physical** order routinely carries a `NULL` reason; only `ResolveVirtualOrderSalesRegion` (0054) populates it, with one of three tokens: `mixed_basket` (an order mixing physical and virtual line items, written identically by either action — see `ResolveOrderTaxRegion`'s own docblock), `ip_country_missing` (no purchaser IP-derived country was captured — every order this application can currently create, since it has no storefront or checkout) or `billing_ip_country_mismatch` (a captured IP-derived country contradicts the order's own frozen `billing_country`). **No index on any of the three columns above** — nothing joins or filters on them, `ip_derived_country` is a two-character token over a backoffice-sized table, and `flag_reason` is `NULL` on effectively every row; a future "flagged orders" list filter's index belongs to that story with a measurement behind it |
| `shipping_address_line1` / `shipping_address_line2` | `VARCHAR(255)`, nullable | frozen copy of the customer's shipping address at order time (**D-4**) — see [The address snapshot](#the-address-snapshot) below |
| `shipping_city` | `VARCHAR(100)`, nullable | " |
| `shipping_postal_code` | `VARCHAR(20)`, nullable | " |
| `shipping_province` | `VARCHAR(100)`, nullable | " |
| `shipping_country` | `VARCHAR(2)`, nullable | " |
| `billing_address_line1` / `billing_address_line2` | `VARCHAR(255)`, nullable | frozen copy of the customer's billing address at order time — the snapshot `ResolveVirtualOrderSalesRegion` (story 0054) resolves tax from, never `$order->customer`'s live address |
| `billing_city` | `VARCHAR(100)`, nullable | " |
| `billing_postal_code` | `VARCHAR(20)`, nullable | " |
| `billing_province` | `VARCHAR(100)`, nullable | " |
| `billing_country` | `VARCHAR(2)`, nullable | " |
| `created_at` / `updated_at` | timestamp, nullable | |

All twelve address columns mirror [`customers`](schema-other.md#customers) column-for-column in both name and length — deliberately, since they exist to hold an exact copy of a `Customer` row's own address at the moment an order is created.

**No `deleted_at`.** Orders are never deleted this phase; `Cancelled` is a `status` value PRD §3.2 defines, not a soft delete.

**`#[Fillable]`** lists the operator-supplied inputs a new order genuinely needs (`customer_id`, `payment_method_id`) plus the twelve address columns — the address columns are fillable because they are a frozen *copy* written at creation time, not a derived/computed value, and a legitimate future caller (an order-address edit screen) could reasonably need to write them directly. Every other column — `order_number`, `status`, `payment_status`, `subtotal`, `tax_amount`, `shipping_amount`, `total`, `tax_rate`, `flagged_for_review`, `sales_region_id`, `shipping_rate_id` — is **deliberately omitted** and written only via `forceFill()`/`forceCreate()` from [`App\Actions\Orders\CreateOrder`](../../app/Actions/Orders/CreateOrder.php): each is either derived arithmetic, a status a later story owns, or a tax input, and none may ever arrive from a form. Pinned by a test that fails if the omission is undone — the same "omission is this codebase's mass-assignment guard" convention [conventions/base-standards.md](../conventions/base-standards.md#model-conventions) documents.

#### Indexes — exactly the FK-created ones, plus one

`php artisan db:table orders` reports exactly six indexes: `primary` on `id`, `orders_order_number_unique`, and four FK-created indexes (`orders_customer_id_foreign`, `orders_payment_method_id_foreign`, `orders_sales_region_id_foreign`, `orders_shipping_rate_id_foreign`) — verified against a live, migrated instance, not read off the migration. No hand-written `$table->index()` anywhere ([migrations.md](migrations.md#an-fk-column-does-not-also-get-an-explicit-index-here)); `order_number`'s UNIQUE index is the only non-FK index on the table.

### `order_items`

Source: `database/migrations/2026_09_11_162630_create_order_items_table.php` (story 0045), timestamped after `create_orders_table` since it FKs into it.

Model: [`App\Models\OrderItem`](../../app/Models/OrderItem.php). Columns in real physical order (verified with `php artisan db:table order_items`):

| Column | Type | Notes |
| --- | --- | --- |
| `id` | uuid (v7) PK | `CHAR(36)`; generated by `HasUuids` |
| `order_id` | `CHAR(36)` FK → `orders.id`, `cascadeOnDelete()` | the only cascade in this domain — see [Delete behaviour](#delete-behaviour--the-same-three-way-rule-stated-once) below |
| `product_id` | `CHAR(36)` FK → `products.id`, nullable, `nullOnDelete()` | the catalog product this line item references at order time |
| `product_variant_id` | `CHAR(36)` FK → `product_variants.id`, nullable, `nullOnDelete()` | present only when the line item names a specific variant |
| `product_name` | `VARCHAR(255)` | snapshot, matching [`products.name`](schema-products.md#products)'s own length — always the **parent product's** name, even for a variant line item (a variant has no name column of its own) |
| `product_sku` | `VARCHAR(128)` | snapshot, capped at [`product_variants.sku`](schema-products.md#product_variants)'s length (128) rather than `products.sku`'s (64) — the **longer** of the two, since this column holds either |
| `quantity` | `INT UNSIGNED` | required, `≥ 1` |
| `unit_price` | `DECIMAL(10,2)` | the price **at the time of order** — this story's core invariant. See [The price snapshot](#the-price-snapshot--read-from-the-variant-never-the-parent-when-one-is-named) below. **Immutable after insert** — story 0048's `UpdateOrderItemQuantity` is the first (and, as of that story, only) writer of an existing line item, and it never touches this column; see [Totals are derived and re-derived, not write-once](#totals-are-derived-and-re-derived-not-write-once) |
| `line_total` | `DECIMAL(10,2)` | `unit_price × quantity`, computed once at creation |
| `refunded_quantity` | `INT UNSIGNED`, default `0` | story 0051's own running total, incremented by `App\Actions\Orders\RecordRefund` on every refund written against this line item — always equal to the SUM of this item's `refunds` rows' `quantity`. Shipped inert at 0045 (**D-3**); this is its first and only consumer |
| `created_at` / `updated_at` | timestamp, nullable | |

**No `deleted_at`.** A line item's lifecycle is tied entirely to its parent order (`cascadeOnDelete()`), never deleted independently.

**`#[Fillable]`** covers only `product_id`, `product_variant_id` and `quantity` — the three values a caller genuinely supplies. `product_name`, `product_sku`, `unit_price`, `line_total` and `refunded_quantity` are all **deliberately omitted**: every one is derived by `CreateOrder` from the resolved catalog row, and a fillable `unit_price` would hand a caller the ability to set its own price — the single sharpest write in this story. Written only via `OrderItem::forceCreate()`, never the relation's ordinary `create()`, which would silently drop all four derived columns.

#### Indexes — exactly the FK-created ones, no others

`php artisan db:table order_items` reports exactly four indexes: `primary` on `id`, and three FK-created indexes (`order_items_order_id_foreign`, `order_items_product_id_foreign`, `order_items_product_variant_id_foreign`) — verified against a live, migrated instance. No hand-written index anywhere on this table either.

### `refunds`

Source: `database/migrations/2026_09_17_120000_create_refunds_table.php` (story 0051) — a greenfield UUID `create_*` migration, the same shape as `create_sales_regions_table`. `database/migrations/2026_09_17_120001_add_refunded_amount_to_orders_table.php`, one timestamp later, adds `orders.refunded_amount` above.

Model: [`App\Models\Refund`](../../app/Models/Refund.php). Columns in real physical order (verified with `php artisan db:table refunds`):

| Column | Type | Notes |
| --- | --- | --- |
| `id` | uuid (v7) PK | `CHAR(36)`; generated by `HasUuids` |
| `order_item_id` | `CHAR(36)` FK → `order_items.id`, `restrictOnDelete()` | **OQ-1, settled by this story's own implementer** — see [Delete behaviour](#delete-behaviour--the-same-three-way-rule-stated-once) below; overrides the `cascadeOnDelete()` the task file's own migration snippet originally shipped |
| `quantity` | `INT UNSIGNED` | units of this line item returned by this one refund event |
| `amount` | `DECIMAL(10,2)` | `quantity × order_items.unit_price`, snapshotted at refund time (**D-8**) — never re-derived, matching [the price snapshot](#the-price-snapshot--read-from-the-variant-never-the-parent-when-one-is-named) rule below. Scope is **merchandise only** — see `orders.refunded_amount`'s own note above and R-2 |
| `refunded_by` | `CHAR(36)` FK → `users.id`, **not** nullable, `restrictOnDelete()` | the administrator who performed the refund, derived from `Auth::id()` — never accepted as a parameter (**D-10**). `restrictOnDelete()` is effectively insurance: `users` is soft-deleted, so `User::delete()` never fires this FK |
| `reason` | `TEXT`, nullable | the column ships now; this story always writes `null` into it — see `App\Actions\Orders\RecordRefund`'s own `$reason` parameter note (**D-11**, OQ-3) |
| `created_at` / `updated_at` | timestamp, nullable | `created_at` **is** the refund's own event timestamp — no separate `refunded_at` column exists (**D-2**): `refunds` is an event log, and `order_items.refunded_quantity` / `orders.refunded_amount` are the fast running totals derived from it, never the other way around |

**No `deleted_at`.** A refund is a recorded, immutable fact; correcting one is a business decision nobody has asked for (backlog).

**`#[Fillable]`** covers only `order_item_id`, `quantity` and `reason` — the caller's own input. `amount` and `refunded_by` are **deliberately omitted**: `amount` is derived arithmetic over a snapshotted price, and `refunded_by` is the actor's identity, which must be derived from `Auth::id()` and never accepted as input (the 0008a rule). Written only via `Refund::forceCreate()` from `App\Actions\Orders\RecordRefund` alone. Pinned by a test that fails if either omission is undone.

#### Indexes — exactly the FK-created ones, no others

`php artisan db:table refunds` reports exactly three indexes: `primary` on `id`, and two FK-created indexes (`refunds_order_item_id_foreign`, `refunds_refunded_by_foreign`) — verified against a live, migrated instance. No hand-written index anywhere on this table, and no index on `created_at` either — the same low-cardinality/write-cost argument `orders.status` already establishes.

### The price snapshot — read from the variant, never the parent, when one is named

The story's central invariant, and the one every downstream Orders story (refunds, tax) depends on: `order_items.unit_price` / `.product_sku` / `.product_name` are written **once**, at creation, from whatever the catalog says *right now* — never re-derived, never re-read on render. A later change to `products.price` or `product_variants.price`, or the deletion of either row (`nullOnDelete()` above), never moves an existing line item's snapshot. This is the identical rule [`sales_regions.rate`](schema-products.md#sales_regions) and this file's own [address snapshot](#the-address-snapshot) already establish for a different column: never re-derive historical financial (or, there, geographic) data from a mutable source.

**D-15 — a variant line item snapshots its price and SKU from the *variant* row, never the parent product.** `product_variants.price` is `NOT NULL` and fully independent of `products.price` — that is what a variant *is* — so the rule is stated per column, exactly as [`CreateOrder`](../../app/Actions/Orders/CreateOrder.php) implements it:

- `unit_price` ← `product_variants.price` when a variant is named, else `products.price`.
- `product_sku` ← `product_variants.sku` when a variant is named, else `products.sku` (also why `order_items.product_sku` is capped at the longer of the two lengths).
- `product_name` ← always `products.name`, whether or not a variant is named.

This needed its own decision and its own regression test rather than "obviously the variant" — the *wrong* implementation (resolve the product, read `$product->price`, attach the variant id only as a reference) is the natural one to write, and it passes every test whose fixtures happen to give the product and its variant the same price. `tests/Feature/Orders/` pins the regression by mutating the **variant's** own row and re-fetching the order — a test that mutates only the parent product's price cannot detect this failure mode at all.

#### F-1 (Phase 4 security audit) — resolving a related pair of ids: validate the relationship, not two existences

A line item payload carries **two** ids that must belong together — `product_id` and `product_variant_id` — and validating that each individually exists (`Rule::exists('products', 'id')`, `Rule::exists('product_variants', 'id')`) does not prove the two rows are *related*. The Phase 4 security audit on this story found exactly that gap: `CreateOrder`'s first draft resolved the product and the variant as two independent lookups, so a caller holding only `orders.create` could submit an expensive product's id together with an unrelated, cheap product's variant id and have the line item written with the expensive product's **name** at the cheap variant's **price** — real, direct price manipulation, invisible to every check a reviewer would naturally run (both ids exist, both rows are re-read from the database, every value is copied from a real catalog row).

The shipped fix resolves the variant **through** its own item's already-resolved product, in memory, after both collections are bulk-fetched (never one query per item — a separate finding, **F-2**, on the same method): a variant whose `product_id` does not match the item's own resolved product is refused with the identical `ModelNotFoundException` a nonexistent variant would produce, never silently accepted. See [security/related-id-pair-resolution.md](../security/related-id-pair-resolution.md) for the full write-up — the vulnerable shape, why the simplest per-item `$product->variants()->findOrFail()` fix could not be used directly here (it would reintroduce the N+1 the bulk-fetch requirement exists to close), and the two other places in this codebase already checked and found safe against the same failure mode.

### Totals are derived and re-derived, not write-once

Story 0045 wrote `subtotal`/`tax_amount`/`total` once, at creation, from the submitted line items — which reads as write-once until a second writer exists to contradict it. Story 0048's [`App\Actions\Orders\RecalculateOrderTotals`](../../app/Actions/Orders/RecalculateOrderTotals.php) is that second writer: every successful `AddOrderItem` / `RemoveOrderItem` / `UpdateOrderItemQuantity` call recomputes all three columns from scratch, inside the same transaction as the line-item write, by summing `order_items.line_total` fresh rather than incrementally adjusting a stored value. `subtotal += $newLine` was considered and rejected for the same reason story 0048's own D-7 states it: an incremental adjustment drifts the moment any single write is missed or repeated, and the row count on one order is small enough that recomputing from scratch costs nothing worth trading correctness for.

Two invariants this re-derivation preserves, neither obvious from the column list alone:

- **`tax_amount` is `subtotal × (tax_rate ÷ 100)`, computed in exactly one place** — [`App\Actions\Orders\CalculateTaxAmount`](../../app/Actions/Orders/CalculateTaxAmount.php), shared by `RecalculateOrderTotals` and story 0053's `ResolveOrderTaxRegion` (story 0053a). `tax_rate` is a **percentage** (`21.000` means 21%), rounded half-up to two decimals; 0048 originally shipped without the `÷ 100`, which would have produced a tax 100× too large on the first edit of a resolved order.
- **`tax_amount` recomputes only when `tax_rate` is already non-null** (D-8, unchanged from 0045) — a `NULL` `tax_rate` still means "not configured" after an edit, exactly as it does at creation, so `tax_amount` stays `0.00` and no sales region is resolved as a side effect of a line-item edit. `shipping_amount` is read and summed into `total`, never itself recomputed — selecting or pricing a shipping rate is stories 0037/0054's job, not this recomputation's.
- **`order_items.unit_price` is immutable after insert.** `UpdateOrderItemQuantity` re-multiplies the *existing* `unit_price` column by the new quantity (`line_total = unit_price × quantity`) and never re-reads `$item->product->price` — the story's own highest-risk line (D-4/R-1), proven by a dedicated regression test that mutates the product's catalog price *after* the line item exists, then changes only the quantity, and asserts `unit_price` is unmoved. `AddOrderItem` is the only writer that ever sets `unit_price`, and only once, at the moment a line item is created — matching [the price snapshot](#the-price-snapshot--read-from-the-variant-never-the-parent-when-one-is-named) rule above exactly, now confirmed by a second story rather than merely stated by the first.

Both `AssertWithinColumnCeiling` (the shared `decimal(10,2)` overflow guard `CreateOrder` already applies at creation) and the recomputation itself run inside the same `DB::transaction()` as the line-item write, so a rejected edit — the hard block, a validation failure, a column-overflow refusal — never leaves the stored totals out of sync with the rows they describe.

### The address snapshot

**D-4.** `orders` carries its own frozen copy of the customer's shipping and billing addresses — twelve columns, written once from the resolved `Customer` at creation and never re-read from it afterward. Two reasons: the same "never re-derive historical data from a mutable source" rule the price snapshot follows, and a forward one — PRD §3.2's tax resolution (stories 0053/0054) reads "the **order's** shipping address" and "the **billing** address", and if those columns did not exist here, "the order's address" could only mean a live join to `customers` — so a customer moving from one Sales Region to another would silently change the tax basis of every past order. An order for a customer holding no addresses at all (a legitimate state, per [`customers`](schema-other.md#customers)) persists twelve `null`s rather than failing.

### `order_number` generation

**D-1.** A UUIDv7 is unreadable over the phone and unwriteable on a delivery note, so `orders` carries a second, human-readable identifier. Format: `ORD-{YYYY}-{NNNNNN}`, a per-year sequence zero-padded to six digits (`ORD-2026-000001`), computed from a count of existing rows already carrying that year's prefix — comfortably inside `VARCHAR(20)`. This count-based generator is the minimum collision protection, not proof of concurrency safety (**R-1**): two administrators creating an order in the same instant can race it, so the UNIQUE index has the last word — `CreateOrder` catches the resulting `23000`/`1062` `QueryException` and retries the whole transaction, up to five attempts, with a freshly recomputed number each time, rather than ever surfacing the collision. `order_number` is never mass-assignable and never caller-supplied; it is derived and written with `forceFill()` from the action alone.

### Delete behaviour — the same three-way rule stated once

`orders` and `order_items` are the first pair of tables in this codebase to exercise all three of this project's foreign-key delete behaviours **at once**, which is what makes the rule behind them statable as one rule rather than left to be inferred from three separate examples scattered across the schema. See [migrations.md](migrations.md#the-three-way-delete-behaviour-rule-cascade-restrict-or-null) for the full statement and every table on either side of it.

`refunds` adds two more **restrict** instances (story 0051, OQ-1 settled): `order_item_id` refuses to let a line item that already carries refund records be deleted — a hard database invariant rather than a rule a future line-item-removal story would have to remember, matching `sales_regions.parent_id`'s "restrict where a cascade would destroy configured/financial data" precedent — and `refunded_by` restricts for the identical "insurance against a soft-deleted parent" reason `orders.customer_id` already establishes.

### Nothing is resolved that this story does not own

A newly created order's `sales_region_id`, `shipping_rate_id` and `tax_rate` are all `null` (never `0`/`0.000` — see above); `flagged_for_review` is `false`; `tax_amount` and `shipping_amount` are `0.00`; and `total` equals `subtotal`. Every one of these is asserted by a dedicated test rather than left to inspection, because a populated column here would look exactly like a working feature while silently masking stories 0037/0053/0054's actual, not-yet-built work.

### Authorization

Gated by [`App\Policies\OrderPolicy`](../../app/Policies/OrderPolicy.php) — see [architecture/authorization.md](../architecture/authorization.md#orderpolicy--the-twelfth-policy) for the full roster entry. `orders.*` are already-seeded module permissions (`RolePermissionSeeder::MODULES` has carried `orders` since task 0002) — no catalog change, no new permission, no re-seed. `CreateOrder` self-authorizes `create` as its own first statement, through `App\Actions\Auth\LogRefusedPrivilegedAttempt` rather than a bare `Gate::authorize()`, since this story ships no route and no Livewire component — the action is the only reachable enforcement point that exists today.

Story 0048's `AddOrderItem`/`RemoveOrderItem`/`UpdateOrderItemQuantity` self-authorize `update` on the `Order` the identical way, and each layers a **second**, state-based refusal (`App\Exceptions\OrderNotEditableException`, 409) strictly after the permission check when `status` is `Shipped`/`Delivered` — see [architecture/authorization.md](../architecture/authorization.md#order-editability--the-second-state-based-refusal-and-the-shape-three-more-stories-copy) for the full pattern and why it is a direct throw rather than a `Gate`/`OrderPolicy` method.

Story 0051's `App\Actions\Orders\RecordRefund` gates on a **bare** `Gate::authorize('orders.refund')` — a non-CRUD permission (`RolePermissionSeeder::ORDER_PERMISSIONS`), not an `OrderPolicy` ability of its own. Its own second, state-based refusal (payment status must be `Paid`/`PartiallyRefunded`) is a `ValidationException`, not a policy method or a domain exception — see [architecture/authorization.md](../architecture/authorization.md#orderpolicy--the-twelfth-policy) for why that refusal is deliberately kept out of `OrderPolicy`.

Story 0050's `App\Actions\Orders\CancelOrder` **does** gate through an `OrderPolicy` ability — `cancel`, the policy's **sixth** and this app's first requiring TWO permissions (`orders.edit` **and** `orders.refund`, D-6). `App\Models\Order::isManuallyCancellable()` (reading both `status` and `payment_status`) is the state clause both `OrderPolicy::cancel()`'s hint and `CancelOrder`'s own `OrderCancellationBlockedException` direct-throw wrap — see [architecture/authorization.md](../architecture/authorization.md#manual-order-cancellation--the-third-state-based-refusal-and-the-first-ability-requiring-two-permissions) for the full mechanism, including why an ordinary actor's blocked-state refusal (403, via the Gate) differs from a Super Admin's (409, via the direct throw).

_Last updated: 2026-09-21 — Story 0054 (Order tax Sales-Region resolution — virtual products, backend). Added `ip_address` (`VARCHAR(45)`), `ip_derived_country` (`VARCHAR(2)`) and `flag_reason` (`VARCHAR(64)`), all nullable — the geo/fraud-check mechanism PRD §3.2 describes, complete and under test but **dormant**: with no storefront or checkout, every order this application can currently create carries no IP data and is never flagged for that reason (see the `ip_address` row for the full explanation, placed beside the columns rather than only in the task file). Extended `sales_region_id`/`tax_rate`/`tax_amount`/`flagged_for_review`/`billing_address_line1` with `ResolveVirtualOrderSalesRegion`'s own write. No index on any of the three new columns.

_Previously: 2026-09-17 — Story 0050 (Order manual cancellation backend). Extended **Authorization** with `CancelOrder`'s `cancel` ability (this policy's sixth, and the first requiring two permissions) and `Order::isManuallyCancellable()`. No column, table, index or schema change of any kind — verified rather than assumed, since this story reuses `orders.status`/`orders.payment_status` entirely.

_Previously: 2026-09-17 — Story 0051 (Order payment/refund state backend). Added [`refunds`](#refunds): the refund event log, `orders.refunded_amount` (the running merchandise-only total it is summed into), and `order_items.refunded_quantity`'s first real consumer. Extended [Delete behaviour](#delete-behaviour--the-same-three-way-rule-stated-once) with `refunds`' two `restrictOnDelete()` FKs (OQ-1, settled in this story's own migration — overriding the task file's originally-contributed `cascadeOnDelete()`). Extended **Authorization** with `RecordRefund`'s bare-permission-string gate and its `ValidationException` state refusal.

_Previously: 2026-09-16 — Story 0048 (Order line-item editing backend). Added [Totals are derived and re-derived, not write-once](#totals-are-derived-and-re-derived-not-write-once): `subtotal`/`tax_amount`/`total` are no longer write-once (`App\Actions\Orders\RecalculateOrderTotals` recomputes all three from scratch, inside the transaction, on every `AddOrderItem`/`RemoveOrderItem`/`UpdateOrderItemQuantity` call), and `order_items.unit_price` is immutable after insert (`UpdateOrderItemQuantity` re-multiplies the existing column, never re-reads the catalog). Noted both directly on the `orders`/`order_items` column tables too. Extended the **Authorization** section with this story's second, state-based refusal (`OrderNotEditableException`, 409) alongside `OrderPolicy`'s permission check. No column, type or index change.

_Previously: 2026-09-14 — Story 0045 (Orders core CRUD backend). New file — `orders`/`order_items` split out from the outset rather than appended to [schema-other.md](schema-other.md), per [contracts.md](../contracts.md#doc-growth-management-rule)'s doc growth management rule, since Epic 3's remaining Orders stories (0046–0055) will all extend this domain rather than the payment-methods/customers/notifications one. Linked from [schema.md](schema.md#domain-tables)._
